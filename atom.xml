<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://giriawsh.github.io/</id>
    <title>Giria&apos;s Blog</title>
    <updated>2020-10-06T14:30:10.918Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://giriawsh.github.io/"/>
    <link rel="self" href="https://giriawsh.github.io/atom.xml"/>
    <subtitle>Live only once, make it count</subtitle>
    <logo>https://giriawsh.github.io/images/avatar.png</logo>
    <icon>https://giriawsh.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Giria&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[NodeJS初学系列5——OS/Path/Net/DNS/Domain/Web模块]]></title>
        <id>https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-5-ospathnetdnsdomainweb-mo-kuai/</id>
        <link href="https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-5-ospathnetdnsdomainweb-mo-kuai/">
        </link>
        <updated>2020-10-06T14:28:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="os模块">OS模块</h2>
<p>Node.js os 模块提供了一些基本的系统操作函数。我们可以通过以下方式引入该模块：</p>
<pre><code class="language-js">var os = require(&quot;os&quot;)
</code></pre>
<h3 id="方法">方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>os.tmpdir()</strong> 返回操作系统的默认临时文件夹。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>os.endianness()</strong> 返回 CPU 的字节序，可能的是 &quot;BE&quot; 或 &quot;LE&quot;。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>os.hostname()</strong> 返回操作系统的主机名。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>os.type()</strong> 返回操作系统名</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>os.platform()</strong> 返回编译时的操作系统名</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>os.arch()</strong> 返回操作系统 CPU 架构，可能的值有 &quot;x64&quot;、&quot;arm&quot; 和 &quot;ia32&quot;。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>os.release()</strong> 返回操作系统的发行版本。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>os.uptime()</strong> 返回操作系统运行的时间，以秒为单位。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>os.loadavg()</strong> 返回一个包含 1、5、15 分钟平均负载的数组。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>os.totalmem()</strong> 返回系统内存总量，单位为字节。</td>
</tr>
<tr>
<td style="text-align:left">11</td>
<td style="text-align:left"><strong>os.freemem()</strong> 返回操作系统空闲内存量，单位是字节。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>os.cpus()</strong> 返回一个对象数组，包含所安装的每个 CPU/内核的信息：型号、速度（单位 MHz）、时间（一个包含 user、nice、sys、idle 和 irq 所使用 CPU/内核毫秒数的对象）。</td>
</tr>
<tr>
<td style="text-align:left">13</td>
<td style="text-align:left"><strong>os.networkInterfaces()</strong> 获得网络接口列表。</td>
</tr>
</tbody>
</table>
<h3 id="属性">属性</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">属性 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>os.EOL</strong> 定义了操作系统的行尾符的常量。</td>
</tr>
</tbody>
</table>
<h3 id="实例">实例</h3>
<pre><code class="language-js">var os = require(&quot;os&quot;);

// CPU 的字节序
console.log('endianness : ' + os.endianness());

// 操作系统名
console.log('type : ' + os.type());

// 编译时操作系统名
console.log('platform : ' + os.platform());

// 系统内存总量
console.log('total memory : ' + os.totalmem() + &quot; bytes.&quot;);

// 操作系统空闲内存量
console.log('free memory : ' + os.freemem() + &quot; bytes.&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>➜  os-demo node main.js
endianness : LE
type : Windows_NT
platform : win32
total memory : 8534626304 bytes.
free memory : 2915053568 bytes.
</code></pre>
<h2 id="path模块">Path模块</h2>
<p>Node.js path 模块提供了一些用于处理文件路径的小工具，我们可以通过以下方式引入该模块：</p>
<pre><code class="language-js">var path = require(&quot;path&quot;)
</code></pre>
<h3 id="方法-2">方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>path.normalize(p)</strong> 规范化路径，注意'..' 和 '.'。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>path.join([path1][, path2][, ...])</strong> 用于连接路径。该方法的主要用途在于，会正确使用当前系统的路径分隔符，Unix系统是&quot;/&quot;，Windows系统是&quot;&quot;。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>path.resolve([from ...], to)</strong> 将 <strong>to</strong> 参数解析为绝对路径，给定的路径的序列是从右往左被处理的，后面每个 path 被依次解析，直到构造完成一个绝对路径。 例如，给定的路径片段的序列为：/foo、/bar、baz，则调用 path.resolve('/foo', '/bar', 'baz') 会返回 /bar/baz。<code>path.resolve('/foo/bar', './baz'); // 返回: '/foo/bar/baz' path.resolve('/foo/bar', '/tmp/file/'); // 返回: '/tmp/file' path.resolve('wwwroot', 'static_files/png/', '../gif/image.gif'); // 如果当前工作目录为 /home/myself/node， // 则返回 '/home/myself/node/wwwroot/static_files/gif/image.gif'</code></td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>path.isAbsolute(path)</strong> 判断参数 <strong>path</strong> 是否是绝对路径。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>path.relative(from, to)</strong> 用于将绝对路径转为相对路径，返回从 from 到 to 的相对路径（基于当前工作目录）。在 Linux 上：<code>path.relative('/data/orandea/test/aaa', '/data/orandea/impl/bbb'); // 返回: '../../impl/bbb'</code>在 Windows 上：<code>path.relative('C:\\orandea\\test\\aaa', 'C:\\orandea\\impl\\bbb'); // 返回: '..\\..\\impl\\bbb'</code></td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>path.dirname(p)</strong> 返回路径中代表文件夹的部分，同 Unix 的dirname 命令类似。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>path.basename(p[, ext])</strong> 返回路径中的最后一部分。同 Unix 命令 bashname 类似。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>path.extname(p)</strong> 返回路径中文件的后缀名，即路径中最后一个'.'之后的部分。如果一个路径中并不包含'.'或该路径只包含一个'.' 且这个'.'为路径的第一个字符，则此命令返回空字符串。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>path.parse(pathString)</strong> 返回路径字符串的对象。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>path.format(pathObject)</strong> 从对象中返回路径字符串，和 path.parse 相反。</td>
</tr>
</tbody>
</table>
<h3 id="属性-2">属性</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">属性 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>path.sep</strong> 平台的文件路径分隔符，'\' 或 '/'。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>path.delimiter</strong> 平台的分隔符, ; or ':'.</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>path.posix</strong> 提供上述 path 的方法，不过总是以 posix 兼容的方式交互。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>path.win32</strong> 提供上述 path 的方法，不过总是以 win32 兼容的方式交互。</td>
</tr>
</tbody>
</table>
<h3 id="实例-2">实例</h3>
<pre><code class="language-js">var path = require(&quot;path&quot;);

// 格式化路径
console.log('normalization : ' + path.normalize('/test/test1//2slashes/1slash/tab/..'));

// 连接路径
console.log('joint path : ' + path.join('/test', 'test1', '2slashes/1slash', 'tab', '..', '.'));

// 转换为绝对路径
console.log('resolve : ' + path.resolve('main.js'));

// 路径中文件的后缀名
console.log('ext name : ' + path.extname('main.js'));
</code></pre>
<p>代码执行结果：</p>
<pre><code>➜  path-demo node main.js
normalization : \test\test1\2slashes\1slash
joint path : \test\test1\2slashes\1slash
resolve : K:\projects\node\first-test\path-demo\main.js
ext name : .js
</code></pre>
<h2 id="net模块">Net模块</h2>
<p>Node.js Net 模块提供了一些用于底层的网络通信的小工具，包含了创建服务器/客户端的方法，我们可以通过以下方式引入该模块：</p>
<pre><code class="language-js">var net = require(&quot;net&quot;)
</code></pre>
<h3 id="方法-3">方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>net.createServer([options][, connectionListener])</strong> 创建一个 TCP 服务器。参数 connectionListener 自动给 'connection' 事件创建监听器。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>net.connect(options[, connectionListener])</strong> 返回一个新的 'net.Socket'，并连接到指定的地址和端口。 当 socket 建立的时候，将会触发 'connect' 事件。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>net.createConnection(options[, connectionListener])</strong> 创建一个到端口 port 和 主机 host的 TCP 连接。 host 默认为 'localhost'。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>net.connect(port[, host][, connectListener])</strong> 创建一个端口为 port 和主机为 host的 TCP 连接 。host 默认为 'localhost'。参数 connectListener 将会作为监听器添加到 'connect' 事件。返回 'net.Socket'。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>net.createConnection(port[, host][, connectListener])</strong> 创建一个端口为 port 和主机为 host的 TCP 连接 。host 默认为 'localhost'。参数 connectListener 将会作为监听器添加到 'connect' 事件。返回 'net.Socket'。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>net.connect(path[, connectListener])</strong> 创建连接到 path 的 unix socket 。参数 connectListener 将会作为监听器添加到 'connect' 事件上。返回 'net.Socket'。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>net.createConnection(path[, connectListener])</strong> 创建连接到 path 的 unix socket 。参数 connectListener 将会作为监听器添加到 'connect' 事件。返回 'net.Socket'。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>net.isIP(input)</strong> 检测输入的是否为 IP 地址。 IPV4 返回 4， IPV6 返回 6，其他情况返回 0。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>net.isIPv4(input)</strong> 如果输入的地址为 IPV4， 返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>net.isIPv6(input)</strong> 如果输入的地址为 IPV6， 返回 true，否则返回 false。</td>
</tr>
</tbody>
</table>
<h3 id="netserver">net.Server</h3>
<p>net.Server通常用于创建一个 TCP 或本地服务器。</p>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>server.listen(port[, host][, backlog][, callback])</strong> 监听指定端口 port 和 主机 host ac连接。 默认情况下 host 接受任何 IPv4 地址(INADDR_ANY)的直接连接。端口 port 为 0 时，则会分配一个随机端口。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>server.listen(path[, callback])</strong> 通过指定 path 的连接，启动一个本地 socket 服务器。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>server.listen(handle[, callback])</strong> 通过指定句柄连接。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>server.listen(options[, callback])</strong> options 的属性：端口 port, 主机 host, 和 backlog, 以及可选参数 callback 函数, 他们在一起调用server.listen(port, [host], [backlog], [callback])。还有，参数 path 可以用来指定 UNIX socket。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>server.close([callback])</strong> 服务器停止接收新的连接，保持现有连接。这是异步函数，当所有连接结束的时候服务器会关闭，并会触发 'close' 事件。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>server.address()</strong> 操作系统返回绑定的地址，协议族名和服务器端口。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>server.unref()</strong> 如果这是事件系统中唯一一个活动的服务器，调用 unref 将允许程序退出。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>server.ref()</strong> 与 unref 相反，如果这是唯一的服务器，在之前被 unref 了的服务器上调用 ref 将不会让程序退出（默认行为）。如果服务器已经被 ref，则再次调用 ref 并不会产生影响。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>server.getConnections(callback)</strong> 异步获取服务器当前活跃连接的数量。当 socket 发送给子进程后才有效；回调函数有 2 个参数 err 和 count。</td>
</tr>
</tbody>
</table>
<h4 id="事件">事件</h4>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">事件 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>listening</strong> 当服务器调用 server.listen 绑定后会触发。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>connection</strong> 当新连接创建后会被触发。socket 是 net.Socket实例。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>close</strong> 服务器关闭时会触发。注意，如果存在连接，这个事件不会被触发直到所有的连接关闭。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>error</strong> 发生错误时触发。'close' 事件将被下列事件直接调用。</td>
</tr>
</tbody>
</table>
<h3 id="netsocket">net.Socket</h3>
<p>net.Socket 对象是 TCP 或 UNIX Socket 的抽象。net.Socket 实例实现了一个双工流接口。 他们可以在用户创建客户端(使用 connect())时使用, 或者由 Node 创建它们，并通过 connection 服务器事件传递给用户。</p>
<h4 id="事件-2">事件</h4>
<p>net.Socket 事件有：</p>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">事件 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>lookup</strong> 在解析域名后，但在连接前，触发这个事件。对 UNIX sokcet 不适用。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>connect</strong> 成功建立 socket 连接时触发。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>data</strong> 当接收到数据时触发。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>end</strong> 当 socket 另一端发送 FIN 包时，触发该事件。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>timeout</strong> 当 socket 空闲超时时触发，仅是表明 socket 已经空闲。用户必须手动关闭连接。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>drain</strong> 当写缓存为空得时候触发。可用来控制上传。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>error</strong> 错误发生时触发。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>close</strong> 当 socket 完全关闭时触发。参数 had_error 是布尔值，它表示是否因为传输错误导致 socket 关闭。</td>
</tr>
</tbody>
</table>
<h4 id="属性-3">属性</h4>
<p>net.Socket 提供了很多有用的属性，便于控制 socket 交互：</p>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">属性 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>socket.bufferSize</strong> 该属性显示了要写入缓冲区的字节数。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>socket.remoteAddress</strong> 远程的 IP 地址字符串，例如：'74.125.127.100' or '2001:4860:a005::68'。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>socket.remoteFamily</strong> 远程IP协议族字符串，比如 'IPv4' or 'IPv6'。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>socket.remotePort</strong> 远程端口，数字表示，例如：80 or 21。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>socket.localAddress</strong> 网络连接绑定的本地接口 远程客户端正在连接的本地 IP 地址，字符串表示。例如，如果你在监听'0.0.0.0'而客户端连接在'192.168.1.1'，这个值就会是 '192.168.1.1'。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>socket.localPort</strong> 本地端口地址，数字表示。例如：80 or 21。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>socket.bytesRead</strong> 接收到得字节数。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>socket.bytesWritten</strong> 发送的字节数。</td>
</tr>
</tbody>
</table>
<h4 id="方法-4">方法</h4>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>new net.Socket([options])</strong> 构造一个新的 socket 对象。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>socket.connect(port[, host][, connectListener])</strong> 指定端口 port 和 主机 host，创建 socket 连接 。参数 host 默认为 localhost。通常情况不需要使用 net.createConnection 打开 socket。只有你实现了自己的 socket 时才会用到。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>socket.connect(path[, connectListener])</strong> 打开指定路径的 unix socket。通常情况不需要使用 net.createConnection 打开 socket。只有你实现了自己的 socket 时才会用到。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>socket.setEncoding([encoding])</strong> 设置编码</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>socket.write(data[, encoding][, callback])</strong> 在 socket 上发送数据。第二个参数指定了字符串的编码，默认是 UTF8 编码。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>socket.end([data][, encoding])</strong> 半关闭 socket。例如，它发送一个 FIN 包。可能服务器仍在发送数据。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>socket.destroy()</strong> 确保没有 I/O 活动在这个套接字上。只有在错误发生情况下才需要。（处理错误等等）。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>socket.pause()</strong> 暂停读取数据。就是说，不会再触发 data 事件。对于控制上传非常有用。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>socket.resume()</strong> 调用 pause() 后想恢复读取数据。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>socket.setTimeout(timeout[, callback])</strong> socket 闲置时间超过 timeout 毫秒后 ，将 socket 设置为超时。</td>
</tr>
<tr>
<td style="text-align:left">11</td>
<td style="text-align:left"><strong>socket.setNoDelay([noDelay])</strong> 禁用纳格（Nagle）算法。默认情况下 TCP 连接使用纳格算法，在发送前他们会缓冲数据。将 noDelay 设置为 true 将会在调用 socket.write() 时立即发送数据。noDelay 默认值为 true。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>socket.setKeepAlive([enable][, initialDelay])</strong> 禁用/启用长连接功能，并在发送第一个在闲置 socket 上的长连接 probe 之前，可选地设定初始延时。默认为 false。 设定 initialDelay （毫秒），来设定收到的最后一个数据包和第一个长连接probe之间的延时。将 initialDelay 设为0，将会保留默认（或者之前）的值。默认值为0.</td>
</tr>
<tr>
<td style="text-align:left">13</td>
<td style="text-align:left"><strong>socket.address()</strong> 操作系统返回绑定的地址，协议族名和服务器端口。返回的对象有 3 个属性，比如{ port: 12346, family: 'IPv4', address: '127.0.0.1' }。</td>
</tr>
<tr>
<td style="text-align:left">14</td>
<td style="text-align:left"><strong>socket.unref()</strong> 如果这是事件系统中唯一一个活动的服务器，调用 unref 将允许程序退出。如果服务器已被 unref，则再次调用 unref 并不会产生影响。</td>
</tr>
<tr>
<td style="text-align:left">15</td>
<td style="text-align:left"><strong>socket.ref()</strong> 与 unref 相反，如果这是唯一的服务器，在之前被 unref 了的服务器上调用 ref 将不会让程序退出（默认行为）。如果服务器已经被 ref，则再次调用 ref 并不会产生影响。</td>
</tr>
</tbody>
</table>
<h3 id="实例-3">实例</h3>
<p>创建 server.js 文件，代码如下所示：</p>
<pre><code class="language-js">var net = require('net');
var server = net.createServer(function(connection) { 
   console.log('client connected');
   connection.on('end', function() {
      console.log('客户端关闭连接');
   });
   connection.write('Hello World!\r\n');
   connection.pipe(connection);
});
server.listen(8080, function() { 
  console.log('server is listening');
});
</code></pre>
<p>执行以上服务端代码：</p>
<pre><code>$ node server.js
server is listening   # 服务已创建并监听 8080 端口
</code></pre>
<p>新开一个窗口，创建 client.js 文件，代码如下所示：</p>
<pre><code class="language-js">var net = require('net');
var client = net.connect({port: 8080}, function() {
   console.log('连接到服务器！');  
});
client.on('data', function(data) {
   console.log(data.toString());
   client.end();
});
client.on('end', function() { 
   console.log('断开与服务器的连接');
});
</code></pre>
<p>执行以上客户端的代码：</p>
<pre><code>连接到服务器！
Hello World!

断开与服务器的连接
</code></pre>
<h2 id="dns模块">DNS模块</h2>
<p>Node.js <strong>DNS</strong> 模块用于解析域名。引入 DNS 模块语法格式如下：</p>
<pre><code class="language-js">var dns = require(&quot;dns&quot;)
</code></pre>
<h3 id="方法-5">方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>dns.lookup(hostname[, options], callback)</strong> 将域名（比如 'runoob.com'）解析为第一条找到的记录 A （IPV4）或 AAAA(IPV6)。参数 options可以是一个对象或整数。如果没有提供 options，IP v4 和 v6 地址都可以。如果 options 是整数，则必须是 4 或 6。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>dns.lookupService(address, port, callback)</strong> 使用 getnameinfo 解析传入的地址和端口为域名和服务。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>dns.resolve(hostname[, rrtype], callback)</strong> 将一个域名（如 'runoob.com'）解析为一个 rrtype 指定记录类型的数组。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>dns.resolve4(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能查询 IPv4 (A 记录）。 addresses IPv4 地址数组 (比如，['74.125.79.104', '74.125.79.105', '74.125.79.106']）。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>dns.resolve6(hostname, callback)</strong> 和 dns.resolve4() 类似， 仅能查询 IPv6( AAAA 查询）</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>dns.resolveMx(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能查询邮件交换(MX 记录)。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>dns.resolveTxt(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能进行文本查询 (TXT 记录）。 addresses 是 2-d 文本记录数组。(比如，[ ['v=spf1 ip4:0.0.0.0 ', '~all' ] ]）。 每个子数组包含一条记录的 TXT 块。根据使用情况可以连接在一起，也可单独使用。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>dns.resolveSrv(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能进行服务记录查询 (SRV 记录）。 addresses 是 hostname可用的 SRV 记录数组。 SRV 记录属性有优先级（priority），权重（weight）, 端口（port）, 和名字（name） (比如，[{'priority': 10, 'weight': 5, 'port': 21223, 'name': 'service.example.com'}, ...]）。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>dns.resolveSoa(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能查询权威记录(SOA 记录）。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>dns.resolveNs(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能进行域名服务器记录查询(NS 记录）。 addresses 是域名服务器记录数组（hostname 可以使用） (比如, ['ns1.example.com', 'ns2.example.com']）。</td>
</tr>
<tr>
<td style="text-align:left">11</td>
<td style="text-align:left"><strong>dns.resolveCname(hostname, callback)</strong> 和 dns.resolve() 类似, 仅能进行别名记录查询 (CNAME记录)。addresses 是对 hostname 可用的别名记录数组 (比如，, ['bar.example.com']）。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>dns.reverse(ip, callback)</strong> 反向解析 IP 地址，指向该 IP 地址的域名数组。</td>
</tr>
<tr>
<td style="text-align:left">13</td>
<td style="text-align:left"><strong>dns.getServers()</strong> 返回一个用于当前解析的 IP 地址数组的字符串。</td>
</tr>
<tr>
<td style="text-align:left">14</td>
<td style="text-align:left"><strong>dns.setServers(servers)</strong> 指定一组 IP 地址作为解析服务器。</td>
</tr>
</tbody>
</table>
<h3 id="rrtypes">rrtypes</h3>
<p>以下列出了 dns.resolve() 方法中有效的 rrtypes值:</p>
<ul>
<li><code>'A'</code> IPV4 地址, 默认</li>
<li><code>'AAAA'</code> IPV6 地址</li>
<li><code>'MX'</code> 邮件交换记录</li>
<li><code>'TXT'</code> text 记录</li>
<li><code>'SRV'</code> SRV 记录</li>
<li><code>'PTR'</code> 用来反向 IP 查找</li>
<li><code>'NS'</code> 域名服务器记录</li>
<li><code>'CNAME'</code> 别名记录</li>
<li><code>'SOA'</code> 授权记录的初始值</li>
</ul>
<hr>
<h3 id="错误码">错误码</h3>
<p>每次 DNS 查询都可能返回以下错误码：</p>
<ul>
<li><code>dns.NODATA</code>: 无数据响应。</li>
<li><code>dns.FORMERR</code>: 查询格式错误。</li>
<li><code>dns.SERVFAIL</code>: 常规失败。</li>
<li><code>dns.NOTFOUND</code>: 没有找到域名。</li>
<li><code>dns.NOTIMP</code>: 未实现请求的操作。</li>
<li><code>dns.REFUSED</code>: 拒绝查询。</li>
<li><code>dns.BADQUERY</code>: 查询格式错误。</li>
<li><code>dns.BADNAME</code>: 域名格式错误。</li>
<li><code>dns.BADFAMILY</code>: 地址协议不支持。</li>
<li><code>dns.BADRESP</code>: 回复格式错误。</li>
<li><code>dns.CONNREFUSED</code>: 无法连接到 DNS 服务器。</li>
<li><code>dns.TIMEOUT</code>: 连接 DNS 服务器超时。</li>
<li><code>dns.EOF</code>: 文件末端。</li>
<li><code>dns.FILE</code>: 读文件错误。</li>
<li><code>dns.NOMEM</code>: 内存溢出。</li>
<li><code>dns.DESTRUCTION</code>: 通道被摧毁。</li>
<li><code>dns.BADSTR</code>: 字符串格式错误。</li>
<li><code>dns.BADFLAGS</code>: 非法标识符。</li>
<li><code>dns.NONAME</code>: 所给主机不是数字。</li>
<li><code>dns.BADHINTS</code>: 非法HINTS标识符。</li>
<li><code>dns.NOTINITIALIZED</code>: c c-ares 库尚未初始化。</li>
<li><code>dns.LOADIPHLPAPI</code>: 加载 iphlpapi.dll 出错。</li>
<li><code>dns.ADDRGETNETWORKPARAMS</code>: 无法找到 GetNetworkParams 函数。</li>
<li><code>dns.CANCELLED</code>: 取消 DNS 查询。</li>
</ul>
<h3 id="实例-4">实例</h3>
<p>创建 main.js 文件，代码如下所示：</p>
<pre><code class="language-js">var dns = require('dns');

dns.lookup('www.github.com', function onLookup(err, address, family) {
   console.log('ip 地址:', address);
   dns.reverse(address, function (err, hostnames) {
   if (err) {
      console.log(err.stack);
   }

   console.log('反向解析 ' + address + ': ' + JSON.stringify(hostnames));
});  
});
</code></pre>
<p>执行结果：</p>
<pre><code>➜  dns-demo node main.js
ip 地址: 52.69.186.44
反向解析 52.69.186.44: [&quot;ec2-52-69-186-44.ap-northeast-1.compute.amazonaws.com&quot;]
</code></pre>
<h2 id="domain模块">Domain模块</h2>
<p>Node.js <strong>Domain(域)</strong> 简化异步代码的异常处理，可以捕捉处理try catch无法捕捉的异常。引入 Domain 模块 语法格式如下：</p>
<pre><code class="language-js">var domain = require(&quot;domain&quot;)
</code></pre>
<p>domain模块，把处理多个不同的IO的操作作为一个组。注册事件和回调到domain，<strong>当发生一个错误事件或抛出一个错误时，domain对象会被通知，不会丢失上下文环境，也不导致程序错误立即退出</strong>，与process.on('uncaughtException')不同。</p>
<p>Domain 模块可分为隐式绑定和显式绑定：</p>
<ul>
<li>隐式绑定: 把在domain上下文中定义的变量，自动绑定到domain对象</li>
<li>显式绑定: 把不是在domain上下文中定义的变量，以代码的方式绑定到domain对象</li>
</ul>
<h3 id="方法-6">方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>domain.run(function)</strong> 在域的上下文运行提供的函数，隐式的绑定了所有的事件分发器，计时器和底层请求。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>domain.add(emitter)</strong> 显式的增加事件</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>domain.remove(emitter)</strong> 删除事件。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>domain.bind(callback)</strong> 返回的函数是一个对于所提供的回调函数的包装函数。当调用这个返回的函数时，所有被抛出的错误都会被导向到这个域的 error 事件。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>domain.intercept(callback)</strong> 和 domain.bind(callback) 类似。除了捕捉被抛出的错误外，它还会拦截 Error 对象作为参数传递到这个函数。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>domain.enter()</strong> 进入一个异步调用的上下文，绑定到domain。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>domain.exit()</strong> 退出当前的domain，切换到不同的链的异步调用的上下文中。对应domain.enter()。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>domain.dispose()</strong> 释放一个domain对象，让node进程回收这部分资源。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>domain.create()</strong> 返回一个domain对象。</td>
</tr>
</tbody>
</table>
<h3 id="属性-4">属性</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">属性 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>domain.members</strong> 已加入domain对象的域定时器和事件发射器的数组。</td>
</tr>
</tbody>
</table>
<h3 id="实例-5">实例</h3>
<pre><code class="language-js">var EventEmitter = require(&quot;events&quot;).EventEmitter;
var domain = require(&quot;domain&quot;);

var emitter1 = new EventEmitter();

// 创建域
var domain1 = domain.create();

domain1.on('error', function(err){
    console.log(&quot;domain1 处理这个错误 (&quot;+err.message+&quot;)&quot;);
});

// 显式绑定
domain1.add(emitter1);

emitter1.on('error',function(err){
    console.log(&quot;监听器处理此错误 (&quot;+err.message+&quot;)&quot;);
});

emitter1.emit('error',new Error('通过监听器来处理'));

emitter1.removeAllListeners('error');

emitter1.emit('error',new Error('通过 domain1 处理'));

var domain2 = domain.create();

domain2.on('error', function(err){
    console.log(&quot;domain2 处理这个错误 (&quot;+err.message+&quot;)&quot;);
});

// 隐式绑定
domain2.run(function(){
    var emitter2 = new EventEmitter();
    emitter2.emit('error',new Error('通过 domain2 处理'));
});


domain1.remove(emitter1);
emitter1.emit('error', new Error('转换为异常，系统将崩溃!'));//无处理器处理了
</code></pre>
<p>执行结果：</p>
<pre><code>➜  domain-demo node main.js
监听器处理此错误 (通过监听器来处理)
domain1 处理这个错误 (通过 domain1 处理)
domain2 处理这个错误 (通过 domain2 处理)
events.js:187
      throw er; // Unhandled 'error' event
      ^

Error: 转换为异常，系统将崩溃!
</code></pre>
<h2 id="web模块">Web模块</h2>
<h3 id="web-服务器">Web 服务器</h3>
<p>Web服务器一般指网站服务器，是指驻留于因特网上某种类型计算机的程序，Web服务器的基本功能就是提供Web信息浏览服务。它只需支持HTTP协议、HTML文档格式及URL，与客户端的网络浏览器配合。</p>
<p>大多数 web 服务器都支持服务端的脚本语言（php、python、ruby）等，并通过脚本语言从数据库获取数据，将结果返回给客户端浏览器。</p>
<p>目前最主流的三个Web服务器是Apache、Nginx、IIS。</p>
<h3 id="web-应用架构">Web 应用架构</h3>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201006180902.jpeg" alt="img" loading="lazy"></figure>
<ul>
<li><strong>Client</strong> - 客户端，一般指<strong>浏览器</strong>，浏览器可以通过 HTTP 协议向服务器请求数据。</li>
<li><strong>Server</strong> - 服务端，一般指 <strong>Web 服务器</strong>，可以接收客户端请求，并向客户端发送响应数据。</li>
<li><strong>Business</strong> - 业务层， 通过 <strong>Web 服务器处理应用程序</strong>，如与数据库交互，逻辑运算，调用外部程序等。</li>
<li><strong>Data</strong> - 数据层，一般由<strong>数据库</strong>组成。</li>
</ul>
<h3 id="使用-node-创建-web-服务器">使用 Node 创建 Web 服务器</h3>
<p>Node.js 提供了 http 模块，http 模块主要用于搭建 HTTP 服务端和客户端，使用 HTTP 服务器或客户端功能必须调用 http 模块，代码如下：</p>
<pre><code class="language-js">var http = require('http');
</code></pre>
<p>演示一个最基本的 HTTP 服务器架构(使用 8080 端口)，创建 server.js 文件</p>
<pre><code class="language-js">var http = require('http');
var fs = require('fs');
var url = require('url');
 
 
// 创建服务器
http.createServer( function (request, response) {  
   // 解析请求，包括文件名
   var pathname = url.parse(request.url).pathname;
   
   // 输出请求的文件名
   console.log(&quot;Request for &quot; + pathname + &quot; received.&quot;);
   
   // 从文件系统中读取请求的文件内容
   fs.readFile(pathname.substr(1), function (err, data) {
      if (err) {
         console.log(err);
         // HTTP 状态码: 404 : NOT FOUND
         // Content Type: text/html
         response.writeHead(404, {'Content-Type': 'text/html'});
      }else{             
         // HTTP 状态码: 200 : OK
         // Content Type: text/html
         response.writeHead(200, {'Content-Type': 'text/html'});    
         
         // 响应文件内容
         response.write(data.toString());        
      }
      //  发送响应数据
      response.end();
   });   
}).listen(8080);
 
// 控制台会输出以下信息
console.log('Server running at http://127.0.0.1:8080/');
</code></pre>
<p>接下来在该目录下创建一个 index.html 文件，代码如下：</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;title&gt;菜鸟教程(runoob.com)&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;h1&gt;我的第一个标题&lt;/h1&gt;
    &lt;p&gt;我的第一个段落。&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>执行 server.js 文件：</p>
<pre><code>$ node server.js
Server running at http://127.0.0.1:8080/
</code></pre>
<p>接着我们在浏览器中打开地址：<strong>http://127.0.0.1:8080/index.html</strong>，显示如下图所示:</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201006222624.jpeg" alt="img" loading="lazy"></figure>
<p>执行 server.js 的控制台输出信息如下：</p>
<pre><code>Server running at http://127.0.0.1:8080/
Request for /index.html received.     #  客户端请求信息
</code></pre>
<hr>
<h3 id="使用-node-创建-web-客户端">使用 Node 创建 Web 客户端</h3>
<p>Node 创建 Web 客户端需要引入 http 模块，创建 client.js 文件，代码如下所示：</p>
<pre><code class="language-js">var http = require('http');
 
// 用于请求的选项
var options = {
   host: 'localhost',
   port: '8080',
   path: '/index.html'  
};
 
// 处理响应的回调函数
var callback = function(response){
   // 不断更新数据
   var body = '';
   response.on('data', function(data) {
      body += data;
   });
   
   response.on('end', function() {
      // 数据接收完成
      console.log(body);
   });
}
// 向服务端发送请求
var req = http.request(options, callback);
req.end();
</code></pre>
<p><strong>新开一个终端</strong>，执行 client.js 文件，输出结果如下：</p>
<pre><code class="language-shell">$ node  client.js 
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset=&quot;utf-8&quot;&gt;
&lt;title&gt;菜鸟教程(runoob.com)&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
    &lt;h1&gt;我的第一个标题&lt;/h1&gt;
    &lt;p&gt;我的第一个段落。&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>执行 server.js 的控制台输出信息如下：</p>
<pre><code>Server running at http://127.0.0.1:8080/
Request for /index.html received.   # 客户端请求信息
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[多个GithubPages+Vuepress+二级域名配置]]></title>
        <id>https://giriawsh.github.io/post/duo-ge-githubpagesvuepresser-ji-yu-ming-pei-zhi/</id>
        <link href="https://giriawsh.github.io/post/duo-ge-githubpagesvuepresser-ji-yu-ming-pei-zhi/">
        </link>
        <updated>2020-10-05T13:36:51.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>配置完这个小站以后嗅到了一丝不对。</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>配置完这个小站以后嗅到了一丝不对。</p>
<!-- more -->
<p>咦，我怎么学习记录的东西这么多，这样放一块儿实在不好看也不适合梳理。</p>
<p>在语雀和Vuepress中选择了UI更简洁清爽，同时界面关键内容较为突出的Vuepress。</p>
<p>此文记录了如何创建第二个GithubPages（多个也类似）+Vuepress基础配置学习+绑定二级域名（可以看到这个域名是record.giriawsh.com也是我主站域名的二级域名）</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005200946.png" alt="image-20201005200945984" loading="lazy"></figure>
<p>在我的blog中点击个人学习系列，即可跳转至用vuepress搭建的学习记录站点。</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005201041.png" alt="image-20201005201041265" loading="lazy"></figure>
<h2 id="创建githubpages部署">创建GithubPages+部署</h2>
<p><strong>我们只能为每个 GitHub 帐户创建一个用户或组织站点；而项目站点没有限制</strong>。</p>
<p>只能拥有一个的，是个人站点，也就是名为 <code>&lt;user&gt;.github.io</code> 的仓库。**想要创建更多的GitHub Pages页面，可以通过创建项目站点实现。**项目站点的数量没有限制。</p>
<p>详情可见**<a href="https://link.zhihu.com/?target=https%3A//docs.github.com/cn/github/working-with-github-pages/about-github-pages%23">GitHub Pages 站点的类型</a>**——官方文档</p>
<h3 id="1-创建项目">1. 创建项目</h3>
<ol>
<li>新建一个仓库，名称随意。</li>
<li>进入仓库主页，点击右面的<code>Settings</code>，找到<strong>GitHub Pages</strong>部分，选择一个主题，之后可以再改，不选择的话页面可能会无法加载</li>
<li>建议勾选 <code>Enforce HTTPS</code>，否则访问时会出现安全警告。</li>
<li>没有出错的话，一个项目主页就建立完成了，可以通过<code>&lt;username&gt;.github.io/&lt;projectname&gt;</code>访问到了</li>
</ol>
<h3 id="2-创建vuepress项目">2. 创建Vuepress项目</h3>
<p>参照<a href="https://vuepress.vuejs.org/zh/guide/getting-started.html">官方文档</a></p>
<ol>
<li>
<p>创建并进入一个新目录</p>
<pre><code class="language-bash">mkdir vuepress-starter &amp;&amp; cd vuepress-starter
</code></pre>
</li>
<li>
<p>使用你喜欢的包管理器进行初始化（听我的，用yarn，如果你不想出现奇怪的bug的话）</p>
<pre><code class="language-bash">yarn init # npm init
</code></pre>
</li>
<li>
<p>将 VuePress 安装为本地依赖</p>
<p>我们已经不再推荐全局安装 VuePress</p>
<pre><code class="language-bash">yarn add -D vuepress # npm install -D vuepress
</code></pre>
</li>
<li>
<p>创建你的第一篇文档</p>
<pre><code class="language-bash">mkdir docs &amp;&amp; echo '# Hello VuePress' &gt; docs/README.md
</code></pre>
</li>
<li>
<p>在 <code>package.json</code> 中添加一些 <a href="https://classic.yarnpkg.com/zh-Hans/docs/package-json#toc-scripts">scripts</a></p>
<p>这一步骤是可选的，但我们推荐你完成它。在下文中，我们会默认这些 scripts 已经被添加。</p>
<pre><code class="language-json">{
  &quot;scripts&quot;: {
    &quot;docs:dev&quot;: &quot;vuepress dev docs&quot;,
    &quot;docs:build&quot;: &quot;vuepress build docs&quot;
  }
}
</code></pre>
</li>
<li>
<p>在本地启动服务器</p>
<pre><code class="language-bash">yarn docs:dev # npm run docs:dev
</code></pre>
<p>VuePress 会在 <a href="http://localhost:8080/">http://localhost:8080</a> 启动一个热重载的开发服务器。</p>
</li>
</ol>
<h2 id="vuepress配置">Vuepress配置</h2>
<h3 id="首页">首页</h3>
<p>这块可参考<a href="https://vuepress.vuejs.org/zh/theme/default-theme-config.html#%E9%A6%96%E9%A1%B5">官方文档主题模块</a>。</p>
<p>在/docs文件夹下创建README.md文件，这是我的个人配置（把logo.jpg放在/docs/.vuepress/public 文件夹中）：</p>
<pre><code class="language-md">---
home: true
heroImage: /logo.jpg
heroText: Giria's learning record
tagline: 个人前端学习记录
actionText: 一起梳理 →
actionLink: /source/element/
sidebar: false
features:
- title: 源码学习
  details: Element UI + Vue 源码
- title: 书籍学习
  details: 记录相关书籍的学习路径
- title: 前端相关
  details: 各代码库的学习，包括不限于NodeJS、NuxtJS、多种视图组件库等
footer: MIT Licensed | Copyright © 2020-present Giria
---
::: tip 更多
博客文章可移步[Giria's Blog](https://www.giriawsh.com)
:::
</code></pre>
<h3 id="导航栏">导航栏</h3>
<h4 id="基本配置">基本配置</h4>
<p>修改/docs/config.js文件</p>
<pre><code class="language-js">module.exports = {
    themeConfig: {
        sidebarDepth: 2, // e'b将同时提取markdown中h2 和 h3 标题，显示在侧边栏上。
        lastUpdated: 'Last Updated',// 文档更新时间：每个文件git最后提交的时间
        searchMaxSuggestions: 10,
        nav: [
            {text: '主页', link: '/'},
            {
                text: '源码学习',
                items: [
                    {text: &quot;Element UI&quot;, link: '/source/element/'},
                    {text: &quot;Vue&quot;, link: '/source/vue/'}
                ],
            },
            {
                text: '书籍学习',
                items: [
                    {text: &quot;JavaScript权威指南&quot;, link: '/books/theDefinitiveGuideToJavaScript/'}
                ],
            },
            {
                text: '前端相关',
                items: [
                    {text: &quot;Node.js&quot;, link: '/fe/nodejs/'},
                    {text: &quot;Vue.js&quot;, link: '/fe/vuejs/'},
                    {text: &quot;SVG.js&quot;, link: '/fe/svgjs/'},
                    {text: &quot;Nuxt框架&quot;, link: '/fe/nuxtjs/'},
                ],
            },
            {text: '博客文章', link: 'https://www.giriawsh.com'},//外链，可进行跳转
        ],
	}
}
</code></pre>
<p>目录组织结构如下：</p>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005202734.png" alt="image-20201005202734817" loading="lazy"></figure>
<p>产生效果如下：</p>
<figure data-type="image" tabindex="4"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005202800.png" alt="image-20201005202759976" loading="lazy"></figure>
<h4 id="导航栏分组配置">导航栏分组配置</h4>
<p>如果想要进行导航栏分组可这样写：</p>
<pre><code class="language-js">// .vuepress/config.js
module.exports = {
  themeConfig: {
    nav: [
      {
                text: '前端相关',
                items: [
                    {
                        text: &quot;JS库相关&quot;, items: [
                            {text: &quot;Node.js&quot;, link: '/fe/nodejs/'},
                            {text: &quot;Vue.js&quot;, link: '/fe/vuejs/'},
                            {text: &quot;SVG.js&quot;, link: '/fe/svgjs/'},
                        ]
                    },
                    {
                        text: &quot;框架相关&quot;, items: [
                            {text: &quot;Nuxt框架&quot;, link: '/fe/nuxtjs/'},
                        ]
                    },
                ],
            },
    ]
  }
}
</code></pre>
<p>效果如下：</p>
<figure data-type="image" tabindex="5"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005203415.png" alt="image-20201005203415682" loading="lazy"></figure>
<h4 id="导航栏logo">导航栏LOGO</h4>
<p>可以通过 <code>themeConfig.logo</code> 增加导航栏 Logo ，Logo 可以被放置在<a href="https://vuepress.vuejs.org/zh/guide/assets.html#public-files">公共文件目录</a>：</p>
<pre><code class="language-js">// .vuepress/config.js
module.exports = {
  themeConfig: {
    logo: '/assets/img/logo.png',
  }
}
</code></pre>
<h3 id="侧边栏">侧边栏</h3>
<p>侧边栏这里我查了很久，很少有清楚的demo，我这里做个demo记录。</p>
<p>目录结构如下：</p>
<figure data-type="image" tabindex="6"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005203923.png" alt="image-20201005203923592" loading="lazy"></figure>
<p>在/docs/.vuepress/config.js中进行如下配置：</p>
<pre><code class="language-js">module.exports = {
    themeConfig: {
        sidebarDepth: 2, // e'b将同时提取markdown中h2 和 h3 标题，显示在侧边栏上。
        lastUpdated: 'Last Updated',// 文档更新时间：每个文件git最后提交的时间
        sidebar: {
            '/source/element/': [
                {
                    title: 'Element源码分析',
                    collapsable: false,
                    children: [
                        '/source/element/1',
                        '/source/element/2',]//这里的顺序决定了在侧边栏中从上到下显示的顺序
                }
            ],
            '/source/vue/': [
                {
                    title: 'Vue源码分析',
                    collapsable: false,
                    children: []
                }
            ],
            '/books/theDefinitiveGuideToJavaScript/': [
                {
                    title: 'JavaScript权威指南',
                    collapsable: false,
                    children: [
                        '/books/theDefinitiveGuideToJavaScript/1',
                        '/books/theDefinitiveGuideToJavaScript/2',
                        '/books/theDefinitiveGuideToJavaScript/3',
                        '/books/theDefinitiveGuideToJavaScript/4',
                        '/books/theDefinitiveGuideToJavaScript/5',
                    ],
                }
            ],
            '/fe/nodejs/': [
                {
                    title: 'NodeJS初学',
                    collapsable: false,
                    children: [
                        '/fe/nodejs/start/1',
                        '/fe/nodejs/start/2',
                        '/fe/nodejs/start/3',
                        '/fe/nodejs/start/4',
                    ]
                },
                {
                    title: 'NodeJS深入学习',
                    collapsable: false,
                    children: [
                        '/fe/nodejs/second/test'
                    ]
                }
            ],
            '/fe/svgjs/': [
                {
                    title: 'SVG学习记录',
                    collapsable: false,
                    children: [
                        '/fe/svgjs/1',
                        '/fe/svgjs/2',
                        '/fe/svgjs/3',
                    ]
                },
            ]
        }
    },

};
</code></pre>
<p>如果想要达到如下多个分组的效果，可查看上面代码中的<code>'/fe/nodejs/'</code>部分，进行多个对象的配置。（上面代码为了直观省略了文件名）</p>
<figure data-type="image" tabindex="7"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005204000.png" alt="image-20201005204000559" loading="lazy"></figure>
<h4 id="踩坑">踩坑</h4>
<ol>
<li>记得给每个新创建的文件夹下增加一个README.md随便写点啥，不然有一定概率会跳转到404</li>
<li>文件名譬如1.md在路径中可以省略.md（.html也是可以的）</li>
</ol>
<h3 id="网页logo">网页LOGO</h3>
<p>将logo文件放置到/docs/.vuepress/public文件夹下，然后在config.js文件中增加：</p>
<pre><code class="language-js">module.exports = {
    // 注入到当前页面的 HTML &lt;head&gt; 中的标签
    head: [
        ['link', {rel: 'icon', href: '/pencil.png'}], // 增加一个自定义的 favicon(网页标签的图标)
    ],
}
</code></pre>
<h3 id="样式修改">样式修改</h3>
<p>在/docs/.vuepress文件夹中新建styles文件夹，创建palette.styl文件。</p>
<pre><code class="language-js">$accentColor = #e6be00//默认主题颜色
$textColor = red//默认字体颜色
$borderColor = #eaecef//默认边框颜色
$codeBgColor = #282c34//默认背景颜色

示例修改相关样式f12找到需要修改的地方找到对应class类拿过来直接用就行了
.sidebar-group.is-sub-group &gt; .sidebar-heading:not(.clickable){
  opacity :1
}
</code></pre>
<p>更多可参考：</p>
<ul>
<li><a href="https://juejin.im/post/6844903864227856392">vuepress主题改造-主题的继承(1)</a></li>
<li><a href="https://artiely.github.io/blog/original/create-custom-VuePress-theme.html#%E8%AE%BE%E7%BD%AE-vuepress-%E9%A1%B9%E7%9B%AE">如何创建自定义 VuePress 主题</a></li>
<li><a href="https://www.xerrors.fun/decorate-vuepress/">Vuepress 主题改造指南</a>   --》  追加了黑色模式，而且博客主题风格我很喜欢</li>
<li><a href="https://vuepress-theme-reco.recoluan.com/">一款简洁而优雅的 vuepress 博客 &amp; 文档 主题</a></li>
</ul>
<h2 id="部署">部署</h2>
<ol>
<li>
<p>在 <code>docs/.vuepress/config.js</code> 中设置正确的 <code>base</code>。</p>
<p>如果你打算发布到 <code>https://&lt;USERNAME&gt;.github.io/</code>，则可以省略这一步，因为 <code>base</code> 默认即是 <code>&quot;/&quot;</code>。</p>
<p>如果你打算发布到 <code>https://&lt;USERNAME&gt;.github.io/&lt;REPO&gt;/</code>（也就是说你的仓库在 <code>https://github.com/&lt;USERNAME&gt;/&lt;REPO&gt;</code>），则将 <code>base</code> 设置为 <code>&quot;/&lt;REPO&gt;/&quot;</code>。</p>
<pre><code class="language-js">module.exports = {
    title: &quot;Girecord&quot;,
    description: 'Giria的学习记录',
    base: '/&lt;REPO&gt;/',//如果要绑定子域名的话这里需要改成/
 }
</code></pre>
</li>
<li>
<p>在你的项目中，创建一个如下的 <code>deploy.sh</code> 文件（请自行判断去掉高亮行的注释）放置到根目录中:</p>
</li>
</ol>
<pre><code class="language-bash">#!/usr/bin/env sh

# 确保脚本抛出遇到的错误
set -e

# 生成静态文件
npm run docs:build

# 进入生成的文件夹
cd docs/.vuepress/dist

# 如果是发布到自定义域名
# echo 'www.example.com' &gt; CNAME
# 后续这里我们会发布到二级域名，像我的就需要像下面这么写：
echo 'record.giriawsh.com' &gt; CNAME

git init
git add -A
git commit -m 'deploy'

# 如果发布到 https://&lt;USERNAME&gt;.github.io
# git push -f git@github.com:&lt;USERNAME&gt;/&lt;USERNAME&gt;.github.io.git master

# 如果发布到 https://&lt;USERNAME&gt;.github.io/&lt;REPO&gt;
# git push -f git@github.com:&lt;USERNAME&gt;/&lt;REPO&gt;.git master:gh-pages

cd -
</code></pre>
<p>这样我们之后每次修改后只需要点击这个脚本进行运行即可部署。</p>
<p>至此我们已经可以通过访问<code>https://&lt;USERNAME&gt;.github.io/&lt;REPO&gt;</code>访问到部署后的页面了</p>
<h2 id="绑定子域名">绑定子域名</h2>
<p>我是在Godaddy上购买的网址，来到DNS配置，添加一条CNAME记录</p>
<figure data-type="image" tabindex="8"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201005213213.png" alt="image-20201005213213303" loading="lazy"></figure>
<p>::: tip</p>
<p>如果是参照了我之前的配置方案，这里不用写成record.giriawsh.com，因为它会自己映射添加，写二级子域名名称record即可</p>
<p>:::</p>
<h2 id="参考网站">参考网站</h2>
<p><a href="https://zhuanlan.zhihu.com/p/183977963">如何拥有多个GitHub Pages</a></p>
<p><a href="https://blog.csdn.net/zhang__ao/article/details/96144441">vuepress 使用与主题自定义样式修改</a></p>
<p><a href="https://www.dazhuanlan.com/2019/10/18/5da96daa80de8/">配置GithubPage二级域名</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NodeJS初学系列4——util工具、文件系统、GET/POST请求]]></title>
        <id>https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-4-util-gong-ju-wen-jian-xi-tong-getpost-qing-qiu/</id>
        <link href="https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-4-util-gong-ju-wen-jian-xi-tong-getpost-qing-qiu/">
        </link>
        <updated>2020-10-03T10:55:45.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>Giria度过了海吃海喝的国庆两天，重拾NodeJS，这是第四天，学习了util模块、文件系统操作、GET/POST请求等。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>Giria度过了海吃海喝的国庆两天，重拾NodeJS，这是第四天，学习了util模块、文件系统操作、GET/POST请求等。</p>
<!-- more -->
<h1 id="常用工具">常用工具</h1>
<p>util 是一个Node.js 核心模块，提供常用函数的集合，用于弥补核心 JavaScript 的功能 过于精简的不足。</p>
<pre><code>const util = require('util');
</code></pre>
<h2 id="utilcallbackify">util.callbackify</h2>
<p><strong>util.callbackify(original)</strong> 将 <code>async</code> 异步函数（或者一个返回值为 <code>Promise</code> 的函数）转换成遵循<strong>异常优先的回调风格的函数</strong>，例如将 <code>(err, value) =&gt; ...</code> 回调作为最后一个参数。 在回调函数中，第一个参数为拒绝的原因（如果 <code>Promise</code> 解决，则为 <code>null</code>），第二个参数则是解决的值。</p>
<p>original 为 async 异步函数。该函数返回传统回调函数。</p>
<pre><code class="language-js">const util = require('util');

async function fn() {
  return 'hello world';
}
const callbackFunction = util.callbackify(fn);

callbackFunction((err, ret) =&gt; {
  if (err) throw err;
  console.log(ret);
});
</code></pre>
<p>执行结果：</p>
<pre><code>hello world
</code></pre>
<p>回调函数是异步执行的，并且有异常堆栈错误追踪。 如果回调函数抛出一个异常，进程会触发一个 'uncaughtException' 异常，如果没有被捕获，进程将会退出。</p>
<p>null 在回调函数中作为一个参数有其特殊的意义，如果回调函数的首个参数为 Promise 拒绝的原因且带有返回值，且值可以转换成布尔值 false，这个值会被封装在 Error 对象里，可以通过属性 reason 获取。</p>
<pre><code class="language-js">function fn() {
  return Promise.reject(null);
}
const callbackFunction = util.callbackify(fn);

callbackFunction((err, ret) =&gt; {
  // 当 Promise 被以 `null` 拒绝时，它被包装为 Error 并且原始值存储在 `reason` 中。
  err &amp;&amp; err.hasOwnProperty('reason') &amp;&amp; err.reason === null;  // true
});
</code></pre>
<h2 id="utilinherits">util.inherits</h2>
<p><strong>util.inherits(constructor, superConstructor)</strong> 是一个实现对象间原型继承的函数。</p>
<pre><code class="language-js">var util = require('util');
function Base() {
    this.name = 'giria';
    this.base = 1999;
    this.sayHello = function() {
        console.log('Hello ' + this.name);
    };
}
Base.prototype.showName = function() {
    console.log('My name is '+this.name);
};
function Sub() {
    this.name = 'sub_giria';
}
util.inherits(Sub, Base);
var objBase = new Base();
objBase.showName();
objBase.sayHello();
console.log(objBase);
var objSub = new Sub();
objSub.showName();
objSub.sayHello();
console.log(objSub);
</code></pre>
<p>我们定义了一个基础对象 Base 和一个继承自 Base 的 Sub，Base 有三个在构造函数内定义的属性和一个原型中定义的函数，通过util.inherits 实现继承。</p>
<p>执行结果：</p>
<pre><code>My name is giria
Hello giria
Base { name: 'giria', base: 1999, sayHello: [Function] }
My name is sub_giria
K:\projects\node\first-test\util-demo\main.js:22
objSub.sayHello();
  	   ^

TypeError: objSub.sayHello is not a function
Sub { name: 'sub_giria' }
</code></pre>
<blockquote>
<p>**注意：**Sub <strong>仅仅继承了Base 在原型中定义的函数</strong>，而构造函数内部创造的 base 属 性和 sayHello 函数都没有被 Sub 继承。</p>
</blockquote>
<h2 id="utilinspect">util.inspect</h2>
<p><strong>util.inspect(object,[showHidden],[depth],[colors])</strong> 是一个将任意对象转换 为字符串的方法，通常用于调试和错误输出。它至少接受一个参数 object，即要转换的对象。</p>
<ul>
<li>
<p>showHidden 是一个可选参数，如果值为 true，将会输出更多隐藏信息。</p>
</li>
<li>
<p>depth 表示最大递归的层数，如果对象很复杂，你可以指定层数以控制输出信息的多 少。如果不指定depth，默认会递归 2 层，指定为 null 表示将不限递归层数完整遍历对象。</p>
</li>
<li>
<p>如果 colors 值为 true，输出格式将会以 ANSI 颜色编码，通常用于在终端显示更漂亮 的效果。</p>
</li>
</ul>
<p>特别要指出的是，util.inspect 并不会简单地直接把对象转换为字符串，即使该对 象定义了 toString 方法也不会调用。（意思是本质上并不是调用toString方法，而且转换的字符串和我们想要的toString形式不一样）</p>
<pre><code class="language-js">var util = require('util');
function Person() {
    this.name = 'giria';
    this.toString = function() {
        return this.name;
    };
}
var obj = new Person();
console.log(util.inspect(obj));
console.log(util.inspect(obj, true));
console.log(util.inspect(obj, true, null));
console.log(util.inspect(obj, true, null, true));
</code></pre>
<p>执行结果：</p>
<pre><code class="language-js">Person { name: 'giria', toString: [Function] }
Person {
  name: 'giria',
  toString: [Function] {
    [length]: 0,
    [name]: '',
    [arguments]: null,
    [caller]: null,
    [prototype]: { [constructor]: [Circular] }
  }
}
Person {
  name: 'giria',
  toString: [Function] {
    [length]: 0,
    [name]: '',
    [arguments]: null,
    [caller]: null,
    [prototype]: { [constructor]: [Circular] }
  }
}
Person {
  name: 'giria',
  toString: [Function] {
    [length]: 0,
    [name]: '',
    [arguments]: null,
    [caller]: null,
    [prototype]: { [constructor]: [Circular] }
  }
}
</code></pre>
<h2 id="utilisarrayobject">util.isArray(object)</h2>
<p>如果给定的参数 &quot;object&quot; 是一个数组返回 true，否则返回 false。</p>
<pre><code class="language-js">var util = require('util');

util.isArray([])
  // true
util.isArray(new Array)
  // true
util.isArray({})
  // false
</code></pre>
<h2 id="utilisregexpobject">util.isRegExp(object)</h2>
<p>如果给定的参数 &quot;object&quot; 是一个正则表达式返回true，否则返回false。</p>
<pre><code class="language-js">var util = require('util');

util.isRegExp(/some regexp/)
  // true
util.isRegExp(new RegExp('another regexp'))
  // true
util.isRegExp({})
  // false
</code></pre>
<h2 id="utilisdateobject">util.isDate(object)</h2>
<p>如果给定的参数 &quot;object&quot; 是一个日期返回true，否则返回false。</p>
<pre><code class="language-js">var util = require('util');

util.isDate(new Date())
  // true
util.isDate(Date())
  // false (without 'new' returns a String) 有点意思
util.isDate({})
  // false
</code></pre>
<h1 id="文件系统">文件系统</h1>
<p>Node.js 提供一组类似 UNIX（POSIX）标准的文件操作API。 Node 导入文件系统模块(fs)语法如下所示(我们前面在文件读写已经用到过了)：</p>
<pre><code>var fs = require(&quot;fs&quot;)
</code></pre>
<h2 id="异步和同步">异步和同步</h2>
<p>Node.js 文件系统（fs 模块）模块中的方法均有异步和同步版本，例如读取文件内容的函数有异步的 fs.readFile() 和同步的 fs.readFileSync()。</p>
<p>异步的方法函数最后一个参数为回调函数，回调函数的第一个参数包含了错误信息(error)。</p>
<p><strong>建议大家使用异步方法，比起同步，异步方法性能更高，速度更快，而且没有阻塞。</strong></p>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

// 异步读取
fs.readFile('input.txt', function (err, data) {
   if (err) {
       return console.error(err);
   }
   console.log(&quot;异步读取: &quot; + data.toString());
});

// 同步读取
var data = fs.readFileSync('input.txt');
console.log(&quot;同步读取: &quot; + data.toString());

console.log(&quot;程序执行完毕。&quot;);
</code></pre>
<p>执行结果</p>
<pre><code>$ node file.js 
同步读取: 菜鸟教程官网地址：www.runoob.com
文件读取实例

程序执行完毕。
异步读取: 菜鸟教程官网地址：www.runoob.com
文件读取实例
</code></pre>
<h2 id="打开文件">打开文件</h2>
<p>以下为在异步模式下打开文件的语法格式：</p>
<pre><code>fs.open(path, flags[, mode], callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件的路径。</li>
<li><strong>flags</strong> - 文件打开的行为。具体值详见下文。</li>
<li><strong>mode</strong> - 设置文件模式(权限)，文件创建默认权限为 0666(可读，可写)。</li>
<li><strong>callback</strong> - 回调函数，带有两个参数如：callback(err, fd)。</li>
</ul>
<p>flags 参数可以是以下值：</p>
<table>
<thead>
<tr>
<th style="text-align:left">Flag</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">r</td>
<td style="text-align:left">以读取模式打开文件。如果文件不存在抛出异常。</td>
</tr>
<tr>
<td style="text-align:left">r+</td>
<td style="text-align:left">以读写模式打开文件。如果文件不存在抛出异常。</td>
</tr>
<tr>
<td style="text-align:left">rs</td>
<td style="text-align:left">以同步的方式读取文件。</td>
</tr>
<tr>
<td style="text-align:left">rs+</td>
<td style="text-align:left">以同步的方式读取和写入文件。</td>
</tr>
<tr>
<td style="text-align:left">w</td>
<td style="text-align:left">以写入模式打开文件，如果文件不存在则创建。</td>
</tr>
<tr>
<td style="text-align:left">wx</td>
<td style="text-align:left">类似 'w'，但是如果文件路径存在，则文件写入失败。</td>
</tr>
<tr>
<td style="text-align:left">w+</td>
<td style="text-align:left">以读写模式打开文件，如果文件不存在则创建。</td>
</tr>
<tr>
<td style="text-align:left">wx+</td>
<td style="text-align:left">类似 'w+'， 但是如果文件路径存在，则文件读写失败。</td>
</tr>
<tr>
<td style="text-align:left">a</td>
<td style="text-align:left">以追加模式打开文件，如果文件不存在则创建。</td>
</tr>
<tr>
<td style="text-align:left">ax</td>
<td style="text-align:left">类似 'a'， 但是如果文件路径存在，则文件追加失败。</td>
</tr>
<tr>
<td style="text-align:left">a+</td>
<td style="text-align:left">以读取追加模式打开文件，如果文件不存在则创建。</td>
</tr>
<tr>
<td style="text-align:left">ax+</td>
<td style="text-align:left">类似 'a+'， 但是如果文件路径存在，则文件读取追加失败。</td>
</tr>
</tbody>
</table>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

// 异步打开文件
console.log(&quot;准备打开文件！&quot;);
fs.open('input.txt', 'r+', function(err, fd) {
    if (err) {
        return console.error(err);
    }
    console.log(&quot;文件打开成功！&quot;);
});
console.log(&quot;打开文件完成！&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>准备打开文件！
打开文件完成！
文件打开成功！
</code></pre>
<h2 id="获取文件信息">获取文件信息</h2>
<p>以下为通过异步模式获取文件信息的语法格式：</p>
<pre><code>fs.stat(path, callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件路径。</li>
<li><strong>callback</strong> - 回调函数，带有两个参数如：(err, stats), <strong>stats</strong> 是 fs.Stats 对象。</li>
</ul>
<p>fs.stat(path)执行后，会将stats类的实例返回给其回调函数。可以通过stats类中的提供方法判断文件的相关属性。</p>
<pre><code class="language-js">var fs = require('fs');

fs.stat('/Users/liuht/code/itbilu/demo/fs.js', function (err, stats) {
    console.log(stats.isFile());         //true
})
</code></pre>
<p>stats类中的方法有：</p>
<table>
<thead>
<tr>
<th style="text-align:left">方法</th>
<th style="text-align:left">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">stats.isFile()</td>
<td style="text-align:left">如果是文件返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">stats.isDirectory()</td>
<td style="text-align:left">如果是目录返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">stats.isBlockDevice()</td>
<td style="text-align:left">如果是块设备返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">stats.isCharacterDevice()</td>
<td style="text-align:left">如果是字符设备返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">stats.isSymbolicLink()</td>
<td style="text-align:left">如果是软链接返回 true，否则返回 false。</td>
</tr>
<tr>
<td style="text-align:left">stats.isFIFO()</td>
<td style="text-align:left">如果是FIFO，返回true，否则返回 false。FIFO是UNIX中的一种特殊类型的命令管道。</td>
</tr>
<tr>
<td style="text-align:left">stats.isSocket()</td>
<td style="text-align:left">如果是 Socket 返回 true，否则返回 false。</td>
</tr>
</tbody>
</table>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

console.log(&quot;准备打开文件！&quot;);
fs.stat('input.txt', function (err, stats) {
   if (err) {
       return console.error(err);
   }
   console.log(stats);
   console.log(&quot;读取文件信息成功！&quot;);
   
   // 检测文件类型
   console.log(&quot;是否为文件(isFile) ? &quot; + stats.isFile());
   console.log(&quot;是否为目录(isDirectory) ? &quot; + stats.isDirectory());    
});
</code></pre>
<pre><code>准备打开文件！
Stats {
  dev: 611366247,
  mode: 33206,
  nlink: 1,
  uid: 0,
  gid: 0,
  rdev: 0,
  blksize: 4096,
  ino: 1407374884218279,
  size: 43,
  blocks: 0,
  atimeMs: 1601623852273.1826,
  mtimeMs: 1601278302892,
  ctimeMs: 1601623852273.1826,
  birthtimeMs: 1601623852272.183,
  atime: 2020-10-02T07:30:52.273Z,
  mtime: 2020-09-28T07:31:42.892Z,
  ctime: 2020-10-02T07:30:52.273Z,
  birthtime: 2020-10-02T07:30:52.272Z
}
读取文件信息成功！
是否为文件(isFile) ? true
是否为目录(isDirectory) ? false
</code></pre>
<h2 id="写入文件">写入文件</h2>
<p>以下为异步模式下写入文件的语法格式：</p>
<pre><code>fs.writeFile(file, data[, options], callback)
</code></pre>
<ul>
<li><strong>file</strong> - 文件名或文件描述符。</li>
<li><strong>data</strong> - 要写入文件的数据，可以是 String(字符串) 或 Buffer(缓冲) 对象。</li>
<li><strong>options</strong> - 该参数是一个对象，包含 {encoding, mode, flag}。默认编码为 utf8, 模式为 0666 ， flag 为 'w'</li>
<li><strong>callback</strong> - 回调函数，回调函数只包含错误信息参数(err)，在写入失败时返回。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

console.log(&quot;准备写入文件&quot;);
fs.writeFile('input.txt', '我是通 过fs.writeFile 写入文件的内容',  function(err) {
   if (err) {
       return console.error(err);
   }
   console.log(&quot;数据写入成功！&quot;);
   console.log(&quot;--------我是分割线-------------&quot;)
   console.log(&quot;读取写入的数据！&quot;);
   fs.readFile('input.txt', function (err, data) {
      if (err) {
         return console.error(err);
      }
      console.log(&quot;异步读取文件数据: &quot; + data.toString());
   });
});
</code></pre>
<pre><code>准备写入文件
数据写入成功！
--------我是分割线-------------
读取写入的数据！
异步读取文件数据: 我是通 过fs.writeFile 写入文件的内容
</code></pre>
<h2 id="读取文件">读取文件</h2>
<p>以下为异步模式下读取文件的语法格式：</p>
<pre><code class="language-js">fs.read(fd, buffer, offset, length, position, callback)
</code></pre>
<ul>
<li><strong>fd</strong> - 通过 fs.open() 方法返回的文件描述符。</li>
<li><strong>buffer</strong> - 数据写入的缓冲区。</li>
<li><strong>offset</strong> - 缓冲区写入的写入偏移量。</li>
<li><strong>length</strong> - 要从文件中读取的字节数。</li>
<li><strong>position</strong> - 文件读取的起始位置，如果 position 的值为 null，则会从当前文件指针的位置读取。</li>
<li><strong>callback</strong> - 回调函数，有三个参数err, bytesRead, buffer，err 为错误信息， bytesRead 表示读取的字节数，buffer 为缓冲区对象。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var buf = new Buffer.alloc(1024);

console.log(&quot;准备打开已存在的文件！&quot;);
fs.open('input.txt', 'r+', function(err, fd) {
   if (err) {
       return console.error(err);
   }
   console.log(&quot;文件打开成功！&quot;);
   console.log(&quot;准备读取文件：&quot;);
   fs.read(fd, buf, 0, buf.length, 0, function(err, bytes){
      if (err){
         console.log(err);
      }
      console.log(bytes + &quot;  字节被读取&quot;);
      
      // 仅输出读取的字节
      if(bytes &gt; 0){
         console.log(buf.slice(0, bytes).toString());
      }
   });
});
</code></pre>
<p>执行结果：</p>
<pre><code>准备打开已存在的文件！
文件打开成功！
准备读取文件：
42  字节被读取
菜鸟教程官网地址：www.runoob.com
</code></pre>
<h2 id="关闭文件">关闭文件</h2>
<p>以下为异步模式下关闭文件的语法格式：</p>
<pre><code>fs.close(fd, callback)
</code></pre>
<ul>
<li><strong>fd</strong> - 通过 fs.open() 方法返回的文件描述符。</li>
<li><strong>callback</strong> - 回调函数，没有参数。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var buf = new Buffer.alloc(1024);

console.log(&quot;准备打开文件！&quot;);
fs.open('input.txt', 'r+', function(err, fd) {
   if (err) {
       return console.error(err);
   }
   console.log(&quot;文件打开成功！&quot;);
   console.log(&quot;准备读取文件！&quot;);
   fs.read(fd, buf, 0, buf.length, 0, function(err, bytes){
      if (err){
         console.log(err);
      }

      // 仅输出读取的字节
      if(bytes &gt; 0){
         console.log(buf.slice(0, bytes).toString());
      }

      // 关闭文件
      fs.close(fd, function(err){
         if (err){
            console.log(err);
         } 
         console.log(&quot;文件关闭成功&quot;);
      });
   });
});
</code></pre>
<p>执行结果：</p>
<pre><code>准备打开文件！
文件打开成功！
准备读取文件！
菜鸟教程官网地址：www.runoob.com
文件关闭成功
</code></pre>
<h2 id="截取文件">截取文件</h2>
<p>以下为异步模式下截取文件的语法格式：</p>
<pre><code>fs.ftruncate(fd, len, callback)
</code></pre>
<ul>
<li><strong>fd</strong> - 通过 fs.open() 方法返回的文件描述符。</li>
<li><strong>len</strong> - 文件内容截取的长度。</li>
<li><strong>callback</strong> - 回调函数，没有参数。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var buf = new Buffer.alloc(1024);

console.log(&quot;ready to open the file&quot;);
fs.open('input.txt', 'r+', function (err, fd) {
    if (err) {
        return console.error(err);
    }
    console.log(&quot;open file successfully&quot;);
    fs.read(fd, buf, 0, buf.length, 0, function (err, bytes) {
        if (err){
            console.log(err);
        }

        // 仅输出读取的字节
        if(bytes &gt; 0){
            console.log(buf.slice(0, bytes).toString()+&quot;before&quot;);
        }
        console.log(&quot;---------------------&quot;);
    });
    console.log(&quot;ready to ftruncate&quot;);
    fs.ftruncate(fd, 10, function (err) {
        if (err) {
            console.error(err);
        }
        fs.read(fd, buf, 0, buf.length, 0, function (err, bytes) {
            if (err){
                console.log(err);
            }

            // 仅输出读取的字节
            if(bytes &gt; 0){
                console.log(buf.slice(0, bytes).toString());
            }

            console.log(&quot;---------------------&quot;);
        });
    });

    fs.read(fd, buf, 0, buf.length, 0, function (err, bytes) {
        if (err){
            console.log(err);
        }

        // 仅输出读取的字节
        if(bytes &gt; 0){
            console.log(buf.slice(0, bytes).toString()+&quot;after&quot;);
        }

        // 关闭文件
        fs.close(fd, function(err){
            if (err){
                console.log(err);
            }
            console.log(&quot;文件关闭成功！&quot;);
        });
    });
});
</code></pre>
<p>执行结果：</p>
<pre><code>➜  file-demo node ftruncate.js
ready to open the file
open file successfully
ready to ftruncate
abcdefg:wwbefore
---------------------
abcdefg:wwafter
abcdefg:ww
---------------------
文件关闭成功！
</code></pre>
<p>奇怪的执行顺序，无论什么时候读，返回的都是截取后的结果。可能和异步执行有关。</p>
<h2 id="删除文件">删除文件</h2>
<p>以下为删除文件的语法格式：</p>
<pre><code>fs.unlink(path, callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件路径。</li>
<li><strong>callback</strong> - 回调函数，没有参数。</li>
</ul>
<pre><code>var fs = require(&quot;fs&quot;);

console.log(&quot;准备删除文件！&quot;);
fs.unlink('input.txt', function(err) {
   if (err) {
       return console.error(err);
   }
   console.log(&quot;文件删除成功！&quot;);
});
</code></pre>
<p>执行结果：</p>
<pre><code>准备删除文件！
文件删除成功！
</code></pre>
<h2 id="创建目录">创建目录</h2>
<p>以下为创建目录的语法格式：</p>
<pre><code>fs.mkdir(path[, options], callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件路径。</li>
<li>options 参数可以是：
<ul>
<li><strong>recursive</strong> - 是否以递归的方式创建目录，默认为 false。</li>
<li><strong>mode</strong> - 设置目录权限，默认为 0777。</li>
</ul>
</li>
<li><strong>callback</strong> - 回调函数，没有参数。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
// tmp 目录必须存在
console.log(&quot;创建目录 /tmp/test/&quot;);
fs.mkdir(&quot;/tmp/test/&quot;,function(err){
   if (err) {
       return console.error(err);
   }
   console.log(&quot;目录创建成功。&quot;);
});
</code></pre>
<p>可以添加 recursive: true 参数，不管创建的目录 /tmp 和 /tmp/a 是否存在：</p>
<pre><code class="language-js">fs.mkdir('/tmp/a/apple', { recursive: true }, (err) =&gt; {
  if (err) throw err;
});
</code></pre>
<h2 id="读取目录">读取目录</h2>
<p>以下为读取目录的语法格式：</p>
<pre><code>fs.readdir(path, callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件路径。</li>
<li><strong>callback</strong> - 回调函数，回调函数带有两个参数err, files，err 为错误信息，files 为 目录下的文件数组列表。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
console.log(&quot;查看当前目录&quot;);
fs.readdir(&quot;../file-demo/&quot;, function (err, files) {
    if (err) {
        return console.error(err);
    }
    files.forEach(function (file) {
        console.log(file);
    });
});
</code></pre>
<p>执行结果：</p>
<pre><code>➜  file-demo node readdir.js
查看当前目录
ftruncate.js
input.txt
main.js
read.js
readdir.js
stat.js
</code></pre>
<h2 id="删除目录">删除目录</h2>
<p>以下为删除目录的语法格式：</p>
<pre><code>fs.rmdir(path, callback)
</code></pre>
<ul>
<li><strong>path</strong> - 文件路径。</li>
<li><strong>callback</strong> - 回调函数，没有参数。</li>
</ul>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
// 执行前创建一个空的 /tmp/test 目录
console.log(&quot;准备删除目录 /tmp/test&quot;);
fs.rmdir(&quot;/tmp/test&quot;,function(err){
   if (err) {
       return console.error(err);
   }
   console.log(&quot;读取 /tmp 目录&quot;);
   fs.readdir(&quot;/tmp/&quot;,function(err, files){
      if (err) {
          return console.error(err);
      }
      files.forEach( function (file){
          console.log( file );
      });
   });
});
</code></pre>
<p>还有其他文件模块API可详见<a href="https://nodejs.org/api/fs.html#fs_fs_rename_oldpath_newpath_callback">官方文档</a></p>
<h1 id="getpost请求">GET/POST请求</h1>
<p>表单提交到服务器一般都使用 GET/POST 请求。</p>
<h2 id="获取get请求内容">获取GET请求内容</h2>
<p>由于GET请求直接被嵌入在路径中，URL是完整的请求路径，包括了?后面的部分，因此你可以手动解析后面的内容作为GET请求的参数。</p>
<p>node.js 中 url 模块中的 parse 函数提供了这个功能。</p>
<p>我们之前有用过：</p>
<pre><code>                   url.parse(string).query
                                           |
           url.parse(string).pathname      |
                       |                   |
                       |                   |
                     ------ -------------------
http://localhost:8888/start?foo=bar&amp;hello=world
                                ---       -----
                                 |          |
                                 |          |
              querystring.parse(queryString)[&quot;foo&quot;]    |
                                            |
                         querystring.parse(queryString)[&quot;hello&quot;]
</code></pre>
<p>可以在下面一个例子中看到和上面图中一样的结果</p>
<pre><code class="language-js">var http = require('http');
var url = require('url');
var util = require('util');
 
http.createServer(function(req, res){
    res.writeHead(200, {'Content-Type': 'text/plain; charset=utf-8'});
    res.end(util.inspect(url.parse(req.url, true)));
}).listen(3000);
</code></pre>
<p>在浏览器中访问 <strong>http://localhost:3000/user?name=菜鸟教程&amp;url=www.runoob.com</strong> 然后查看返回结果:</p>
<pre><code>Url {
  protocol: null,
  slashes: null,
  auth: null,
  host: null,
  port: null,
  hostname: null,
  hash: null,
  search: '?name=%E8%8F%9C%E9%B8%9F%E6%95%99%E7%A8%8B&amp;url=www.runoob.com',
  query: [Object: null prototype] { name: '菜鸟教程', url: 'www.runoob.com' },
  pathname: '/user',
  path: '/user?name=%E8%8F%9C%E9%B8%9F%E6%95%99%E7%A8%8B&amp;url=www.runoob.com',
  href: '/user?name=%E8%8F%9C%E9%B8%9F%E6%95%99%E7%A8%8B&amp;url=www.runoob.com'
}
</code></pre>
<h3 id="获取-url-的参数">获取 URL 的参数</h3>
<p>我们可以使用 url.parse 方法来解析 URL 中的参数，代码如下：</p>
<pre><code class="language-js">var http = require('http');
var url = require('url');
var util = require('util');
 
http.createServer(function(req, res){
    res.writeHead(200, {'Content-Type': 'text/plain'});
 
    // 解析 url 参数
    var params = url.parse(req.url, true).query;//拿到问号后面的参数
    res.write(&quot;网站名：&quot; + params.name);
    res.write(&quot;\n&quot;);
    res.write(&quot;网站 URL：&quot; + params.url);
    res.end();
 
}).listen(3000);
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201003180539.jpeg" alt="img" loading="lazy"></figure>
<h2 id="获取-post-请求内容">获取 POST 请求内容</h2>
<p>POST 请求的内容全部的都在请求体中，http.ServerRequest 并没有一个属性内容为请求体，原因是等待请求体传输可能是一件耗时的工作。</p>
<p>比如上传文件，而很多时候我们可能并不需要理会请求体的内容，恶意的POST请求会大大消耗服务器的资源，所以 node.js 默认是不会解析请求体的，当你需要的时候，需要手动来做。</p>
<p>请求结构如下：</p>
<pre><code class="language-js">var http = require('http');
var querystring = require('querystring');
var util = require('util');
 
http.createServer(function(req, res){
    // 定义了一个post变量，用于暂存请求体的信息
    var post = '';     
 
    // 通过req的data事件监听函数，每当接受到请求体的数据，就累加到post变量中
    req.on('data', function(chunk){    
        post += chunk;
    });
 
    // 在end事件触发后，通过querystring.parse将post解析为真正的POST请求格式，然后向客户端返回。
    req.on('end', function(){    
        post = querystring.parse(post);
        res.end(util.inspect(post));
    });
}).listen(3000);
</code></pre>
<p><code>querystring</code>模块是node.js解析和格式化URL查询字符串的工具。在node.js中，<code>querystring</code>代表<strong>查询字符串</strong>，说白了就是HTTP GET方法请求服务器的查询参数：</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20201003184228.webp" alt="img" loading="lazy"></figure>
<p>（黄色的框内就是查询字符串）</p>
<blockquote>
<ul>
<li>
<p>使用安全的字符表示不安全的字符，如中文，某些特殊的字符等。这样的话不管是浏览器环境，或者服务器环境，传输的数据都是安全的，可以正确地使用，不会出现数据的丢失或显示错误的情况</p>
</li>
<li>
<p>消除一些有歧义的字符，举个例子，你想通过一个参数<code>data</code>传输<code>'&amp;a=1&amp;b=1&amp;c=1'</code>这几个字符，如果这样写</p>
</li>
</ul>
</blockquote>
<p>以下实例表单通过 POST 提交并输出数据：</p>
<pre><code class="language-js">var http = require('http');
var querystring = require('querystring');
 
var postHTML = 
  '&lt;html&gt;&lt;head&gt;&lt;meta charset=&quot;utf-8&quot;&gt;&lt;title&gt;菜鸟教程 Node.js 实例&lt;/title&gt;&lt;/head&gt;' +
  '&lt;body&gt;' +
  '&lt;form method=&quot;post&quot;&gt;' +
  '网站名： &lt;input name=&quot;name&quot;&gt;&lt;br&gt;' +
  '网站 URL： &lt;input name=&quot;url&quot;&gt;&lt;br&gt;' +
  '&lt;input type=&quot;submit&quot;&gt;' +
  '&lt;/form&gt;' +
  '&lt;/body&gt;&lt;/html&gt;';
 
http.createServer(function (req, res) {
    var body = &quot;&quot;;
    req.on('data', function (chunk) {
        console.log(&quot;chunk=&quot;+chunk);
        body += chunk;
        console.log(&quot;body=&quot;+body);
    });
    req.on('end', function () {
        console.log(body);
        console.log(&quot;------------&quot;);
         // 解析参数
        body = querystring.parse(body);
        console.log(body);
        // 设置响应头部信息及编码
        res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'})
        if (body.name &amp;&amp; body.url) {// 输出提交的数据
            res.write(&quot;site name:&quot; +
                body.name);
            res.write(&quot;&lt;br&gt;&quot;);
            res.write(&quot;url:&quot; + body.url);
        } else {// 输出表单
            res.write(postHTML);
        }
        res.end();
    });
}).listen(3000);
</code></pre>
<p>执行结果：</p>
<pre><code>➜  getpost-demo node post.js
chunk=name=444&amp;url=555
body=name=444&amp;url=555
name=444&amp;url=555
------------
[Object: null prototype] { name: '444', url: '555' }

------------
[Object: null prototype] {}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NodeJS初学系列3——函数、路由、全局对象]]></title>
        <id>https://giriawsh.github.io/post/nodejs/</id>
        <link href="https://giriawsh.github.io/post/nodejs/">
        </link>
        <updated>2020-09-30T14:09:23.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>学习Node的第三天，今天学习了有函数、路由、全局对象（console、process等）</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>学习Node的第三天，今天学习了有函数、路由、全局对象（console、process等）</p>
<!-- more -->
<h1 id="函数">函数</h1>
<p>在 JavaScript中，一个函数可以作为另一个函数的参数。我们可以先定义一个函数，然后传递，也可以在传递参数的地方直接定义函数。</p>
<p>Node.js 中函数的使用与 JavaScript 类似：</p>
<pre><code class="language-js">function say(word) {
  console.log(word);
}

function execute(someFunction, value) {
  someFunction(value);
}

execute(say, &quot;Hello&quot;);
</code></pre>
<p>我们把 say 函数作为 execute 函数的第一个变量进行了传递。</p>
<p>这样一来， say 就变成了execute 中的本地变量 someFunction ，execute 可以通过调用 someFunction() （带括号的形式）来使用 say 函数。</p>
<p>当然，因为 say 有一个变量， execute 在调用 someFunction 时可以传递这样一个变量。</p>
<h2 id="匿名函数">匿名函数</h2>
<p>我们可以把一个函数作为变量传递。但是我们不一定要绕这个&quot;先定义，再传递&quot;的圈子，我们可以直接在另一个函数的括号中定义和传递这个函数：</p>
<pre><code class="language-js">function execute(someFunction, value) {
  someFunction(value);
}

execute(function(word){ console.log(word) }, &quot;Hello&quot;);
</code></pre>
<p>用这种方式，我们甚至不用给这个函数起名字——匿名函数</p>
<h2 id="函数传递是如何让http服务器工作的">函数传递是如何让HTTP服务器工作的</h2>
<pre><code class="language-js">var http = require(&quot;http&quot;);

http.createServer(function(request, response) {
  response.writeHead(200, {&quot;Content-Type&quot;: &quot;text/plain&quot;});
  response.write(&quot;Hello World&quot;);
  response.end();
}).listen(8888);
</code></pre>
<p>我们向 createServer 函数传递了一个匿名函数。</p>
<p>等价于以下的代码：</p>
<pre><code class="language-js">var http = require(&quot;http&quot;);

function onRequest(request, response) {
  response.writeHead(200, {&quot;Content-Type&quot;: &quot;text/plain&quot;});
  response.write(&quot;Hello World&quot;);
  response.end();
}

http.createServer(onRequest).listen(8888);
</code></pre>
<h1 id="路由">路由</h1>
<p>我们要为路由提供请求的 URL 和其他需要的 GET 及 POST 参数，随后路由需要根据这些数据来执行相应的代码。</p>
<p>因此，我们需要查看 HTTP 请求，<strong>从中提取出请求的 URL 以及 GET/POST 参数</strong>。这里暂定其为我们的HTTP服务器的功能。</p>
<p>我们需要的所有数据都会包含在 <strong>request 对象中，该对象作为 onRequest() 回调函数的第一个参数传递</strong>。但是为了解析这些数据，我们需要额外的 Node.JS 模块，它们分别是 url 和 querystring 模块。</p>
<pre><code>                   url.parse(string).query
                                           |
           url.parse(string).pathname      |
                       |                   |
                       |                   |
                     ------ -------------------
http://localhost:8888/start?foo=bar&amp;hello=world
                                ---       -----
                                 |          |
                                 |          |
              querystring.parse(queryString)[&quot;foo&quot;]    |
                                            |
                         querystring.parse(queryString)[&quot;hello&quot;]
</code></pre>
<p>在server.js中给 onRequest() 函数加上一些逻辑，用来找出浏览器请求的 URL 路径：</p>
<pre><code class="language-js">var http = require(&quot;http&quot;);
var url = require(&quot;url&quot;);
 
function start() {
  function onRequest(request, response) {
    var pathname = url.parse(request.url).pathname;
    console.log(&quot;Request for &quot; + pathname + &quot; received.&quot;);
    response.writeHead(200, {&quot;Content-Type&quot;: &quot;text/plain&quot;});
    response.write(&quot;Hello World&quot;);
    response.end();
  }
 
  http.createServer(onRequest).listen(8888);
  console.log(&quot;Server has started.&quot;);
}
 
exports.start = start;
</code></pre>
<p>我们的应用现在可以通过请求的 URL 路径来区别不同请求了(可以区分/start或者/upload)，可以根据pathname的不同使用不同的代码来处理。</p>
<p>编写路由，建立一个名为 <strong>router.js</strong> 的文件：</p>
<pre><code class="language-js">function route(pathname) {
  console.log(&quot;About to route a request for &quot; + pathname);
}
 
exports.route = route;
</code></pre>
<p>我们的服务器应当知道路由的存在并加以有效利用。我们当然可以通过硬编码的方式将这一依赖项绑定到服务器上，但是其它语言的编程经验告诉我们这会是一件非常痛苦的事，因此我们将使用依赖注入的方式较松散地添加路由模块。</p>
<p>扩展一下服务器的 start() 函数，以便将路由函数作为参数传递过去。</p>
<pre><code class="language-js">var http = require(&quot;http&quot;);
var url = require(&quot;url&quot;);
 
function start(route) {
  function onRequest(request, response) {
    var pathname = url.parse(request.url).pathname;
    console.log(&quot;Request for &quot; + pathname + &quot; received.&quot;);
 
    route(pathname);
 
    response.writeHead(200, {&quot;Content-Type&quot;: &quot;text/plain&quot;});
    response.write(&quot;Hello World&quot;);
    response.end();
  }
 
  http.createServer(onRequest).listen(8888);
  console.log(&quot;Server has started.&quot;);
}
 
exports.start = start;
</code></pre>
<p>同时，我们会相应扩展 index.js，使得路由函数可以被注入到服务器中：</p>
<pre><code class="language-js">var server = require(&quot;./server&quot;);
var router = require(&quot;./router&quot;);
 
server.start(router.route);
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200930202252.png" alt="image-20200930202245566" loading="lazy"></figure>
<h1 id="全局对象">全局对象</h1>
<p>JavaScript 中有一个特殊的对象，称为全局对象（Global Object），它及其所有属性都可以在程序的任何地方访问，即全局变量。</p>
<p>在浏览器 JavaScript 中，通常 window 是全局对象， 而 Node.js 中的全局对象是 global，所有全局变量（除了 global 本身以外）都是 global 对象的属性。</p>
<h2 id="全局对象与全局变量">全局对象与全局变量</h2>
<p>global 最根本的作用是作为全局变量的宿主。按照 ECMAScript 的定义，满足以下条 件的变量是全局变量：</p>
<ul>
<li>在最外层定义的变量；</li>
<li>全局对象的属性；</li>
<li>隐式定义的变量（未定义直接赋值的变量）。</li>
</ul>
<p>当你定义一个全局变量时，这个变量同时也会成为全局对象的属性，反之亦然。</p>
<p>需要注 意的是，在 Node.js 中你不可能在最外层定义变量，因为<strong>所有用户代码都是属于当前模块的</strong>， 而模块本身不是最外层上下文。</p>
<h2 id="__filename">__filename</h2>
<p><strong>__filename</strong> 表示当前正在执行的脚本的文件名。它将输出文件所在位置的绝对路径，且和命令行参数所指定的文件名不一定相同。 如果在模块中，返回的值是模块文件的路径。</p>
<p>创建文件 main.js:</p>
<pre><code>// 输出全局变量 __filename 的值
console.log( __filename );
</code></pre>
<p>执行 main.js 文件，代码如下所示:</p>
<pre><code>➜  global-demo node index.js
K:\projects\node\first-test\global-demo\index.js
</code></pre>
<h2 id="__dirname">__dirname</h2>
<p><strong>__dirname</strong> 表示当前执行脚本所在的目录。</p>
<pre><code>➜  global-demo node index.js
K:\projects\node\first-test\global-demo
</code></pre>
<h2 id="settimeoutcb-ms">setTimeout(cb, ms)</h2>
<p><strong>setTimeout(cb, ms)</strong> 全局函数在指定的毫秒(ms)数后执行指定函数(cb)。：setTimeout() 只执行一次指定函数。</p>
<p>返回一个代表定时器的句柄值。</p>
<pre><code>function printHello(){
   console.log( &quot;Hello, World!&quot;);
}
// 两秒后执行以上函数
setTimeout(printHello, 2000);
</code></pre>
<h2 id="cleartimeoutt">clearTimeout(t)</h2>
<p><strong>clearTimeout( t )</strong> 全局函数用于停止一个之前通过 setTimeout() 创建的定时器。 参数 <strong>t</strong> 是通过 setTimeout() 函数创建的定时器。</p>
<pre><code>function printHello(){
   console.log( &quot;Hello, World!&quot;);
}
// 两秒后执行以上函数
var t = setTimeout(printHello, 2000);

// 清除定时器
clearTimeout(t);
</code></pre>
<h2 id="setintervalcb-ms">setInterval(cb, ms)</h2>
<p><strong>setInterval(cb, ms)</strong> 全局函数在指定的毫秒(ms)数后执行指定函数(cb)。</p>
<p>返回一个代表定时器的句柄值。可以使用 <strong>clearInterval(t)</strong> 函数来清除定时器。</p>
<p><strong>setInterval() 方法会不停地调用函数，直到 clearInterval() 被调用或窗口被关闭。</strong></p>
<pre><code>function printHello(){
   console.log( &quot;Hello, World!&quot;);
}
// 两秒后执行以上函数
setInterval(printHello, 2000);
</code></pre>
<p>以上程序每隔两秒就会输出一次&quot;Hello, World!&quot;，且会永久执行下去，直到你按下 <strong>ctrl + c</strong> 按钮。</p>
<h2 id="console">console</h2>
<p>console 用于提供控制台标准输出，它是由 Internet Explorer 的 JScript 引擎提供的调试工具，后来逐渐成为浏览器的实施标准。</p>
<p>Node.js 沿用了这个标准，提供与习惯行为一致的 console 对象，用于向标准输出流（stdout）或标准错误流（stderr）输出字符。</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200930213631.png" alt="image-20200930213631442" loading="lazy"></figure>
<ul>
<li>console.error()：与console.log() 用法相同，只是向标准错误流输出。</li>
<li>console.trace()：向标准错误流输出当前的调用栈。</li>
</ul>
<pre><code class="language-js">console.info(&quot;程序开始执行：&quot;);

var counter = 10;
console.log(&quot;计数: %d&quot;, counter);

console.time(&quot;获取数据&quot;);
//
// 执行一些代码
// 
console.timeEnd('获取数据');

console.info(&quot;程序执行完毕。&quot;)
</code></pre>
<h2 id="process">process</h2>
<p>process 是一个全局变量，即 global 对象的属性。</p>
<p>它用于描述当前Node.js 进程状态的对象，提供了一个与操作系统的简单接口。通常在你写本地命令行程序的时候，少不了要 和它打交道。</p>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">事件 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>exit</strong> 当进程准备退出时触发。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>beforeExit</strong> 当 node 清空事件循环，并且没有其他安排时触发这个事件。通常来说，当没有进程安排时 node 退出，但是 'beforeExit' 的监听器可以异步调用，这样 node 就会继续执行。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>uncaughtException</strong> 当一个异常冒泡回到事件循环，触发这个事件。如果给异常添加了监视器，默认的操作（打印堆栈跟踪信息并退出）就不会发生。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>Signal 事件</strong> 当进程接收到信号时就触发。信号列表详见标准的 POSIX 信号名，如 SIGINT、SIGUSR1 等。</td>
</tr>
</tbody>
</table>
<pre><code class="language-js">process.on('exit', function(code) {

  // 以下代码永远不会执行
  setTimeout(function() {
    console.log(&quot;该代码不会执行&quot;);
  }, 0);
  
  console.log('退出码为:', code);
});
console.log(&quot;程序执行结束&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>$ node main.js
程序执行结束
退出码为: 0
</code></pre>
<h3 id="退出状态码">退出状态码</h3>
<table>
<thead>
<tr>
<th style="text-align:left">状态码</th>
<th style="text-align:left">名称 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>Uncaught Fatal Exception</strong> 有未捕获异常，并且没有被域或 uncaughtException 处理函数处理。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>Unused</strong> 保留</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>Internal JavaScript Parse Error</strong> JavaScript的源码启动 Node 进程时引起解析错误。非常罕见，仅会在开发 Node 时才会有。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>Internal JavaScript Evaluation Failure</strong> JavaScript 的源码启动 Node 进程，评估时返回函数失败。非常罕见，仅会在开发 Node 时才会有。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>Fatal Error</strong> V8 里致命的不可恢复的错误。通常会打印到 stderr ，内容为： FATAL ERROR</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>Non-function Internal Exception Handler</strong> 未捕获异常，内部异常处理函数不知为何设置为on-function，并且不能被调用。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>Internal Exception Handler Run-Time Failure</strong> 未捕获的异常， 并且异常处理函数处理时自己抛出了异常。例如，如果 process.on('uncaughtException') 或 domain.on('error') 抛出了异常。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>Unused</strong> 保留</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>Invalid Argument</strong> 可能是给了未知的参数，或者给的参数没有值。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>Internal JavaScript Run-Time Failure</strong> JavaScript的源码启动 Node 进程时抛出错误，非常罕见，仅会在开发 Node 时才会有。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>Invalid Debug Argument</strong> 设置了参数--debug 和/或 --debug-brk，但是选择了错误端口。</td>
</tr>
<tr>
<td style="text-align:left">128</td>
<td style="text-align:left"><strong>Signal Exits</strong> 如果 Node 接收到致命信号，比如SIGKILL 或 SIGHUP，那么退出代码就是128 加信号代码。这是标准的 Unix 做法，退出信号代码放在高位。</td>
</tr>
</tbody>
</table>
<h3 id="process属性">Process属性</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号.</th>
<th style="text-align:left">属性 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>stdout</strong> 标准输出流。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>stderr</strong> 标准错误流。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>stdin</strong> 标准输入流。</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>argv</strong> argv 属性返回一个数组，由命令行执行脚本时的各个参数组成。它的第一个成员总是node，第二个成员是脚本文件名，其余成员是脚本文件的参数。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>execPath</strong> 返回执行当前脚本的 Node 二进制文件的绝对路径。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>execArgv</strong> 返回一个数组，成员是命令行下执行脚本时，在Node可执行文件与脚本文件之间的命令行参数。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>env</strong> 返回一个对象，成员为当前 shell 的环境变量</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>exitCode</strong> 进程退出时的代码，如果进程优通过 process.exit() 退出，不需要指定退出码。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>version</strong> Node 的版本，比如v0.10.18。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>versions</strong> 一个属性，包含了 node 的版本和依赖.</td>
</tr>
<tr>
<td style="text-align:left">11</td>
<td style="text-align:left"><strong>config</strong> 一个包含用来编译当前 node 执行文件的 javascript 配置选项的对象。它与运行 ./configure 脚本生成的 &quot;config.gypi&quot; 文件相同。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>pid</strong> 当前进程的进程号。</td>
</tr>
<tr>
<td style="text-align:left">13</td>
<td style="text-align:left"><strong>title</strong> 进程名，默认值为&quot;node&quot;，可以自定义该值。</td>
</tr>
<tr>
<td style="text-align:left">14</td>
<td style="text-align:left"><strong>arch</strong> 当前 CPU 的架构：'arm'、'ia32' 或者 'x64'。</td>
</tr>
<tr>
<td style="text-align:left">15</td>
<td style="text-align:left"><strong>platform</strong> 运行程序所在的平台系统 'darwin', 'freebsd', 'linux', 'sunos' 或 'win32'</td>
</tr>
<tr>
<td style="text-align:left">16</td>
<td style="text-align:left"><strong>mainModule</strong> require.main 的备选方法。不同点，如果主模块在运行时改变，require.main可能会继续返回老的模块。可以认为，这两者引用了同一个模块。</td>
</tr>
</tbody>
</table>
<pre><code>// 输出到终端
process.stdout.write(&quot;Hello World!&quot; + &quot;\n&quot;);

// 通过参数读取
process.argv.forEach(function(val, index, array) {
   console.log(index + ': ' + val);
});

// 获取执行路径
console.log(process.execPath);


// 平台信息
console.log(process.platform);
</code></pre>
<pre><code>Hello World!
0: C:\Program Files\nodejs\node.exe
1: K:\projects\node\first-test\global-demo\index.js
C:\Program Files\nodejs\node.exe
win32
</code></pre>
<h3 id="process方法">Process方法</h3>
<table>
<thead>
<tr>
<th style="text-align:left">序号</th>
<th style="text-align:left">方法 &amp; 描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">1</td>
<td style="text-align:left"><strong>abort()</strong> 这将导致 node 触发 abort 事件。会让 node 退出并生成一个核心文件。</td>
</tr>
<tr>
<td style="text-align:left">2</td>
<td style="text-align:left"><strong>chdir(directory)</strong> 改变当前工作进程的目录，如果操作失败抛出异常。</td>
</tr>
<tr>
<td style="text-align:left">3</td>
<td style="text-align:left"><strong>cwd()</strong> 返回当前进程的工作目录</td>
</tr>
<tr>
<td style="text-align:left">4</td>
<td style="text-align:left"><strong>exit([code])</strong> 使用指定的 code 结束进程。如果忽略，将会使用 code 0。</td>
</tr>
<tr>
<td style="text-align:left">5</td>
<td style="text-align:left"><strong>getgid()</strong> 获取进程的群组标识（参见 getgid(2)）。获取到得时群组的数字 id，而不是名字。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">6</td>
<td style="text-align:left"><strong>setgid(id)</strong> 设置进程的群组标识（参见 setgid(2)）。可以接收数字 ID 或者群组名。如果指定了群组名，会阻塞等待解析为数字 ID 。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">7</td>
<td style="text-align:left"><strong>getuid()</strong> 获取进程的用户标识(参见 getuid(2))。这是数字的用户 id，不是用户名。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">8</td>
<td style="text-align:left"><strong>setuid(id)</strong> 设置进程的用户标识（参见setuid(2)）。接收数字 ID或字符串名字。果指定了群组名，会阻塞等待解析为数字 ID 。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">9</td>
<td style="text-align:left"><strong>getgroups()</strong> 返回进程的群组 iD 数组。POSIX 系统没有保证一定有，但是 node.js 保证有。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">10</td>
<td style="text-align:left"><strong>setgroups(groups)</strong> 设置进程的群组 ID。这是授权操作，所以你需要有 root 权限，或者有 CAP_SETGID 能力。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">11</td>
<td style="text-align:left"><strong>initgroups(user, extra_group)</strong> 读取 /etc/group ，并初始化群组访问列表，使用成员所在的所有群组。这是授权操作，所以你需要有 root 权限，或者有 CAP_SETGID 能力。 注意：这个函数仅在 POSIX 平台上可用(例如，非Windows 和 Android)。</td>
</tr>
<tr>
<td style="text-align:left">12</td>
<td style="text-align:left"><strong>kill(pid[, signal])</strong> 发送信号给进程. pid 是进程id，并且 signal 是发送的信号的字符串描述。信号名是字符串，比如 'SIGINT' 或 'SIGHUP'。如果忽略，信号会是 'SIGTERM'。</td>
</tr>
<tr>
<td style="text-align:left">13</td>
<td style="text-align:left"><strong>memoryUsage()</strong> 返回一个对象，描述了 Node 进程所用的内存状况，单位为字节。</td>
</tr>
<tr>
<td style="text-align:left">14</td>
<td style="text-align:left"><strong>nextTick(callback)</strong> 一旦当前事件循环结束，调用回调函数。</td>
</tr>
<tr>
<td style="text-align:left">15</td>
<td style="text-align:left"><strong>umask([mask])</strong> 设置或读取进程文件的掩码。子进程从父进程继承掩码。如果mask 参数有效，返回旧的掩码。否则，返回当前掩码。</td>
</tr>
<tr>
<td style="text-align:left">16</td>
<td style="text-align:left"><strong>uptime()</strong> 返回 Node 已经运行的秒数。</td>
</tr>
<tr>
<td style="text-align:left">17</td>
<td style="text-align:left"><strong>hrtime()</strong> 返回当前进程的高分辨时间，形式为 [seconds, nanoseconds]数组。它是相对于过去的任意事件。该值与日期无关，因此不受时钟漂移的影响。主要用途是可以通过精确的时间间隔，来衡量程序的性能。 你可以将之前的结果传递给当前的 process.hrtime() ，会返回两者间的时间差，用来基准和测量时间间隔。</td>
</tr>
</tbody>
</table>
<pre><code class="language-js">// 输出当前目录
console.log('当前目录: ' + process.cwd());

// 输出当前版本
console.log('当前版本: ' + process.version);

// 输出内存使用情况
console.log(process.memoryUsage());
</code></pre>
<pre><code>当前目录: K:\projects\node\first-test\global-demo
当前版本: v12.14.0
{
  rss: 18538496,
  heapTotal: 4734976,
  heapUsed: 2444624,
  external: 844606
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NodeJS初学系列2——Buffer、Stream、模块系统]]></title>
        <id>https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-2-bufferstreammo-kuai-xi-tong/</id>
        <link href="https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-2-bufferstreammo-kuai-xi-tong/">
        </link>
        <updated>2020-09-30T10:24:18.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>今天是学习Node的第二天，今天学习了有Buffer、Stream、模块系统等（wait怎么越来越有c++那味儿了）</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>今天是学习Node的第二天，今天学习了有Buffer、Stream、模块系统等（wait怎么越来越有c++那味儿了）</p>
<!-- more -->
<h1 id="buffer">Buffer</h1>
<p>JavaScript 语言自身只有字符串数据类型，没有二进制数据类型。</p>
<p>但在处理像TCP流或文件流时，必须使用到二进制数据。因此在 Node.js中，定义了一个 Buffer 类，该类用来创建一个专门存放二进制数据的缓存区。</p>
<p>在 Node.js 中，Buffer 类是随 Node 内核一起发布的核心库。Buffer 库为 Node.js 带来了一种存储原始数据的方法，可以让 Node.js 处理二进制数据，每当需要在 Node.js 中处理I/O操作中移动的数据时，就有可能使用 Buffer 库。</p>
<p>原始数据存储在 Buffer 类的实例中。一个 Buffer 类似于一个整数数组，但它对应于 V8 堆内存之外的一块原始内存。</p>
<h2 id="buffer与字符编码">Buffer与字符编码</h2>
<p>Buffer 实例一般用于表示编码字符的序列，比如 UTF-8 、 UCS2 、 Base64 、或十六进制编码的数据。</p>
<p>Buffer 实例与普通的 JavaScript 字符串之间进行相互转换：</p>
<pre><code>const buf = Buffer.from('runoob', 'ascii');

// 输出 72756e6f6f62
console.log(buf.toString('hex'));

// 输出 cnVub29i
console.log(buf.toString('base64'));
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200929170751.png" alt="image-20200929170744188" loading="lazy"></figure>
<h2 id="buffer类">Buffer类</h2>
<p>Buffer 提供了以下 API 来创建 Buffer 类：</p>
<ul>
<li><strong>Buffer.alloc(size[, fill[, encoding]])：</strong> 返回一个指定大小的 Buffer 实例，如果没有设置 fill，则默认填满 0</li>
<li><strong>Buffer.allocUnsafe(size)：</strong> 返回一个指定大小的 Buffer 实例，但是它不会被初始化，所以它可能包含敏感的数据</li>
<li><strong>Buffer.allocUnsafeSlow(size)</strong></li>
<li><strong>Buffer.from(array)：</strong> 返回一个被 array 的值初始化的新的 Buffer 实例（传入的 array 的元素只能是数字，不然就会自动被 0 覆盖）</li>
<li><strong>Buffer.from(arrayBuffer[, byteOffset[, length]])：</strong> 返回一个新建的与给定的 ArrayBuffer 共享同一内存的 Buffer。</li>
<li><strong>Buffer.from(buffer)：</strong> 复制传入的 Buffer 实例的数据，并返回一个新的 Buffer 实例</li>
<li><strong>Buffer.from(string[, encoding])：</strong> 返回一个被 string 的值初始化的新的 Buffer 实例</li>
</ul>
<pre><code class="language-js">// 创建一个长度为 10、且用 0 填充的 Buffer。
const buf1 = Buffer.alloc(10);

// 创建一个长度为 10、且用 0x1 填充的 Buffer。 
const buf2 = Buffer.alloc(10, 1);

// 创建一个长度为 10、且未初始化的 Buffer。
// 这个方法比调用 Buffer.alloc() 更快，
// 但返回的 Buffer 实例可能包含旧数据，
// 因此需要使用 fill() 或 write() 重写。
const buf3 = Buffer.allocUnsafe(10);

// 创建一个包含 [0x1, 0x2, 0x3] 的 Buffer。
const buf4 = Buffer.from([1, 2, 3]);

// 创建一个包含 UTF-8 字节 [0x74, 0xc3, 0xa9, 0x73, 0x74] 的 Buffer。
const buf5 = Buffer.from('tést');

// 创建一个包含 Latin-1 字节 [0x74, 0xe9, 0x73, 0x74] 的 Buffer。
const buf6 = Buffer.from('tést', 'latin1');
</code></pre>
<h2 id="写入缓冲区">写入缓冲区</h2>
<pre><code class="language-js">buf.write(string[, offset[, length]][, encoding])
</code></pre>
<ul>
<li><strong>string</strong> - 写入缓冲区的字符串。</li>
<li><strong>offset</strong> - 缓冲区开始写入的索引值，默认为 0 。</li>
<li><strong>length</strong> - 写入的字节数，默认为 buffer.length</li>
<li><strong>encoding</strong> - 使用的编码。默认为 'utf8' 。</li>
</ul>
<p>返回实际写入的大小。如果 buffer 空间不足， 则只会写入部分字符串。</p>
<pre><code>buf = Buffer.alloc(256);
len = buf.write(&quot;www.runoob.com&quot;);

console.log(&quot;写入字节数 : &quot;+  len);
</code></pre>
<p>执行结果：</p>
<pre><code>$node main.js
写入字节数 : 14
</code></pre>
<h2 id="读取缓冲区">读取缓冲区</h2>
<pre><code>buf.toString([encoding[, start[, end]]])
</code></pre>
<ul>
<li><strong>encoding</strong> - 使用的编码。默认为 'utf8' 。</li>
<li><strong>start</strong> - 指定开始读取的索引位置，默认为 0。</li>
<li><strong>end</strong> - 结束位置，默认为缓冲区的末尾。</li>
</ul>
<p>解码缓冲区数据并使用指定的编码返回字符串。</p>
<pre><code>buf = Buffer.alloc(26);
for (var i = 0 ; i &lt; 26 ; i++) {
  buf[i] = i + 97;
}

console.log( buf.toString('ascii'));       // 输出: abcdefghijklmnopqrstuvwxyz
console.log( buf.toString('ascii',0,5));   //使用 'ascii' 编码, 并输出: abcde
console.log( buf.toString('utf8',0,5));    // 使用 'utf8' 编码, 并输出: abcde
console.log( buf.toString(undefined,0,5)); // 使用默认的 'utf8' 编码, 并输出: abcde
</code></pre>
<h2 id="buffer转换为json对象">Buffer转换为JSON对象</h2>
<pre><code class="language-js">buf.toJSON()
</code></pre>
<p>当字符串化一个 Buffer 实例时，JSON.stringify() 会隐式地调用该 <strong>toJSON()</strong>。</p>
<p>返回 JSON 对象。</p>
<pre><code class="language-js">const buf = Buffer.from([0x1, 0x2, 0x3, 0x4, 0x5]);
const json = JSON.stringify(buf);

// 输出: {&quot;type&quot;:&quot;Buffer&quot;,&quot;data&quot;:[1,2,3,4,5]}
console.log(json);

const copy = JSON.parse(json, (key, value) =&gt; {
  return value &amp;&amp; value.type === 'Buffer' ?
    Buffer.from(value.data) :
    value;
});

// 输出: &lt;Buffer 01 02 03 04 05&gt;
console.log(copy);
</code></pre>
<h2 id="缓冲区合并">缓冲区合并</h2>
<pre><code class="language-js">Buffer.concat(list[, totalLength])
</code></pre>
<ul>
<li><strong>list</strong> - 用于合并的 Buffer 对象数组列表。</li>
<li><strong>totalLength</strong> - 指定合并后Buffer对象的总长度。</li>
</ul>
<p>返回一个多个成员合并的新 Buffer 对象。</p>
<pre><code>var buffer1 = Buffer.from(('菜鸟教程'));
var buffer2 = Buffer.from(('www.runoob.com'));
var buffer3 = Buffer.concat([buffer1,buffer2]);
console.log(&quot;buffer3 内容: &quot; + buffer3.toString());
</code></pre>
<p>执行结果：</p>
<pre><code>buffer3 内容: 菜鸟教程www.runoob.com
</code></pre>
<h2 id="缓冲区比较">缓冲区比较</h2>
<pre><code>buf.compare(otherBuffer);
</code></pre>
<p><strong>otherBuffer</strong> - 与 <strong>buf</strong> 对象比较的另外一个 Buffer 对象。</p>
<p>返回一个数字，表示 <strong>buf</strong> 在 <strong>otherBuffer</strong> 之前，之后或相同。</p>
<pre><code class="language-js">var buffer1 = Buffer.from('ABC');
var buffer2 = Buffer.from('ABCD');
var result = buffer1.compare(buffer2);

if(result &lt; 0) {
   console.log(buffer1 + &quot; 在 &quot; + buffer2 + &quot;之前&quot;);
}else if(result == 0){
   console.log(buffer1 + &quot; 与 &quot; + buffer2 + &quot;相同&quot;);
}else {
   console.log(buffer1 + &quot; 在 &quot; + buffer2 + &quot;之后&quot;);
}
</code></pre>
<p>执行结果：</p>
<pre><code>ABC在ABCD之前
</code></pre>
<h2 id="拷贝缓冲区">拷贝缓冲区</h2>
<pre><code>buf.copy(targetBuffer[, targetStart[, sourceStart[, sourceEnd]]])
</code></pre>
<ul>
<li><strong>targetBuffer</strong> - 要拷贝的 Buffer 对象。</li>
<li><strong>targetStart</strong> - 数字, 可选, 默认: 0</li>
<li><strong>sourceStart</strong> - 数字, 可选, 默认: 0</li>
<li><strong>sourceEnd</strong> - 数字, 可选, 默认: buffer.length</li>
</ul>
<p>没有返回值</p>
<pre><code class="language-js">var buf1 = Buffer.from('abcdefghijkl');
var buf2 = Buffer.from('RUNOOB');

//将 buf2 插入到 buf1 指定位置上
buf2.copy(buf1, 2);

console.log(buf1.toString());
</code></pre>
<p>执行结果：</p>
<pre><code>abRUNOOBijkl
</code></pre>
<h2 id="缓冲区裁剪">缓冲区裁剪</h2>
<pre><code>buf.slice([start[, end]])
</code></pre>
<ul>
<li><strong>start</strong> - 数字, 可选, 默认: 0</li>
<li><strong>end</strong> - 数字, 可选, 默认: buffer.length</li>
</ul>
<p>返回一个新的缓冲区，它和旧缓冲区指向同一块内存，但是从索引 start 到 end 的位置剪切。</p>
<pre><code class="language-js">var buffer1 = Buffer.from('runoob');
// 剪切缓冲区
var buffer2 = buffer1.slice(0,2);
console.log(&quot;buffer2 content: &quot; + buffer2.toString());
</code></pre>
<p>执行结果：</p>
<pre><code>buffer2 content: ru
</code></pre>
<h2 id="缓冲区长度">缓冲区长度</h2>
<pre><code>buf.length;
</code></pre>
<p>返回 Buffer 对象所占据的内存长度。</p>
<h1 id="stream">Stream</h1>
<p>Stream 有四种流类型：</p>
<ul>
<li><strong>Readable</strong> - 可读操作。</li>
<li><strong>Writable</strong> - 可写操作。</li>
<li><strong>Duplex</strong> - 可读可写操作.</li>
<li><strong>Transform</strong> - 操作被写入数据，然后读出结果。</li>
</ul>
<p>所有的 Stream 对象都是 EventEmitter 的实例。常用的事件有：</p>
<ul>
<li><strong>data</strong> - 当有数据可读时触发。</li>
<li><strong>end</strong> - 没有更多的数据可读时触发。</li>
<li><strong>error</strong> - 在接收和写入过程中发生错误时触发。</li>
<li><strong>finish</strong> - 所有数据已被写入到底层系统时触发。</li>
</ul>
<h2 id="从流中读取数据">从流中读取数据</h2>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var data = '';

// 创建可读流
var readerStream = fs.createReadStream('input.txt');

// 设置编码为 utf8。
readerStream.setEncoding('UTF8');

// 处理流事件 --&gt; data, end, and error
readerStream.on('data', function(chunk) {
   data += chunk;
});

readerStream.on('end',function(){
   console.log(data);
});

readerStream.on('error', function(err){
   console.log(err.stack);
});

console.log(&quot;程序执行完毕&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>程序执行完毕
菜鸟教程官网地址：www.runoob.com
</code></pre>
<h2 id="写入流">写入流</h2>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var data = '菜鸟教程官网地址：www.runoob.com';

// 创建一个可以写入的流，写入到文件 output.txt 中
var writerStream = fs.createWriteStream('output.txt');

// 使用 utf8 编码写入数据
writerStream.write(data,'UTF8');

// 标记文件末尾
writerStream.end();

// 处理流事件 --&gt; finish、error
writerStream.on('finish', function() {
    console.log(&quot;写入完成。&quot;);
});

writerStream.on('error', function(err){
   console.log(err.stack);
});

console.log(&quot;程序执行完毕&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>$ node main.js 
程序执行完毕
写入完成。
</code></pre>
<h2 id="管道流">管道流</h2>
<p>管道提供了一个输出流到输入流的机制。通常我们用于从一个流中获取数据并将数据传递到另外一个流中。</p>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

// 创建一个可读流
var readerStream = fs.createReadStream('input.txt');

// 创建一个可写流
var writerStream = fs.createWriteStream('output.txt');

// 管道读写操作
// 读取 input.txt 文件内容，并将内容写入到 output.txt 文件中
readerStream.pipe(writerStream);

console.log(&quot;程序执行完毕&quot;);
</code></pre>
<p>代码执行结果如下：</p>
<pre><code>$ node main.js 
程序执行完毕
</code></pre>
<p>查看 output.txt 文件的内容：</p>
<pre><code>$ cat output.txt 
菜鸟教程官网地址：www.runoob.com
管道流操作实例
</code></pre>
<h2 id="链式流">链式流</h2>
<p><strong>链式是通过连接输出流到另外一个流并创建多个流操作链的机制</strong>。链式流一般用于管道操作。</p>
<p>接下来我们就是用管道和链式来压缩和解压文件。</p>
<p>压缩文件实例，创建 compress.js 文件：</p>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var zlib = require('zlib');

// 压缩 input.txt 文件为 input.txt.gz
fs.createReadStream('input.txt')
  .pipe(zlib.createGzip())
  .pipe(fs.createWriteStream('input.txt.gz'));
  
console.log(&quot;文件压缩完成。&quot;);
</code></pre>
<p>执行完以上操作后，我们可以看到当前目录下生成了 input.txt 的压缩文件 input.txt.gz。</p>
<p>接下来，让我们来解压该文件，创建 decompress.js 文件：</p>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);
var zlib = require('zlib');

// 解压 input.txt.gz 文件为 input.txt
fs.createReadStream('input.txt.gz')
  .pipe(zlib.createGunzip())
  .pipe(fs.createWriteStream('input.txt'));
  
console.log(&quot;文件解压完成。&quot;);
</code></pre>
<h1 id="模块系统">模块系统</h1>
<p>为了让Node.js的文件可以相互调用，Node.js提供了一个简单的模块系统。</p>
<p>模块是Node.js 应用程序的基本组成部分，文件和模块是一一对应的。换言之，一个 Node.js 文件就是一个模块，这个文件可能是JavaScript 代码、JSON 或者编译过的C/C++ 扩展。</p>
<h2 id="创建模块">创建模块</h2>
<p>Node.js 提供了 exports 和 require 两个对象，其中 exports 是模块公开的接口，require 用于从外部获取一个模块的接口，即所获取模块的 exports 对象。</p>
<p>简单使用：</p>
<pre><code class="language-js">//main.js
var hello = require('./hello');
hello.world();

//hello.js
exports.world = function() {
  console.log('Hello World');
}
</code></pre>
<p>有时候我们只是想把一个对象封装到模块中</p>
<pre><code class="language-js">//hello.js 
function Hello() { 
    var name; 
    this.setName = function(thyName) { 
        name = thyName; 
    }; 
    this.sayHello = function() { 
        console.log('Hello ' + name); 
    }; 
}; 
module.exports = Hello;
</code></pre>
<p>这样就可以直接获得这个对象了：</p>
<pre><code class="language-js">//main.js 
var Hello = require('./hello'); 
hello = new Hello(); 
hello.setName('BYVoid'); 
hello.sayHello(); 
</code></pre>
<p>使用 module.exports = Hello 代替了exports.world = function(){}。 在外部引用该模块时，其接口对象就是要输出的 <strong>Hello 对象本身</strong>，而不是原先的 exports。</p>
<h2 id="服务端的模块">服务端的模块</h2>
<pre><code>var http = require(&quot;http&quot;);

...

http.createServer(...);
</code></pre>
<p>Node.js 中自带了一个叫做 <strong>http</strong> 的模块，我们在我们的代码中请求它并把返回值赋给一个本地变量。</p>
<p>这把我们的本地变量变成了一个拥有所有 http 模块所提供的公共方法的对象。</p>
<h3 id="require-方法中的文件查找策略">require 方法中的文件查找策略</h3>
<p>由于 Node.js 中存在 4 类模块（原生模块和3种文件模块），尽管 require 方法极其简单，但是内部的加载却是十分复杂的，其加载优先级也各自不同。</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200929183520.jpeg" alt="img" loading="lazy"></figure>
<h4 id="从文件模块缓存中加载">从文件模块缓存中加载</h4>
<p>尽管原生模块与文件模块的优先级不同，但是都会优先从文件模块的缓存中加载已经存在的模块。</p>
<h4 id="从原生模块加载">从原生模块加载</h4>
<p>原生模块的优先级仅次于文件模块缓存的优先级。require 方法在解析文件名之后，优先检查模块是否在原生模块列表中。以http模块为例，尽管在目录下存在一个 http/http.js/http.node/http.json 文件，require(&quot;http&quot;) 都不会从这些文件中加载，而是<strong>从原生模块中加载</strong>。</p>
<p>原生模块也有一个缓存区，同样也是优先从缓存区加载。如果缓存区没有被加载过，则调用原生模块的加载方式进行加载和执行。</p>
<h4 id="从文件加载">从文件加载</h4>
<p>当文件模块缓存中不存在，而且不是原生模块的时候，Node.js 会解析 require 方法传入的参数，并从文件系统中加载实际的文件。</p>
<p>require方法接受以下几种参数的传递：</p>
<ul>
<li>http、fs、path等，原生模块。</li>
<li>./mod或../mod，相对路径的文件模块。</li>
<li>/pathtomodule/mod，绝对路径的文件模块。</li>
<li>mod，非原生模块的文件模块。</li>
</ul>
<h4 id="exports-和-moduleexports-的使用"><strong>exports 和 module.exports 的使用</strong></h4>
<p>如果要对外暴露属性或方法，就用 <strong>exports</strong> 就行，要暴露对象(类似class，包含了很多属性和方法)，就用 <strong>module.exports</strong>。</p>
<blockquote>
<p>不建议同时使用 exports 和 module.exports。</p>
<p>如果先使用 exports 对外暴露属性或方法，再使用 module.exports 暴露对象，会使得 exports 上暴露的属性或者方法失效。</p>
<p>原因在于，exports 仅仅是 module.exports 的一个引用。</p>
</blockquote>
<p>在 nodejs 中，是这么设计 require 函数的：</p>
<pre><code class="language-js">function require(...){
  var module = {exports: {}};

  ((module, exports) =&gt; {
    function myfn () {}
    // 这个myfn就是我们自己的代码
    exports.myfn = myfn; // 这里是在原本的对象上添加了一个myfn方法。
    module.exports = myfn;// 这个直接把当初的对象进行覆盖。
  })(module,module.exports)
  return module.exports;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[NodeJS初学系列1——回调函数、事件循环、EventEmitter]]></title>
        <id>https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-hui-diao-han-shu-shi-jian-xun-huan-eventemitter/</id>
        <link href="https://giriawsh.github.io/post/nodejs-chu-xue-xi-lie-hui-diao-han-shu-shi-jian-xun-huan-eventemitter/">
        </link>
        <updated>2020-09-28T08:54:55.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>Giria开始踏入Nodejs的坑了，今天学习了回调函数、事件循环、EventEmitter等内容。以下是一些记录。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>Giria开始踏入Nodejs的坑了，今天学习了回调函数、事件循环、EventEmitter等内容。以下是一些记录。</p>
<!-- more -->
<h1 id="回调函数">回调函数</h1>
<h2 id="阻塞代码">阻塞代码</h2>
<p>阻塞代码示例：</p>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

var data = fs.readFileSync('input.txt');

console.log(data.toString());
console.log(&quot;程序执行结束!&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>$ node main.js
菜鸟教程官网地址：www.runoob.com

程序执行结束!
</code></pre>
<h2 id="非阻塞代码">非阻塞代码</h2>
<pre><code class="language-js">var fs = require(&quot;fs&quot;);

fs.readFile('input.txt', function (err, data) {
    if (err) return console.error(err);
    console.log(data.toString());
});

console.log(&quot;程序执行结束!&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>$ node main.js
程序执行结束!
菜鸟教程官网地址：www.runoob.com
</code></pre>
<p>第一个实例在文件读取完后才执行程序。 第二个实例我们不需要等待文件读取完，这样就可以在读取文件时同时执行接下来的代码，大大提高了程序的性能。</p>
<p>因此，阻塞是按顺序执行的，而非阻塞是不需要按顺序的，所以如果需要处理回调函数的参数，我们就需要写在回调函数内。</p>
<h1 id="事件循环">事件循环</h1>
<p>Node.js 单线程类似进入一个while(true)的事件循环，直到没有事件观察者退出，每个异步事件都生成一个事件观察者，如果有事件发生就调用该回调函数.</p>
<h2 id="事件驱动程序">事件驱动程序</h2>
<p>Node.js 使用事件驱动模型，当web server接收到请求，就把它关闭然后进行处理，然后去服务下一个web请求。</p>
<p>当这个请求完成，它被放回处理队列，当到达队列开头，这个结果被返回给用户。</p>
<p>这个模型非常高效可扩展性非常强，因为 webserver 一直接受请求而不等待任何读写操作。（这也称之为非阻塞式IO或者事件驱动IO）</p>
<p>在事件驱动模型中，<strong>会生成一个主循环来监听事件，当检测到事件时触发回调函数</strong>。</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200928154841.jpeg" alt="img" loading="lazy"></figure>
<p>Node.js 有多个内置的事件，我们可以通过引入 events 模块，并通过实例化 EventEmitter 类来绑定和监听事件。</p>
<pre><code class="language-js">// 引入 events 模块
var events = require('events');
// 创建 eventEmitter 对象
var eventEmitter = new events.EventEmitter();
 
// 创建事件处理程序
var connectHandler = function connected() {
   console.log('连接成功。');
  
   // 触发 data_received 事件 
   eventEmitter.emit('data_received');
}
 
// 绑定 connection 事件处理程序
eventEmitter.on('connection', connectHandler);
 
// 使用匿名函数绑定 data_received 事件
eventEmitter.on('data_received', function(){
   console.log('数据接收成功。');
});
 
// 触发 connection 事件 
eventEmitter.emit('connection');
 
console.log(&quot;程序执行完毕。&quot;);
</code></pre>
<p>在 Node 应用程序中，执行异步操作的函数将回调函数作为最后一个参数， 回调函数接收错误对象作为第一个参数。</p>
<h1 id="eventemitter">EventEmitter</h1>
<p>Node.js 所有的异步 I/O 操作在完成时都会发送一个事件到事件队列。</p>
<p>Node.js 里面的许多对象都会分发事件， 所有产生事件的对象都是 events.EventEmitter 的实例。</p>
<h2 id="eventemitter类">EventEmitter类</h2>
<p>events 模块只提供了一个对象： events.EventEmitter。EventEmitter 的核心就是事件触发与事件监听器功能的封装。</p>
<pre><code class="language-js">// 引入 events 模块
var events = require('events');
// 创建 eventEmitter 对象
var eventEmitter = new events.EventEmitter();
</code></pre>
<p>EventEmitter 对象如果在实例化时发生错误，会触发 error 事件。当添加新的监听器时，newListener 事件会触发，当监听器被移除时，removeListener 事件被触发。</p>
<p>也可以这样写：</p>
<pre><code class="language-js">var EventEmitter = require('events').EventEmitter; 
var event = new EventEmitter(); 
</code></pre>
<p>EventEmitter 的每个事件由一个事件名和若干个参数组成，事件名是一个字符串，通常表达一定的语义。对于每个事件，EventEmitter 支持 若干个事件监听器。</p>
<p>当事件触发时，注册到这个事件的事件监听器被依次调用，事件参数作为回调函数参数传递。</p>
<p>多个事件的例子：</p>
<pre><code>//event.js 文件
var events = require('events'); 
var emitter = new events.EventEmitter(); 
emitter.on('someEvent', function(arg1, arg2) { 
    console.log('listener1', arg1, arg2); 
}); 
emitter.on('someEvent', function(arg1, arg2) { 
    console.log('listener2', arg1, arg2); 
}); 
emitter.emit('someEvent', 'arg1 参数', 'arg2 参数'); 
</code></pre>
<p>执行结果：</p>
<pre><code>listener1 arg1 参数 arg2 参数
listener2 arg1 参数 arg2 参数
</code></pre>
<p>可以看到两个事件监听器回调函数被先后调用。 这就是EventEmitter最简单的用法。</p>
<p>一个实例：</p>
<pre><code class="language-js">var events = require('events');
var eventEmitter = new events.EventEmitter();

// 监听器 #1
var listener1 = function listener1() {
   console.log('监听器 listener1 执行。');
}

// 监听器 #2
var listener2 = function listener2() {
  console.log('监听器 listener2 执行。');
}

// 绑定 connection 事件，处理函数为 listener1 
eventEmitter.addListener('connection', listener1);

// 绑定 connection 事件，处理函数为 listener2
eventEmitter.on('connection', listener2);

var eventListeners = eventEmitter.listenerCount('connection');
console.log(eventListeners + &quot; 个监听器监听连接事件。&quot;);

// 处理 connection 事件 
eventEmitter.emit('connection');

// 移除监绑定的 listener1 函数
eventEmitter.removeListener('connection', listener1);
console.log(&quot;listener1 不再受监听。&quot;);

// 触发连接事件
eventEmitter.emit('connection');

eventListeners = eventEmitter.listenerCount('connection');
console.log(eventListeners + &quot; 个监听器监听连接事件。&quot;);

console.log(&quot;程序执行完毕。&quot;);
</code></pre>
<p>执行结果：</p>
<pre><code>2 个监听器监听连接事件。
监听器 listener1 执行。
监听器 listener2 执行。
listener1 不再受监听。
监听器 listener2 执行。
1 个监听器监听连接事件。
程序执行完毕。
</code></pre>
<h2 id="error事件">error事件</h2>
<p>EventEmitter 定义了一个特殊的事件 error，它包含了错误的语义，我们在遇到 异常的时候通常会触发 error 事件。</p>
<p>当 error 被触发时，EventEmitter 规定如果没有响 应的监听器，Node.js 会把它当作异常，退出程序并输出错误信息。</p>
<p>我们一般要为会触发 error 事件的对象设置监听器，避免遇到错误后整个程序崩溃。</p>
<h2 id="继承eventemitter">继承EventEmitter</h2>
<p>大多数时候我们不会直接使用 EventEmitter，而是在对象中继承它。包括 fs、net、 http 在内的，只要是支持事件响应的核心模块都是 EventEmitter 的子类。</p>
<p>原因有两点：</p>
<ul>
<li>
<p>首先，具有某个实体功能的对象实现事件符合语义， 事件的监听和发生应该是一个对象的方法。</p>
</li>
<li>
<p>其次 JavaScript 的对象机制是基于原型的，支持部分多重继承，继承 EventEmitter 不会打乱对象原有的继承关系。</p>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[《JS权威指南》第四章1]]></title>
        <id>https://giriawsh.github.io/post/lesslessjs-quan-wei-zhi-nan-greatergreater-di-si-zhang-1/</id>
        <link href="https://giriawsh.github.io/post/lesslessjs-quan-wei-zhi-nan-greatergreater-di-si-zhang-1/">
        </link>
        <updated>2020-09-27T15:16:47.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>《JS权威指南》的第四章第一部分，包括原始表达式、对象数组表达式、函数定义表达式、属性访问表达式、调用表达式、对象创建表达式等内容。</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>《JS权威指南》的第四章第一部分，包括原始表达式、对象数组表达式、函数定义表达式、属性访问表达式、调用表达式、对象创建表达式等内容。</p>
<!-- more -->
<blockquote>
<p>表达式：JS解释器会将其计算出一个结果</p>
</blockquote>
<ul>
<li><strong>常量</strong>是最简单的一类表达式。<strong>变量名</strong>也是一种简单的表达式，它的值就是赋给变量的值。</li>
<li>复杂表达式是由简单表达式组成的。
<ul>
<li>最常用的方法是使用运算符</li>
</ul>
</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200927010128.png" alt="image-20200927010120328" loading="lazy"></figure>
<p>今天的电脑桌面也很漂亮……顺手存一下</p>
<h2 id="1-原始表达式">1. 原始表达式</h2>
<p><strong>最简单的表达式是原始表达式</strong>。是表达式的最小单位（不再包含其他表达式）</p>
<p>JS中的原始表达式：</p>
<ul>
<li>常量</li>
<li>直接量（直接在程序中出现的常数值，例如字符直接量、正则表达式直接量等）</li>
<li>部分关键字（true false this null等）
<ul>
<li>this并不是一个常量，在一个方法体内this返回调用这个方法的对象。</li>
</ul>
</li>
<li>变量</li>
</ul>
<h2 id="2-对象和数组的初始化表达式">2. 对象和数组的初始化表达式</h2>
<blockquote>
<p>对象直接量、数组直接量：实际上是一个新创建的对象和数组</p>
</blockquote>
<p>他们不是原始表达式，因为它们所包含的成员或者元素都是子表达式（例如a[1]等）</p>
<pre><code class="language-js">var a = [1];
</code></pre>
<p>数组可以嵌套，数组初始化表达式中的元素表达式也都会各自计算一次。</p>
<pre><code>var martrix = [[1,2,3],[4,5,6]];
</code></pre>
<p>也就是说，数组初始化表达式每次计算的值有可能是不同的。</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200927174852.png" alt="image-20200927174845777" loading="lazy"></figure>
<p>数组直接量中的列表逗号之间的元素可以省略，会自动填充undefined，结尾处可以留下单个逗号而不会填充。</p>
<pre><code>var s = {
	&quot;yes&quot;: 0,
	&quot;no&quot;: 1
}
</code></pre>
<p>对象直接量也可以嵌套，也都会各自计算一次并且它们可以是任意JS表达式。</p>
<p>对象直接量中的属性名称可以是字符串而不是标识符。</p>
<h2 id="3-函数定义表达式">3. 函数定义表达式</h2>
<blockquote>
<p>函数直接量：函数定义表达式定义为一个JS函数，表达式的值是这个新定义的函数。</p>
</blockquote>
<pre><code class="language-js">var f = function(x){return x;}
</code></pre>
<p>函数定义表达式同样可以包含函数的名字，也可以通过函数语句来定义。</p>
<h2 id="4-属性访问表达式">4. 属性访问表达式</h2>
<p>属性访问表达式运算得到一个对象属性或一个数组元素的值。拥有两种语法：</p>
<pre><code>expression.identifier
expression[expression]
</code></pre>
<p>第二个表达式指定要访问的属性的名称或者代表要访问数组元素的索引。</p>
<p>计算顺序：</p>
<ol>
<li>在“.”和“[”之前的表达式总会首先计算，如果expression计算的结果是null或undefined，抛出一个类型错误异常</li>
<li>如果运算结果不是对象或者数组，JS会将其转换成对象（参照第三章JS类型转换）</li>
<li>如果跟随句点和标识符（第一种写法），会查找由这个标识符所指定的属性的值，并将其作为整个表达式的值返回</li>
<li>如果跟随方括号，会计算方括号内表达式的值<strong>并将它转换为字符串</strong>。</li>
<li>如果命名的属性不存在，整个属性访问表达式的值是undefined</li>
</ol>
<h3 id="注意">注意</h3>
<p>如果属性名称是一个保留字或者包含空格和标点符号，或是一个数字（对于数组来说），或当属性名是通过运算得出而不是固定的值时，则必须使用方括号的写法。</p>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200927180412.png" alt="image-20200927180412330" loading="lazy"></figure>
<p>PS:亲测浏览器调试台中保留字可以，但是看起来非常不具有语义化</p>
<h2 id="5-调用表达式">5. 调用表达式</h2>
<blockquote>
<p>调用表达式：一种调用（或者执行）函数或方法的语法表示</p>
</blockquote>
<p>以一个函数表达式开始，指代了要调用的函数，跟随一对圆括号，括号内是以逗号隔开的参数列表</p>
<pre><code class="language-js">f(0);
Math.max(x, y, z);
a.sort();
</code></pre>
<p>计算顺序：</p>
<ol>
<li>函数表达式</li>
<li>参数表达式=&gt;得到参数值</li>
<li>如果函数表达式不是一个可调用对象，抛出类型错误异常（所有的函数都是可调用的，即使宿主对象不是函数它也有可能被调用）</li>
<li>实参的值依次赋值给形参</li>
<li>执行函数体</li>
<li>返回（若无return 返回undefined）</li>
</ol>
<blockquote>
<ul>
<li>宿主对象就是执行JS脚本的环境提供的对象。对于嵌入到网页中的JS来说，其宿主对象就是浏览器提供的对象，所以又称为浏览器对象，如IE、Firefox等浏览器提供的对象。不同的浏览器提供的宿主对象可能不同，即使提供的对象相同，其实现方式也大相径庭！这会带来浏览器兼容问题，增加开发难度。</li>
<li>浏览器对象有很多，如Window和Document等等。</li>
</ul>
</blockquote>
<h4 id="方法调用翻译垃圾还得重翻原版">方法调用（翻译垃圾，还得重翻原版）</h4>
<blockquote>
<p>方法调用：如果表达式是属性访问表达式（例如a.f(x)），那么这个调用被称为方法调用</p>
</blockquote>
<p>在方法调用中，作为属性访问主体的对象和数组，便是其调用方法内this的指向（看下面的例子就明白了）</p>
<pre><code class="language-js">Object.prototype.callThis = function(){
    return this;
}
var b = [1, 2, 3];
var c = {x:1};
var d = 1;
console.log(b.callThis());//=&gt; [1, 2, 3]
console.log(c.callThis());//=&gt; {x: 1}
</code></pre>
<p>this的指向便是调用者本身。</p>
<p>这种特性使得在面向对象编程范例中，函数可以调用其宿主对象。</p>
<p>来看后面这个重点的话：</p>
<figure data-type="image" tabindex="4"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200927230259.png" alt="image-20200927230259821" loading="lazy"></figure>
<p>那些不是方法调用的调用表达式，通常使用全局对象作为this的指向值。</p>
<figure data-type="image" tabindex="5"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200927230532.png" alt="image-20200927230532043" loading="lazy"></figure>
<p>在ES5的严格模式下，函数在调用时将使用undefined作为this的值。</p>
<p>总结来说，this的指向在本篇中提到了三种情况（当然实际上会比这要复杂）</p>
<ul>
<li>方法调用，this指向调用的主体</li>
<li>不是方法调用，通常使用全局对象作为this的指向值</li>
<li>ES5的严格模式下，使用undefined作为this的值</li>
</ul>
<h2 id="6-对象创建表达式">6. 对象创建表达式</h2>
<blockquote>
<p>对象创建表达式：创建一个对象并使用一个函数（构造函数）初始化新对象的属性</p>
</blockquote>
<pre><code class="language-js">new Object() // == new Object
new Point(2, 3);
</code></pre>
<p>如果一个对象创建表达式不需要传入任何参数给构造函数，空圆括号可以省略。</p>
<p>计算对象创建表达式：</p>
<ol>
<li>JS创建一个新的空对象</li>
<li>JS通过传入指定的参数，并<strong>将新参数当做this的值来调用一个指定的函数</strong></li>
<li><strong>函数可以使用this来初始化这个新创建对象的属性</strong></li>
<li>被当成构造函数的函数不会返回一个值，并且这个新创建并被初始化后的对象就是整个对象创建表达式的值（总之就是返回个对象）</li>
<li>如果一个构造函数确实<strong>返回了一个对象</strong>，那么这个对象就作为整个对象创建表达式的值，新创建的对象被废弃。</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[element源码学习-Layout组件]]></title>
        <id>https://giriawsh.github.io/post/element-yuan-ma-xue-xi-layout-zu-jian/</id>
        <link href="https://giriawsh.github.io/post/element-yuan-ma-xue-xi-layout-zu-jian/">
        </link>
        <updated>2020-09-24T15:58:41.000Z</updated>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>Layout布局尝试：</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200924002254.png" alt="image-20200924002254419" loading="lazy"></figure>
<p>大致由两个重要组件构成，el-row与el-col</p>
<h2 id="row函数">row函数</h2>
<p><code>row</code>用来作为<code>col</code>的容器，通过render函数创建，上面有一些动态的<code>class</code>、<code>style</code>，以及一个<code>slot</code>用来放置<code>col</code>，相对较为简单。</p>
<h3 id="render函数">*render函数</h3>
<p><a href="https://cn.vuejs.org/v2/guide/render-function.html">参考官方手册</a></p>
<p>实际上提供了js操作渲染组件的能力（类似于React的思想），给予了组件更高的灵活性。使用JS可以完全代替模板功能（用 JavaScript 的 <code>if</code>/<code>else</code> 和 <code>map</code> 来重写），以下两段代码等价：</p>
<pre><code class="language-html">&lt;h1&gt;{{ blogTitle }}&lt;/h1&gt;
</code></pre>
<pre><code class="language-js">render: function (createElement) {
  return createElement('h1', this.blogTitle)
}
</code></pre>
<p>在这两种情况下，Vue 都会自动保持页面的更新，即便 <code>blogTitle</code> 发生了改变。</p>
<p>Vue 的模板实际上被编译成了渲染函数。</p>
<h3 id="indexjs">index.js</h3>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200924002451.png" alt="image-20200924002451523" loading="lazy"></figure>
<p>这一块是按常规的组件注册和封装。</p>
<p>打开<code>index.js</code>,这里最后一句导出<code>Row</code>供我们<code>import</code>。</p>
<p>中间的<code>install</code>方法则是把这个组件当成一个Vue的插件来使用，通过<code>Vue.use()</code>来使用该组件,install方法传递一个Vue的构造器，Element的所有组件都是一个对象{...}。</p>
<p>之后会看到里面有个<code>render</code>函数来创建组件的html结构，<code>render</code>方法的好处很大，使得创建html模板的代码更加简洁高效，而不是冗长的各种div标签堆叠，更类似于一种配置形式来创建html。</p>
<p>最后通过<code>export default</code>导出,而不是常用的单文件组件形式，因此必须提供install方法。</p>
<p>之前讲到过<code>/* istanbul ignore next */</code>，这个是一个npm代码覆盖率的工具istanbul，在写测试的时候，有些不必要的文件，在计算覆盖率时会被计算到，所以加上这句注释就可以忽略下面的代码。</p>
<h3 id="rowjs">row.js</h3>
<pre><code class="language-js">  render(h) {
    return h(this.tag, {
      class: [
        'el-row',
        this.justify !== 'start' ? `is-justify-${this.justify}` : '',
        this.align !== 'top' ? `is-align-${this.align}` : '',
        { 'el-row--flex': this.type === 'flex' }
      ],
      style: this.style
    }, this.$slots.default);
  }
</code></pre>
<p>它的style是一个计算属性，会根据<code>gutter</code>这一<code>prop</code>来改变，栏目之间的间隔，用来抵消<code>col</code>造成的两边的<code>padding</code>。</p>
<pre><code class="language-js">  computed: {
    style() {
      const ret = {};

      if (this.gutter) {
        ret.marginLeft = `-${this.gutter / 2}px`;
        ret.marginRight = ret.marginLeft;
      }

      return ret;
    }
  },
</code></pre>
<p>这里margin为负值的详细解释可看：<a href="https://juejin.im/post/6844903657431891975">element-layout分析</a></p>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200924233307.webp" alt="img" loading="lazy"></figure>
<p>大概就是为了消除图中最左侧和最右侧的间隔。</p>
<p><code>class</code>中的justify/align/type属性都是props，由用户选择性提供</p>
<pre><code class="language-js">    type: String,//是否采用flex布局
    justify: {//flex 布局下的水平排列方式
      type: String,
      default: 'start'
    },
    align: {// flex 布局下的垂直排列方式
      type: String,
      default: 'top'
    }
</code></pre>
<h2 id="col函数">col函数</h2>
<h3 id="coljs">col.js</h3>
<p>首先来看gutter这一style，与row中类似，是通过一个计算属性进行调整。</p>
<pre><code class="language-js">    if (this.gutter) {
      style.paddingLeft = this.gutter / 2 + 'px';
      style.paddingRight = style.paddingLeft;
    }
</code></pre>
<p>它的计算过程，涉及父实例。如果父实例不是el-row而是直接使用了el-col将以自身为准返回0，否则返回父组件已有的gutter，然后在此基础上增加padding。</p>
<blockquote>
<p>一般的想法是col之间用margin来间隔，其实是不行的，而用padding来间隔就很简单，width按百分比来分配就行(box-sizing要设置为border-box)</p>
</blockquote>
<pre><code class="language-js">  computed: {
    gutter() {
      let parent = this.$parent;
      while (parent &amp;&amp; parent.$options.componentName !== 'ElRow') {//这里的componentName在row.js中有定义
        parent = parent.$parent;//如果没有被el-row包裹，继续向上查找有无el-row，直到找到el-row父组件为止。
      }
      return parent ? parent.gutter : 0;
    }
  },
</code></pre>
<h4 id="栅格系统">*栅格系统</h4>
<p>这里最完备也是最早最具有启发性的是<a href="https://v3.bootcss.com/css/">boostrap提出的栅格系统</a>。</p>
<p>栅格系统用于通过一系列的行（row）与列（column）的组合来创建页面布局，你的内容就可以放入这些创建好的布局中。</p>
<blockquote>
<ul>
<li>“行（row）”必须包含在 <code>.container</code> （固定宽度）或 <code>.container-fluid</code> （100% 宽度）中，以便为其赋予合适的排列（aligment）和内补（padding）。</li>
<li>通过“行（row）”在水平方向创建一组“列（column）”。</li>
<li>你的内容应当放置于“列（column）”内，并且，只有“列（column）”可以作为行（row）”的直接子元素。</li>
<li>类似 <code>.row</code> 和 <code>.col-xs-4</code> 这种预定义的类，可以用来快速创建栅格布局。Bootstrap 源码中定义的 mixin 也可以用来创建语义化的布局。</li>
<li>通过为“列（column）”设置 <code>padding</code> 属性，从而创建列与列之间的间隔（gutter）。通过为 <code>.row</code> 元素设置负值 <code>margin</code> 从而抵消掉为 <code>.container</code> 元素设置的 <code>padding</code>，也就间接为“行（row）”所包含的“列（column）”抵消掉了<code>padding</code>。</li>
<li>负值的 margin就是下面的示例为什么是向外突出的原因。在栅格列中的内容排成一行。</li>
<li>栅格系统中的列是通过指定1到12的值来表示其跨越的范围。例如，三个等宽的列可以使用三个 <code>.col-xs-4</code> 来创建。</li>
<li>如果一“行（row）”中包含了的“列（column）”大于 12，多余的“列（column）”所在的元素将被作为一个整体另起一行排列。</li>
<li>栅格类适用于与屏幕宽度大于或等于分界点大小的设备 ， 并且针对小屏幕设备覆盖栅格类。 因此，在元素上应用任何 <code>.col-md-*</code> 栅格类适用于与屏幕宽度大于或等于分界点大小的设备 ， 并且针对小屏幕设备覆盖栅格类。 因此，在元素上应用任何 <code>.col-lg-*</code> 不存在， 也影响大屏幕设备。</li>
</ul>
</blockquote>
<figure data-type="image" tabindex="4"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200924231845.png" alt="image-20200924231838614" loading="lazy"></figure>
<h4 id="render函数部分">render函数部分</h4>
<p>看下官网中属性的定义和效果</p>
<figure data-type="image" tabindex="5"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200924233832.png" alt="image-20200924233832568" loading="lazy"></figure>
<p>再来看源代码</p>
<pre><code class="language-js">render(h) {
    let classList = [];
    let style = {};

    if (this.gutter) {
      style.paddingLeft = this.gutter / 2 + 'px';
      style.paddingRight = style.paddingLeft;
    }
// span 栅格占据的列数，通过 width 来实现
  // offset 栅格左侧的间隔格数，通过 margin-left 实现
  // push 栅格向右移动格数，通过 left 实现
  // pull 栅格向左移动格数，通过 right 实现
    ['span', 'offset', 'pull', 'push'].forEach(prop =&gt; {
      if (this[prop] || this[prop] === 0) {
        classList.push(
          prop !== 'span'
            ? `el-col-${prop}-${this[prop]}`
            : `el-col-${this[prop]}`
        );
      }
    });
 // 不同屏幕大小下的适配，传入数字的话只会影响 span，还可以通过传入对象进行更多的控制
  // xs &lt;768px 响应式栅格数或者栅格属性对象
  // sm ≥768px 响应式栅格数或者栅格属性对象
  // md ≥992px 响应式栅格数或者栅格属性对象
  // lg ≥1200px 响应式栅格数或者栅格属性对象
    ['xs', 'sm', 'md', 'lg', 'xl'].forEach(size =&gt; {
      if (typeof this[size] === 'number') {
        classList.push(`el-col-${size}-${this[size]}`);
      } else if (typeof this[size] === 'object') {
        let props = this[size];
        Object.keys(props).forEach(prop =&gt; {
          classList.push(
            prop !== 'span'
              ? `el-col-${size}-${prop}-${props[prop]}`
              : `el-col-${size}-${props[prop]}`
          );
        });
      }
    });

    return h(this.tag, {
      class: ['el-col', classList],
      style
    }, this.$slots.default);
  }
</code></pre>
<p>下面解释下<code>['span', 'offset', 'pull', 'push']</code>这几个的作用，span很好理解，占父容器的列数，对应scss代码如下：</p>
<pre><code class="language-js">[class*=&quot;el-col-&quot;] {
  float: left;
  box-sizing: border-box;
}

.el-col-0 {
  display: none;
}

@for $i from 0 through 24 {
    //el-col-数字类型的类的宽度就是百分比
  .el-col-#{$i} {
    width: (1 / 24 * $i * 100) * 1%;
  }

  .el-col-offset-#{$i} {
    margin-left: (1 / 24 * $i * 100) * 1%;
  }

  .el-col-pull-#{$i} {
    position: relative;
    right: (1 / 24 * $i * 100) * 1%;
  }

  .el-col-push-#{$i} {
    position: relative;
    left: (1 / 24 * $i * 100) * 1%;
  }
}

</code></pre>
<ul>
<li>
<p>[attribute*=value] 选择器，它选择了所有类名以<code>el-col-</code>开头的类.</p>
</li>
<li>
<p><code>offset</code>实际上是<code>margin-left</code>(很好理解，相对左移就是偏移了),这可能会导致一行排列不下所有的col，会导致换行出现</p>
</li>
<li>
<p><code>el-col-pull</code>则不同，仅仅只是相对原来的位置移动，不会造成挤下去换行的情况，而会造成不同col互相覆盖</p>
</li>
</ul>
<p>js部分大量使用模板字符串而不是字符串拼接，达到简化代码的目的。</p>
<h2 id="参考网站">参考网站</h2>
<p><a href="https://www.jianshu.com/p/52ae9ad78f80">Element分析（组件篇）——Layout</a></p>
<p><a href="https://juejin.im/post/6844903657431891975">Element源码分析系列1一Layout(布局)</a></p>
<p><a href="https://v3.bootcss.com/css/#grid-options">Bootstrap 栅格参数</a></p>
<p><a href="https://element.eleme.io/#/zh-CN/component/layout">Element.io - layout</a></p>
<p><a href="https://learnku.com/articles/21215">图解bootstrap栅格系统</a>  --&gt; 这一篇讲得太清楚了。。忍不住点赞</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[vue-cli中使用vue-router]]></title>
        <id>https://giriawsh.github.io/post/vue-cli-zhong-shi-yong-vue-router/</id>
        <link href="https://giriawsh.github.io/post/vue-cli-zhong-shi-yong-vue-router/">
        </link>
        <updated>2020-09-23T13:47:14.000Z</updated>
        <content type="html"><![CDATA[<h2 id="前言">前言</h2>
<p>最近开始从Nuxt跳回基础的vue-cli，进行重新学习。集成性框架的便捷性可以使项目快速搭建，但当下对底层的东西也更需要积累才是。算是日常对vue学习进度的总结跟进。</p>
<p>vue-cli是官方所推的一个脚手架，它的优越性在于图形化、便捷性，更鼓励像插槽一样添加自己所需要的组件，这带来了更好的延展性。</p>
<p>本文记录了如何在vue-cli中使用vue-router。</p>
<h2 id="步骤记录">步骤记录</h2>
<p>前置条件：已经使用创建命令搭建好基础框架</p>
<pre><code>vue create vue-cli-test
</code></pre>
<h3 id="1-添加vue-router">1. 添加vue-router</h3>
<p>参照<a href="https://router.vuejs.org/installation.html#npm">官方文档</a>，使用add加入router插件（顺便说只有英文版才有vue-cli的专门说明，害）</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200923214325.png" alt="image-20200923214318588" loading="lazy"></figure>
<p>安装前会提醒你记得commit，因为它将会覆盖掉（如果没有将会新生成）app.vue和router/index.js文件。</p>
<h3 id="2-vue-router的使用">2. vue-router的使用</h3>
<p><img src="https://p6-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/b3cb282a329643caa8137c4a5779909f~tplv-k3u1fbpfcp-zoom-1.image" alt="" loading="lazy"><br>
安装完成，将会自动生成Home和About两个page，同时更新App.vue，并且在router目录下生成index.js文件。默认运行效果如下：</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200923214451.png" alt="image-20200923214451577" loading="lazy"></figure>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200923214458.png" alt="image-20200923214458324" loading="lazy"></figure>
<p>如果要新增，可以参照官方默认方式，通过创建新的view，插入新的组件。</p>
<pre><code class="language-js">//App.vue
&lt;template&gt;
  &lt;div id=&quot;nav&quot;&gt;
    &lt;router-link to=&quot;/&quot;&gt;Home&lt;/router-link&gt; |
    &lt;router-link to=&quot;/about&quot;&gt;About&lt;/router-link&gt; |
    &lt;router-link to=&quot;/test&quot;&gt;Test&lt;/router-link&gt;
  &lt;/div&gt;
  &lt;router-view/&gt;
&lt;/template&gt;
</code></pre>
<pre><code class="language-js">//Test.vue
&lt;template&gt;
    &lt;div class=&quot;about&quot;&gt;
        &lt;h1&gt;This is an test page&lt;/h1&gt;
    &lt;/div&gt;
&lt;/template&gt;
</code></pre>
<pre><code class="language-js">//index.js
import { createRouter, createWebHistory } from 'vue-router'
import Home from '../views/Home.vue'

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home
  },
  {
    path: '/about',
    name: 'About',
    // route level code-splitting
    // this generates a separate chunk (about.[hash].js) for this route
    // which is lazy-loaded when the route is visited.
    component: () =&gt; import(/* webpackChunkName: &quot;about&quot; */ '../views/About.vue')
  },
  {
    path: '/test',
    name: 'Test',
    component: () =&gt; import(/* webpackChunkName: &quot;about&quot; */ '../views/Test.vue')
  },

]

const router = createRouter({
  history: createWebHistory(process.env.BASE_URL),
  routes
})

export default router
</code></pre>
<p>即可实现简单的页面路由效果。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[《JS权威指南》第三章3]]></title>
        <id>https://giriawsh.github.io/post/lesslessjs-quan-wei-zhi-nan-greatergreater-di-san-zhang-3-bian-liang-sheng-ming-bian-liang-zuo-yong-yu/</id>
        <link href="https://giriawsh.github.io/post/lesslessjs-quan-wei-zhi-nan-greatergreater-di-san-zhang-3-bian-liang-sheng-ming-bian-liang-zuo-yong-yu/">
        </link>
        <updated>2020-09-22T17:19:50.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="️前言">✍️前言</h2>
<p>《JS权威指南》学习笔记第三章第三部分，涉及变量声明、变量作用域、作用域链等内容</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="️前言">✍️前言</h2>
<p>《JS权威指南》学习笔记第三章第三部分，涉及变量声明、变量作用域、作用域链等内容</p>
<!-- more -->
<h2 id="1-变量声明">1. 变量声明</h2>
<p>在JS中，使用一个变量之前应用var来声明，也可以将初始赋值和变量声明合写一起。</p>
<pre><code>var i = 0, j = 0;
</code></pre>
<p>如果未指定初始值，它的初始值为undefined</p>
<p>JS变量可以是任意数据类型（轻量级的优点基础）</p>
<h3 id="重复声明和遗漏声明">重复声明和遗漏声明</h3>
<ul>
<li>
<p>使用var语句重复声明变量是合法且无害的。</p>
</li>
<li>
<p>如果试图读取一个没有声明的变量的值，JS会报错。</p>
</li>
<li>
<p>ES5严格模式下，给一个没有声明的变量赋值也会报错</p>
</li>
<li>
<p>非严格模式下，给一个没有声明的变量赋值，实际上会给全局对象创建一个同名属性，使它工作起来像一个正确声明的全局变量（后文会看到区别）</p>
</li>
</ul>
<p>总之别有什么坏习惯，该声明声明。</p>
<h2 id="2-变量作用域">2. 变量作用域</h2>
<blockquote>
<p>一个变量的作用域：程序源代码中定义这个变量的区域</p>
</blockquote>
<p>全局变量拥有全局作用域，函数内声明的变量只在函数体内有定义（局部变量，作用域是局部性的），函数参数也是局部变量。</p>
<p>在函数体内，<strong>局部变量的优先级高于同名的全局变量</strong>。如果同名，全局变量被局部变量所覆盖</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922222448.png" alt="image-20200922222440903" loading="lazy"></figure>
<p>尽管在全局作用域编写代码时可以不写var语句，但生命局部变量时必须使用var语句（不然可能带有泄漏局部变量的风险）</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922223529.png" alt="image-20200922223529787" loading="lazy"></figure>
<p>可以看到如果没有在函数作用域内用var重新声明，内部的局部变量值泄漏到了全局。</p>
<p>函数定义也可以嵌套，每个函数都有自己的作用域。</p>
<h3 id="21-函数作用域和声明提前">2.1 函数作用域和声明提前</h3>
<blockquote>
<p>块级作用域：在类似C语言的编程语言中，花括号内的每一段代码都具有各自的作用域，并且变量在声明它们的代码段之外是不可见的</p>
</blockquote>
<p>JS中没有块级作用域，取而代之地使用了函数作用域。</p>
<blockquote>
<p>函数作用域：变量在声明它们的函数体以及这个函数体嵌套的任意函数体内都是有定义的。</p>
</blockquote>
<p>在函数内声明的所有变量在函数体内始终是可见的。</p>
<p>这意味着，变量在声明之前甚至已经可用。JS的这个特性被非正式地成为<strong>声明提前</strong>(代码运行开始之前完成，预编译阶段)</p>
<blockquote>
<p>声明提前：JS函数里声明的所有变量（但不涉及赋值）都被“提前”至函数体的顶部</p>
</blockquote>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922230319.png" alt="image-20200922230319362" loading="lazy"></figure>
<figure data-type="image" tabindex="4"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922230232.png" alt="image-20200922230232712" loading="lazy"></figure>
<p>一开始觉得有点惊讶，实际试验以后发现的却是这样。如果函数体内重新声明了变量，它的声明会被提前，赋值不会。</p>
<p>由于函数作用域的特性，局部变量在整个函数体始终是有定义的。但是只有执行到var语句的时候，局部变量才会被真正赋值。它的等价表现如下（声明提前，赋值保留原始位置）：</p>
<figure data-type="image" tabindex="5"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922230514.png" alt="image-20200922230514238" loading="lazy"></figure>
<p>在JS中没有块级作用域，一些程序员特意将变量声明放在函数体顶部，可以使得源代码更加清晰地反映真实的变量作用域。</p>
<h3 id="22-作为属性的变量">2.2 作为属性的变量</h3>
<p>之前我们提到过全局对象，当声明一个JS全局变量时，实际上是定义了全局对象的一个属性。</p>
<p>如果使用了var声明变量，创建的这个属性时不可配置的，无法通过delete运算符删除。</p>
<p>如果在非严格模式下，并给未声明的变量赋值，JS会自动创建一个全局变量，这种方式创建的全局变量时可配置属性，并且可以通过delete删除。</p>
<figure data-type="image" tabindex="6"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200922230803.png" alt="image-20200922230803723" loading="lazy"></figure>
<p>但是对于局部变量，没有这个规定。局部变量当做跟函数调用相关的某个对象的属性。</p>
<p>ES5中称为“<strong>声明上下文对象</strong>”。</p>
<p>JS允许使用this来引用全局对象，this.xxx引用全局对象的某个属性（或者说某个全局变量），却没有方法可以引用局部变量中存放的对象。</p>
<p>这种存放局部变量的对象的特有性质，是一种我们不可见的内部实现。</p>
<h3 id="23-作用域链重点">2.3 作用域链（重点）</h3>
<p>根据上面可以知道，全局变量在程序中始终都是有定义的。局部变量在声明它的函数体内以及其所嵌套的函数内始终是有定义的。</p>
<p>根据2.2的性质，我们类似地可以把一个局部变量看做是自定义实现的对象的属性（就像全局变量是全局对象的属性一样）。</p>
<p>每一段JS代码都有一个与之关联的作用域链。这个作用域链是一个对象列表或者链表，这组对象定义了这段代码“作用域”中的变量。</p>
<blockquote>
<p>变量解析：JS查找变量x的过程</p>
</blockquote>
<p>进行变量解析时，会从链中的第一个对象开始查找。如果有x属性，就会直接使用这个。如果不存在，会继续查找链上的下一个对象（相当于从函数内部往外找）。如果没有任何一个对象含有x属性，那么认为这段代码作用域链上不存在x，抛出引用错误异常。</p>
<ul>
<li>在最顶层代码中，作用域链由一个全局对象组成（很好理解，所有变量都是全局对象的属性）。</li>
<li>在不包含嵌套的函数体内，作用域链上有两个对象
<ul>
<li>第一个是定义函数参数和局部变量的对象</li>
<li>第二个是全局对象（也很好理解，如果要找x，先找函数体内再找外面的全局对象内）</li>
</ul>
</li>
<li>在一个嵌套的函数体内，作用域上至少有三个对象</li>
</ul>
<p>这里可以参考：<a href="https://juejin.im/post/6844903797135769614">深入理解JavaScript作用域和作用域链</a></p>
<p><strong>对象链的创建规则：</strong></p>
<p>当定义一个函数时，它实际上保存一个作用域链。当调用这个函数时，它创建一个新的对象来存储它的局部变量，并将这个对象添加至保存的那个作用域链上，同时创建一个新的更长的表示函数调用作用域的“链”。</p>
<p>对于嵌套函数来说，<strong>每次调用外部函数时，内部函数又会重新定义一遍</strong>。因为<strong>每次调用外部函数的时候作用域链都是不同的</strong>，内部函数在每次定义的时候都会有微妙的差别。</p>
<p>总结来说：<strong>在每次调用外部函数时，内部函数的代码都是相同的，但是关联这段代码的作用域链将会是不相同的，因此内部函数在每次定义时都会有微妙的不同。</strong></p>
<p>PS：不得不吐槽下中文书翻译，这里建议看原版。</p>
<figure data-type="image" tabindex="7"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200923011322.png" alt="image-20200923011322623" loading="lazy"></figure>
<figure data-type="image" tabindex="8"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200923011414.png" alt="image-20200923011414788" loading="lazy"></figure>
<p>我吐啦这翻译，我来回看了好几遍……</p>
]]></content>
    </entry>
</feed>