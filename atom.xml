<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://giriawsh.github.io/</id>
    <title>Giria&apos;s Blog</title>
    <updated>2020-09-11T04:43:17.346Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://giriawsh.github.io/"/>
    <link rel="self" href="https://giriawsh.github.io/atom.xml"/>
    <subtitle>Live only once, make it count</subtitle>
    <logo>https://giriawsh.github.io/images/avatar.png</logo>
    <icon>https://giriawsh.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Giria&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[《剑指Offer》JS题解——JZ11~15]]></title>
        <id>https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz11~15/</id>
        <link href="https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz11~15/">
        </link>
        <updated>2020-09-11T04:42:05.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>又是每日一刷，既然没人看那我就开启自嗨式写法。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">前言</h1>
<p>又是每日一刷，既然没人看那我就开启自嗨式写法。</p>
<!-- more -->
<h1 id="jz11-二进制中1的个数">JZ11 二进制中1的个数</h1>
<h2 id="题目">题目</h2>
<p>输入一个整数，输出该数32位二进制表示中1的个数。其中负数用补码表示。</p>
<h2 id="解法">解法</h2>
<p>顺便复习下如何用js十进制转二进制：</p>
<pre><code>parseInt(n).toString(2)
</code></pre>
<p>这题一开始也挺蒙的，后面看了高人指点，能够领略一二，有种玩数竞的感觉。</p>
<p>1100 减去1后 -》1011.</p>
<p>可以发现减1的结果是把最右边的一个1开始的所有位都取反了。</p>
<p>这个时候如果我们再把原来的整数和减去1之后的结果做与运算：1100&amp;1011=1000.</p>
<p>也就是说，把一个整数减去1，再和原整数做与运算，<strong>会把该整数最右边一个1变成0</strong>.</p>
<p><strong>那么一个整数的二进制有多少个1，就可以进行多少次这样的操作</strong>。</p>
<pre><code>function NumberOf1(n)
{
    // write code here
    var count = 0;
    while(n)
    {
        n = n &amp; (n-1);
        count++;
    }
    return count;
    
}
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/7/24/1737cc0d717c452e?w=172&amp;h=59&amp;f=png&amp;s=1953" alt="xiaolv" loading="lazy"></figure>
<h1 id="jz12-数值的整数次方">JZ12 数值的整数次方</h1>
<h2 id="题目描述">题目描述</h2>
<p>给定一个double类型的浮点数base和int类型的整数exponent。求base的exponent次方。</p>
<p>保证base和exponent不同时为0</p>
<h2 id="答案">答案</h2>
<h3 id="解法一">解法一</h3>
<p>pow函数的简单实现</p>
<pre><code class="language-js">  /**
   * @return {number}
   */
  function Power(base, exponent) {
      // write code here
      if (exponent === 0) {
          return 1;
      } else if (exponent === 1) {
          return base;
      }
      let ret = base;
      if (exponent &gt; 0) {
          for (let i = 1; i &lt; exponent; i++) {
              ret *= base;
          }
          return ret;
      } else {
          for (let i = 1; i &lt; Math.abs(exponent); i++) {
              ret *= base;
          }
          return 1 / ret;
      }
  }
</code></pre>
<h3 id="解法二">解法二</h3>
<p>应用ES6最新运算符 **</p>
<pre><code>function Power(base, exponent)
{
    // write code here
    return base**exponent;
}
</code></pre>
<h1 id="jz13-调整数组顺序使奇数在偶数前面">JZ13 调整数组顺序使奇数在偶数前面</h1>
<h2 id="题目描述-2">题目描述</h2>
<p>输入一个整数数组，实现一个函数来调整该数组中数字的顺序，使得所有的奇数位于数组的前半部分，所有的偶数位于数组的后半部分，并保证奇数和奇数，偶数和偶数之间的相对位置不变。</p>
<h2 id="解法一-2">解法一</h2>
<p>开两个数组分别记录</p>
<pre><code>function reOrderArray(array)
{
    let oddArray = [];
        let evenArray = [];
        for(let i = 0; i &lt; array.length; i++)
        {
            if(array[i]%2 === 0){
                evenArray.push(array[i]);
            }else{
                oddArray.push(array[i]);
            }
        }
        return oddArray.concat(evenArray);
}
</code></pre>
<h2 id="解法二-2">解法二</h2>
<p>相当于一个特殊条件判断的插入排序.是奇数的话把他插入到前面，其余数字后移</p>
<pre><code>function reOrderArray2(array) {
    let k = 0;
    for(let i = 0; i &lt; array.length; i++)
    {
        let tmp = array[i];
        if(tmp &amp; 1){//是奇数
            for(let j = i - 1; j &gt;= k; j--)//到已经排序好的为止
            {
                array[j+1] = array[j];
            }
            array[k++] = tmp;
        }
    }
    return array;
}
console.log(reOrderArray2([1,2,3,4,5,6]))
</code></pre>
<h1 id="jz14-链表中倒数第k个节点">JZ14 链表中倒数第k个节点</h1>
<h2 id="题目描述-3">题目描述</h2>
<p>输入一个链表，输出该链表中倒数第k个结点。</p>
<h2 id="解法-2">解法</h2>
<p>快慢指针，相距k个，那么快指针到末尾时，慢指针的位置便是倒数k个</p>
<pre><code>function FindKthToTail(head, k) {
    if (!head || k &lt; 1) return null;
    let slowptr = head;
    let fastptr = head;
    let count = 0;
    while (fastptr != null &amp;&amp; count &lt; k) {
        count++;
        fastptr = fastptr.next;
    }
    if (count &lt; k) return null;//k的取值超过了链表本身的长度
    while (fastptr != null) {
        fastptr = fastptr.next;
        slowptr = slowptr.next;
    }
    return slowptr;
}
</code></pre>
<h1 id="jz15-反转链表">JZ15 反转链表</h1>
<h2 id="题目描述-4">题目描述</h2>
<p>输入一个链表，反转链表后，输出新链表的表头。</p>
<h2 id="解答">解答</h2>
<p>基本思路，保存当前指针.next.next，再将当前指针.next指向当前指针，然后前移</p>
<pre><code>function ReverseList(pHead) {
    // write code here
    if (!pHead) return null;
    if (!pHead.next) return pHead;
    let p1 = pHead;
    let p2 = pHead.next;
    p1.next = null;
    let temp;
    while (p2 != null) {
        temp = p2.next;
        p2.next = p1;
        p1 = p2;
        p2 = temp;
    }
    return p1;
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[《剑指Offer》JS题解——JZ6~10]]></title>
        <id>https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz6~10/</id>
        <link href="https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz6~10/">
        </link>
        <updated>2020-09-11T04:41:06.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="️前言">✍️前言</h1>
<p>话不多说，肝就完事。不过感觉在掘金上写算法没啥人看……也有可能是我太菜了吧。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="️前言">✍️前言</h1>
<p>话不多说，肝就完事。不过感觉在掘金上写算法没啥人看……也有可能是我太菜了吧。</p>
<!-- more -->
<h1 id="jz6-旋转数组的最小数字">JZ6 旋转数组的最小数字</h1>
<h2 id="题目">题目</h2>
<p>把一个数组最开始的若干个元素搬到数组的末尾，我们称之为数组的旋转。<br>
输入一个非递减排序的数组的一个旋转，输出旋转数组的最小元素。<br>
例如数组{3,4,5,1,2}为{1,2,3,4,5}的一个旋转，该数组的最小值为1。<br>
NOTE：给出的所有元素都大于0，若数组大小为0，请返回0。</p>
<h2 id="解法一">解法一</h2>
<p>恕我直言，好像没什么意思啊，第一直觉，O(n)扫一遍</p>
<pre><code>while(line=readline()){
    //将json字符串转换为JavaScript值或对象
    let arr = JSON.parse(line);
    print(minNumberInRotateArray(arr));
}
function minNumberInRotateArray(rotateArray)
{
    // write code here
    var len = rotateArray.length;
    if(len == 0)
        return 0;
    var min = rotateArray[0];
    for(var i = 0; i &lt; len; i++)
    {
        if(min &gt; rotateArray[i]){
            min = rotateArray[i];
        }
    }
    return min;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376ccec4afc05d?w=160&amp;h=76&amp;f=png&amp;s=2029" alt="效率" loading="lazy"><br>
js搜索有更简单的办法：</p>
<pre><code>function minNumberInRotateArray(rotateArray)
{
    return Math.min(...rotateArray)
}
</code></pre>
<h2 id="解法二">解法二</h2>
<p>仔细观察下题目，会发现有着某种设定，输入的总是经过一次旋转的数组，也就是说会形成从小到大然后一个跌越再从小到大这样的规律。而我们要找的便是中间那个跌越点，就是整个数组的最小值。</p>
<p>也就是说实际上最小的元素就是两个子数组的分界线。</p>
<p>既然两个子数组是有序的，可以用<strong>二分查找</strong>来找。</p>
<ol>
<li>我们用两个指针left,right分别指向数组的第一个元素和最后一个元素。按照题目的旋转的规则，第一个元素应该是大于最后一个元素的</li>
<li>找到数组的中间元素。</li>
</ol>
<pre><code>(1)array[mid] &gt; array[high]:
出现这种情况的array类似[3,4,5,6,0,1,2]，此时最小数字一定在mid的右边。
low = mid + 1
(2)array[mid] == array[high]:
出现这种情况的array类似 [1,0,1,1,1] 或者[1,1,1,0,1]，此时最小数字不好判断在mid左边
还是右边,这时只好一个一个试 ，把范围缩小一步
high = high - 1
(3)array[mid] &lt; array[high]:
出现这种情况的array类似[2,2,3,4,5,6,6],此时最小数字一定就是array[mid]或者在mid的左
边。因为右边必然都是递增的。
high = mid
</code></pre>
<p>代码如下：</p>
<pre><code>while(line=readline()){
    //将json字符串转换为JavaScript值或对象
    let arr = JSON.parse(line);
    print(minNumberInRotateArray(arr));
}
function minNumberInRotateArray(rotateArray)
{
    // write code here
    var len = rotateArray.length;
    if(len == 0)    return 0;
    var left = 0;
    var right = len - 1;
    while(left &lt; right)
    {
        var mid = parseInt((left + right) / 2);
        if(rotateArray[mid] &gt; rotateArray[right])
        {
            left = mid + 1;
        }else if(rotateArray[mid] &lt; rotateArray[right])
        {
            right = mid;
        }else{
            right--;
        }
    }
    return rotateArray[left];
}
</code></pre>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376dde81ca17b9?w=181&amp;h=71&amp;f=png&amp;s=2064" alt="效率" loading="lazy"></figure>
<h1 id="jz7-斐波那契数列">JZ7 斐波那契数列</h1>
<h2 id="题目-2">题目</h2>
<p>大家都知道斐波那契数列，现在要求输入一个整数n，请你输出斐波那契数列的第n项（从0开始，第0项为0，第1项是1）。<br>
n&lt;=39</p>
<h2 id="解法一-2">解法一</h2>
<p>一个比较丑陋的解法，直接依照公式。<br>
F(1)=1，F(2)=1, F(n)=F(n - 1)+F(n - 2)</p>
<pre><code>function Fibonacci(n)
{
    // write code here
    if(n == 0) return 0;
    if(n == 1) return 1;
    if(n == 2) return 1;
    var ret = 0;
    ret = Fibonacci(n-1) + Fibonacci(n-2);
    return ret;
}

</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376e6c1323cae3?w=190&amp;h=89&amp;f=png&amp;s=2250" alt="可以看到效果差的出奇" loading="lazy"><br>
可以看到效果差的出奇。</p>
<h2 id="解法二-2">解法二</h2>
<p>一种动态规划方法.详细看注释</p>
<pre><code>function Fibonacci(n)
{
    // write code here
    if(n == 0) return 0;
    if(n == 1) return 1;
    if(n == 2) return 1;
    let a = 0, b = 1;
    while(n--)
    {
        b += a;//形成新的后值
        a = b - a;//相当于原来b的值
    }
    return a;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376ee57cbadb45?w=168&amp;h=76&amp;f=png&amp;s=1993" alt="xiaolv" loading="lazy"><br>
这成绩进排行榜了吧可以。</p>
<p>还有一种从榜上看到的写法：</p>
<pre><code>function Fibonacci(n)
{
    // write code here
    const arr=[0,1]
    for(let i=2;i&lt;=n;i++){
        arr[i]=arr[i-1]+arr[i-2]
    }
    return arr[n]
}
</code></pre>
<h1 id="jz8-跳台阶">JZ8 跳台阶</h1>
<h2 id="题目-3">题目</h2>
<p>一只青蛙一次可以跳上1级台阶，也可以跳上2级。求该青蛙跳上一个n级的台阶总共有多少种跳法（先后次序不同算不同的结果）。</p>
<h2 id="解法一-3">解法一</h2>
<p>很典型的递归啦，一般都练到过吧伙伴们？说下思路，一次跳两级或者一级，那么我们就作为青蛙站在某个台阶上思考蛙生，我只有两种可能来到这里：1.从我下面一级跳上来的2.从我下面两级那里跳上来的。</p>
<p>得到这样的表达式：</p>
<pre><code>if(n == 1)  return 1;
if(n == 2)  return 3;

F(n) = F(n-1) + F(n-2);
</code></pre>
<p>代码如下：</p>
<pre><code>function jumpFloor(number)
{
    // write code here
    if(number == 1)  return 1;
    if(number == 2)  return 2;
    let ret = 0;
    ret = jumpFloor(number - 1) + jumpFloor(number - 2);
    return ret;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376f367b7dbbae?w=165&amp;h=80&amp;f=png&amp;s=2189" alt="xiaolv " loading="lazy"><br>
一般不进行优化的递归也就这种效率了。</p>
<h2 id="解法二-3">解法二</h2>
<p>同样可以用动态规划进行优化。</p>
<pre><code>function jumpFloor(number)
{
    // write code here
    if(number == 1)  return 1;
    if(number == 2)  return 2;
    let ret = 0;
    let a = 1, b = 2, c = 0;//分别表示跳上一级、二级有几种方法 三级的还没算，每次移步都算下一步的值
    while(number &gt; 2)
    {
        c = a + b;
        a = b;
        b = c;
        number--;
    }
    return c;
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2020/7/22/17376fcd3b7967d3?w=164&amp;h=70&amp;f=png&amp;s=1953" alt="xiaolv" loading="lazy"></figure>
<p>榜上有一种写法：</p>
<pre><code>function jumpFloor(number) {
      let dp = []
      dp[0] = 1
      dp[1] = 2
      for (let i = 2; i &lt; number; i++) {
        dp[i]=dp[i-1]+dp[i-2]
      }
      return dp[number-1]
    }
</code></pre>
<h1 id="jz9-变态跳台阶">JZ9 变态跳台阶</h1>
<h2 id="题目-4">题目</h2>
<p>一只青蛙一次可以跳上1级台阶，也可以跳上2级……它也可以跳上n级。求该青蛙跳上一个n级的台阶总共有多少种跳法。</p>
<h2 id="解法">解法</h2>
<p>哦豁，真的变态。但是其实很简单啦。</p>
<p>每个台阶可以看作一块木板，让青蛙跳上去，n个台阶就有n块木板，最后一块木板是青蛙到达的位子， 必须存在，其他 (n-1) 块木板可以任意选择是否存在（存在即是选择了踏上这块跳板），则每个木板有存在和不存在两种选择，(n-1) 块木板 就有 [2^(n-1)] 种跳法，可以直接得到结果。</p>
<p>所要求的序列为：0,1,2,4,8,16,……</p>
<pre><code>function jumpFloorII(number)
{
    // write code here
    if(number == 1)    return 1;
    return jumpFloorII(number-1)*2;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/22/1737731ae8d21b0c?w=173&amp;h=70&amp;f=png&amp;s=2031" alt="" loading="lazy"><br>
这才是真的算法题吧（头秃的那种）</p>
<h1 id="jz10-矩形覆盖">JZ10 矩形覆盖</h1>
<h2 id="题目-5">题目</h2>
<p>我们可以用2<code>*</code>1的小矩形横着或者竖着去覆盖更大的矩形。请问用n个2<code>*</code>1的小矩形无重叠地覆盖一个2<code>*</code>n的大矩形，总共有多少种方法？</p>
<p>比如n=3时，2*3的矩形块有3种覆盖方法：</p>
<figure data-type="image" tabindex="3"><img src="https://user-gold-cdn.xitu.io/2020/7/22/173773333d9fb587?w=767&amp;h=586&amp;f=png&amp;s=39258" alt="" loading="lazy"></figure>
<h1 id="解法-2">解法</h1>
<p>这里主要是思路的问题，我一开始用没想明白，分奇偶解决，但是不知为啥一直溢出。后面重新分析了一下。</p>
<p>n = 1时 return 1；</p>
<p>n = 2时，只有||和=两种情况；</p>
<p>n = 3时，有上面三种情况；</p>
<p>n = 4时，有从n=3出发的变形情况：和=||和||||和|=|这样三种情况，所以只在n=3的基础上往后面添加了一个竖着的。</p>
<p>从n=2出发的变形情况||=，==，=||和||||（这两种重复了），所以只在n=2的基础上往后面添加了两个横着的情况。</p>
<p>这样的话n=4的情况就是和n=2的情况+n=3的情况数量一致。</p>
<p>其他情况都是类似的。可以自然得到这样一个规律，f(n) = f(n-1) + f(n-2)，就是我们之前做过的斐波那契数列了。</p>
<pre><code>function rectCover(number)
{
    // write code here
    if(number == 0) return 0;
    if(number == 1) return 1;
    if(number == 2) return 2;
    let a = 1, b = 2;
    while(number-- &gt;= 2)
    {
        b += a;
        a = b - a;
    }
    return a;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/23/17377605740a999c?w=156&amp;h=74&amp;f=png&amp;s=1976" alt="" loading="lazy"><br>
注意下我们的起始值与之前的斐波那契不大一样。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[《剑指Offer》JS题解——JZ1~5]]></title>
        <id>https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz1~5/</id>
        <link href="https://giriawsh.github.io/post/lesslessjian-zhi-offergreatergreater-js-ti-jie-jz1~5/">
        </link>
        <updated>2020-09-11T04:38:47.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="️前言">✍️前言</h1>
<p>第一次刷这个系列，以前ACM的时候刷了一些leetcode，都没做什么记录。秋招前就做个刷题加算法记录吧。解法纯属个人理解，如若有错望指出。</p>
<p>语言基本用的JS，算作自我练习。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="️前言">✍️前言</h1>
<p>第一次刷这个系列，以前ACM的时候刷了一些leetcode，都没做什么记录。秋招前就做个刷题加算法记录吧。解法纯属个人理解，如若有错望指出。</p>
<p>语言基本用的JS，算作自我练习。</p>
<!-- more -->
<h1 id="jz1-二维数组中的查找">#JZ1 二维数组中的查找</h1>
<h2 id="题目">题目：</h2>
<p>在一个二维数组中（每个一维数组的长度相同），每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。</p>
<h2 id="解法一">解法一</h2>
<p>最直观的解法，大部分人都能很快写出来吧。直接暴搜，稍微利用一下递增的信息。</p>
<p>对于每个一维数组来说，最后一个是整个数组的最大值。同时又满足垂直递增，因此可以在搜索前判断一下范围。</p>
<ul>
<li>如果target比一维数组的最后值还要大，那就没必要搜它了。</li>
<li>同理，target比一维数组的第一个数字还要小，就不用管了。</li>
</ul>
<pre><code class="language-javascript">//通过简单判断进行搜索范围划定，降低复杂度
function Find(target, array)
{
    // write code here
    var num = array.length;
    var nlength = array[0].length;
    for(var i = 0; i &lt; array.length; i++)
    {
        if(array[i][nlength] &lt; target || array[i][0] &gt; target)//范围划定
        {
            continue;
        }else{
            for(var j = 0; j &lt; nlength; j++)
            {
                if(target == array[i][j])
                {
                    return true;
                }
            }
        }
    }
    return false;
}
</code></pre>
<p>效率如下：<br>
<img src="https://user-gold-cdn.xitu.io/2020/7/21/17370cdc33e3e06d?w=149&amp;h=82&amp;f=png&amp;s=1974" alt="解法一效率" loading="lazy"><br>
不过这就没二分搜索什么事了，这题八成要考的就是这玩意儿。</p>
<h2 id="解法二">解法二</h2>
<p>利用二分搜索</p>
<h3 id="关于二分搜索">关于二分搜索</h3>
<p>二分搜索是一种基本的搜索算法，在拥有递增、递减等规律性数据结构中有重要应用。由于其简单易懂的思路、清晰的递归过程，是有序前提下不二的算法选择。</p>
<p>二分查找法的O(log n)让它成为十分高效的算法。不过它的缺陷却也是那么明显的。就在它的限定之上：</p>
<ul>
<li>
<p>必须有序，我们很难保证我们的数组都是有序的。当然可以在构建数组的时候进行排序，可是又落到了第二个瓶颈上：它必须是数组。</p>
</li>
<li>
<p>数组读取效率是O(1)，可是它的插入和删除某个元素的效率却是O(n)。因而导致构建有序数组变成低效的事情。</p>
</li>
</ul>
<p>不过是题外话了。回到本题，借用牛客网的题解图，对于一维数组的二分折半搜索图示如下：</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/7/21/17370d7f4674bcd3?w=521&amp;h=159&amp;f=png&amp;s=11453" alt="一维二分查找" loading="lazy"></figure>
<p>假设目标tar在arr[1]处，那么我们的二分过程就是(来源为牛客网)：</p>
<ol>
<li>设初始值：定义一个二分的开始下标为l，结束下标为r，如图所示：</li>
<li>二分一半，中间位置为 mid = l + （（r - l) &gt;&gt; 1)， val&gt;&gt;1, 表示val右移一位相当于val／2，相当于 l+(r-l)/2,这样的写法是防止溢出。如果写成 mid = （l+r)/2; l+r可能会溢出。</li>
<li>如果 tar == arr[mid]，说明找到tar</li>
<li>比较：如果tar &gt; arr[mid], 说明tar在区间[mid+1, r]中，l = mid + 1</li>
<li>如果tar &lt; arr[mid]，说明tar在区间[l, mid-1]中, r = mid - 1</li>
</ol>
<p>所以进一步改进一维数组的搜索方式：</p>
<pre><code class="language-js">function Find(target, array)
{
    // write code here
    var num = array.length;
    var nlength = array[0].length;
    for(var i = 0; i &lt; num; i++)
    {
        if(array[i][nlength] &lt; target || array[i][0] &gt; target)//范围划定
        {
            continue;
        }else{
            var low = 0;
            var high = nlength - 1;
            while(low &lt;= high)
            {
                var mid = parseInt((low + high) / 2);
                if(target &lt; array[i][mid])
                {
                    high = mid-1;
                    
                }else if(target &gt; array[i][mid])
                {
                    low = mid+1;
                }else{
                    return true;
                }
            }
        }
    }
    return false;
}
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371c50cd4e6de7?w=164&amp;h=87&amp;f=png&amp;s=2079" alt="" loading="lazy"></figure>
<h2 id="解法三">解法三</h2>
<p>拓展到二维数组还有一种类似于动态规划（？）的算法。牛客网中有把基标定在右上角的解析，那我就定在左下角来分析吧。</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/17370dccf4cd37b9?w=594&amp;h=153&amp;f=png&amp;s=7706" alt="二维折半" loading="lazy"><br>
根据题意，我们能知道val右侧均&gt; val，其上侧均&lt; val，假设俺们的target位于arr[1][2]处。</p>
<p>首先进行tar与当前val的判断，若tar &gt; val，说明tar必定不在小于val的区域内，所以val可以右移进入下一层递归了。arr[3][0] -&gt; arr[3][1]</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/173711ba5e14f39a?w=570&amp;h=126&amp;f=png&amp;s=7814" alt="大于情况" loading="lazy"><br>
若tar &lt; val说明，tar不在大于val的区域内，val可以上移了。arr[3][]</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/173711bff9f1c85d?w=578&amp;h=129&amp;f=png&amp;s=7846" alt="小于情况" loading="lazy"><br>
把val的位置横向看做x坐标，纵向看做y坐标，就可以变相得出：</p>
<ul>
<li>若tar &gt; val, x++（val右移）</li>
<li>若tar &lt; val, y++（val上移）</li>
</ul>
<p><strong>复杂度分析</strong></p>
<p>时间复杂度：O(m+n) ，其中m为行数，n为列数，最坏情况下，需要遍历m+n次。</p>
<p>空间复杂度：O(1)</p>
<pre><code class="language-js">function Find(target, array)
{
    var num = array.length;
    var nlength = array[0].length;
    for(var x = 0, y = 0; x &lt; nlength &amp;&amp; y &lt;= num - 1; )
    {
        if(target &lt; array[num - 1 - y][x])//注意这里要-1以及在二维数组中需要进行坐标切换
        {
            y++;
        }else if(target &gt; array[num - 1 - y][x])
        {
            x++;
        }else
        {
            return true;
        }
    }
    return false;
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371ae35bed1571?w=235&amp;h=187&amp;f=png&amp;s=6701" alt="二分方法" loading="lazy"><br>
啧看起来提升效果不多呀。顺便说如果想在牛客网上刷运行时间的话，最好自己写处理函数，会快很多。</p>
<p>在函数前加上这个：</p>
<pre><code class="language-js">while(line=readline()){
    var index = line.indexOf(',');
    var left = parseInt(line.substring(0,index));
    var right = JSON.parse(line.substring(index+1));
    print(Find(left,right))
}
</code></pre>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371b09f6258296?w=171&amp;h=64&amp;f=png&amp;s=1969" alt="" loading="lazy"><br>
登登登~自个儿parse就是比内部过输入数据模式切换快啊。</p>
<h1 id="jz2-替换空格">#JZ2 替换空格</h1>
<h2 id="题目-2">题目</h2>
<p>请实现一个函数，将一个字符串中的每个空格替换成“%20”。例如，当字符串为We Are Happy.则经过替换之后的字符串为We%20Are%20Happy。</p>
<h2 id="解法">解法</h2>
<p>啊这，这不就是我js的大天下？一行搞定</p>
<pre><code class="language-js">function replaceSpace(str)
{
    // write code here
    return str.replace(/ /g, &quot;%20&quot;)
    //str.replace(/\s/g,'%20')这样写也可以。
}
</code></pre>
<p>顺便说一声，如果直接用str.replace(&quot; &quot;, &quot;%20&quot;)是8行的，因为replace<strong>只替换首个匹配</strong>。</p>
<h1 id="jz3-从头到尾打印链表">#JZ3 从头到尾打印链表</h1>
<h2 id="题目-3">题目</h2>
<p>输入一个链表，按链表从尾到头的顺序返回一个ArrayList。</p>
<h2 id="解法-2">解法</h2>
<p>先来复习下链表，长这样：</p>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371d6f78166bb2?w=625&amp;h=163&amp;f=png&amp;s=17101" alt="链表结构" loading="lazy"><br>
那么只要构造一个数组，将数据每次都从头部插入（由于数据是倒着插入的，最后会得到一个正着的数组）</p>
<pre><code class="language-js">/*function ListNode(x){
    this.val = x;
    this.next = null;
}*/
function printListFromTailToHead(head)
{
    // write code here
    var arr = new Array;
    while(head != null)
    {
        arr.unshift(head.val);
        head = head.next;
    }
    return arr;
}
</code></pre>
<figure data-type="image" tabindex="3"><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371dbb8620f47c?w=165&amp;h=85&amp;f=png&amp;s=2081" alt="链表结果" loading="lazy"></figure>
<p>这里有很多关于链表的操作，后面可以拎出来练练：<a href="https://juejin.im/entry/6844903498362912775">JS中的算法与数据结构——链表(Linked-list)</a></p>
<h1 id="jz4-重建二叉树">#JZ4 重建二叉树</h1>
<h2 id="题目-4">题目</h2>
<p>输入某二叉树的前序遍历和中序遍历的结果，请重建出该二叉树。假设输入的前序遍历和中序遍历的结果中都不含重复的数字。例如输入前序遍历序列{1,2,4,7,3,5,6,8}和中序遍历序列{4,7,2,1,5,3,8,6}，则重建二叉树并返回。</p>
<h1 id="解法-3">解法：</h1>
<p>复习下二叉树，如果是一棵完全二叉树长这样：</p>
<figure data-type="image" tabindex="4"><img src="https://user-gold-cdn.xitu.io/2020/7/21/17371e1fe4fd8edd?w=175&amp;h=186&amp;f=png&amp;s=8126" alt="完全二叉树" loading="lazy"></figure>
<p>前序遍历：根结点 ---&gt; 左子树 ---&gt; 右子树（先遍历根节点，然后左右））</p>
<p>中序遍历：左子树---&gt; 根结点 ---&gt; 右子树（在中间遍历根节点）</p>
<p>后序遍历：左子树 ---&gt; 右子树 ---&gt; 根结点（最后遍历根节点）</p>
<p>层次遍历：只需按层次遍历即可</p>
<p>思路：</p>
<ol>
<li>根据前序遍历序列找到根节点的值（根据前序遍历规律知道第一个就是根节点了）</li>
<li>在中序遍历序列中找到根节点的位置</li>
<li>找到左子树根节点（中序遍历中根结点的前一个位置，如果存在的话，就是左子树的根结点）</li>
<li>找到右子树根节点（中序遍历中根结点的后一个位置，如果存在的话，就是右子树的根结点）</li>
<li>递归地重建子树（使用左子树的前序、中序序列构建左子树；使用右子树的前序、中序序列构建右子树）</li>
</ol>
<pre><code class="language-js">function reConstructBinaryTree(pre, vin)
{
    // write code here
    if(vin.length == 0)
        return null;
    var ret = {};
    if(vin.length &gt; 1)
    {
        var root = pre[0];
        var i = vin.indexOf(root);
        var left_vin = vin.slice(0, i);//根据中序遍历原理，根结点前面的一定是它下面的左子树结点们
        var left_pre = pre.slice(1, left_vin.length+1);//根据前序遍历原理，它的左子树的前序遍历，只需要从根节点之后摘出相应左子树结点个数就可以。
        var right_vin = vin.slice(i+1, vin.length);//根据中序遍历原理，根结点后面的一定是它下面的左子树结点们
        var right_pre = pre.slice(left_vin.length+1, pre.length);//后面的都是右节点的前序遍历了（因为前-&gt;左-&gt;右，而前和左已经取完了，剩下都是右了）
        ret = {
            val: root,
            left: reConstructBinaryTree(left_pre, left_vin),
            right: reConstructBinaryTree(right_pre, right_vin)
        }
    }else if(vin.length == 1){
        ret = {
            val: pre[0],
            left: null,
            right: null
        }
    }
    return ret;
}
</code></pre>
<figure data-type="image" tabindex="5"><img src="https://user-gold-cdn.xitu.io/2020/7/21/173720bf464957cd?w=188&amp;h=78&amp;f=png&amp;s=2171" alt="结果" loading="lazy"></figure>
<h1 id="jz5-用两个栈实现队列">#JZ5 用两个栈实现队列</h1>
<h2 id="题目-5">题目</h2>
<p>用两个栈来实现一个队列，完成队列的Push和Pop操作。 队列中的元素为int类型。</p>
<h2 id="解法-4">解法</h2>
<p>队列就是像排队一样，一个接一个，先来排队的先出去。</p>
<p>而栈则是像杯子一样，先进来的韭菜被压在下面，后进来的韭菜先被割走（斜眼笑）</p>
<p>要变成队列就很简单了呀~想象一下，这里有两个杯子，要怎样才能实现整齐地先来先被割的韭菜队列呢？</p>
<p>进来一系列的韭菜，在栈中他们就是</p>
<pre><code>|=============================================
|（底）1号韭菜--2号韭菜--……---最后来的韭菜（顶）
|=============================================
</code></pre>
<p>这样的顺序，那么只要把它反过来像杯子倒水一样扣在另一个栈里，不就变成这样子了：</p>
<pre><code>            ===============================================|
得到解放&lt;---(顶)1号韭菜--2号韭菜--……---最后来的韭菜(底)      |
            ===============================================|
</code></pre>
<p>就可以按序倒出去了，这就是pop的实现方法了。这时候数据是存在2号栈中的，要是又有新数据来了怎么办，那就倒回1号栈中然后把新韭菜塞进来就OK了。</p>
<pre><code>|=============================================
|（底）1号韭菜--2号韭菜--……---最后来的韭菜（顶）&lt;--新来的韭菜
|=============================================
</code></pre>
<p>那么代码就很好写了：</p>
<pre><code class="language-js">var stack1 = [];//默认数据先装这里
var stack2 = [];
function push(node)
{
    // write code here
    if(stack2.length &gt; 0)
    {
        while(stack2.length &gt; 0)
        {
            stack1.push(stack2.pop());
        }
    }
     return stack1.push(node);
    
}
function pop()
{
    // write code here
    if(stack1.length &gt; 0){
            while(stack1.length &gt; 0)
            {
                stack2.push(stack1.pop());
            }
    }
    return stack2.pop();

}
</code></pre>
<figure data-type="image" tabindex="6"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911123617.png" alt="运行结果" loading="lazy"></figure>
<p>好像上算法课讲过一道更难的，忘了题目了，下次见到了补充进来吧。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Gridea+GithubPages+godaddy+gitalk个人博客配置]]></title>
        <id>https://giriawsh.github.io/post/grideagithubpagesgodaddygitalk-ge-ren-bo-ke-pei-zhi/</id>
        <link href="https://giriawsh.github.io/post/grideagithubpagesgodaddygitalk-ge-ren-bo-ke-pei-zhi/">
        </link>
        <updated>2020-09-11T04:26:04.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">👨‍💻前言</h1>
<p>最近重新思考了一下生命的意义，觉得在人世间活过一遭总归是要留下些什么，如果更好的是能够带去些什么。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">👨‍💻前言</h1>
<p>最近重新思考了一下生命的意义，觉得在人世间活过一遭总归是要留下些什么，如果更好的是能够带去些什么。</p>
<!-- more -->
<p>之前有写过hexo建站，但是hexo的不稳定性以及频繁的hexo d hexo g手动命令操作使我厌烦。机缘巧合之中，在知乎上看到了一款快速建站软件。</p>
<p>它体积量很小，也很年轻，除了主题外配置都是一个人在维护，但是现在看来总体运行情况十分稳定。我承蒙了该软件的好处，写下这篇新人向的快速建站手册，希望能够帮助到有同样想法的人。</p>
<h1 id="简单介绍">简单介绍</h1>
<p><a href="https://gridea.dev/">Gridea</a> 最早叫 Hve Notes ，开发者为了更易读和好记，重新命名为 Gridea ，支持 Windows 和 Mac 平台，它的基础界面非常地小清新。</p>
<figure data-type="image" tabindex="1"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911113005.png" alt="image-20200911113005316" loading="lazy"></figure>
<p>只需要简单配置，便可图形化可交互地进行博文上传同步、主题更改等操作。这些后面都会详细介绍，包括一些踩坑向。</p>
<h1 id="软件安装与配置">软件安装与配置</h1>
<h2 id="git安装">Git安装</h2>
<p>这块不用多说，写的比我详细的大有人去， <a href="https://zhuanlan.zhihu.com/p/71681116/Git">Git官网</a>下载平台对应的安装程序。安装没有特别的要求，一路下一步即可。</p>
<p>如果遇到问题谷歌would help you</p>
<h2 id="gridea安装">Gridea安装</h2>
<p><a href="https://link.zhihu.com/?target=https%3A//gridea.dev/">Gridea官网</a>下载对应平台的安装程序进行安装。</p>
<h1 id="初始配置">初始配置</h1>
<p>这里搬出官网文档：https://gridea.dev/gridea-start/</p>
<h2 id="github-pages仓库创建与开通">github pages仓库创建与开通</h2>
<ol>
<li>
<p>首先，需要创建一个仓库，建议仓库为 <strong><a href="https://link.zhihu.com/?target=http%3A//username.github.io">http://username.github.io</a></strong>。根据[官方文档](https://zhuanlan.zhihu.com/p/71681116/GitHub 帮助)， GitHub 会默认将名称符合要求的仓库设置成 GitHub Pages 可访问类型。例如我的用户名是 giriawsh，仓库名便为giriawsh.github.io</p>
</li>
<li>
<p>创建成功后来到项目仓库的settings</p>
</li>
<li>
<p>找到 <strong>GitHub Pages</strong> 设置的地方 点击 <strong>Choose a theme</strong> 随意选择一个主题。因为后续我们使用 Gridea 设置主题，这边选择主题只是为了开启 GitHub Pages 可访问类型。（不然po不上去）</p>
</li>
</ol>
<h2 id="创建访问token">创建访问token</h2>
<p>这块官方文档里有：</p>
<p>Gridea 管理 GitHub Pages 需要 token。登录 GitHub 后点击<a href="https://link.zhihu.com/?target=https%3A//github.com/settings/tokens/new">这里</a> 创建一个新的 token。</p>
<p>勾选上 <strong>repo</strong> 相关权限即可。</p>
<p>生成之后记得把 Token 复制到你的本地，因为一旦关闭网页将不能再看到它。</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911113404.png" alt="img" loading="lazy"></figure>
<h2 id="gridea配置">Gridea配置</h2>
<p><strong>进入 Gridea 进行远程设置</strong>。按照下图的示例进行设置，然后点击「<strong>检测远程链接</strong>」，进行配置检查。</p>
<figure data-type="image" tabindex="3"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911113455.png" alt="img" loading="lazy"></figure>
<ul>
<li>域名：Github Pages 对应的域名</li>
<li>仓库：你的静态文件存放的仓库</li>
<li>分支：你的 Github Pages 对应的分支（例如：master）</li>
<li>用户名：仓库所属用户的用户名（通常就是你的 Github 账号的昵称）</li>
<li>邮箱：Git 推送时使用的邮箱（通常就是你的 Github 账号的邮箱）</li>
<li>Token: Git 推送时需要的 Token，用来向 Github 提交构建后的文件 <a href="https://github.com/settings/tokens">Github personal access tokens</a></li>
<li>CNAME: 可通过这个选项配置你自己的域名</li>
</ul>
<p>出现错误可能原因：</p>
<ol>
<li>域名未填对，如果https的域名不行改用http</li>
<li>仓库名错误</li>
<li>Token在开头多加了一个空格（复制的锅）</li>
</ol>
<p><strong>同步</strong>。点击 Gridea 中的 同步按钮即可。初次部署等待几分钟之后就可以去浏览器访问了（也有可能是半小时），默认地址为你在第 3 步所填写的域名。</p>
<h1 id="godaddy域名配置">Godaddy域名配置</h1>
<p>如果想要拥有自己独一无二又好记的域名，可以到<a href="https://sg.godaddy.com/zh/offers/domains/godaddy-b?isc=gennlcn08&amp;countryview=1&amp;currencyType=CNY&amp;gclid=CjwKCAjwnef6BRAgEiwAgv8mQc7yfrZ59mrhuZLX7P0MkksaGjI8_FbM_8qeG7lLv6MkMCFBDK2OeBoCFoAQAvD_BwE&amp;gclsrc=aw.ds">godaddy</a>网站上进行域名购买。如果在国内购买需要去登记下。</p>
<p>选择自己喜欢的（注意一下续费年费），用支付宝买下就OK</p>
<p>然后在“我的产品”中找到购买的域名：</p>
<figure data-type="image" tabindex="4"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114053.png" alt="image-20200911114052942" loading="lazy"></figure>
<p>点击DNS，进入配置界面</p>
<p>在记录中增加两条记录：</p>
<figure data-type="image" tabindex="5"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114140.png" alt="image-20200911114140274" loading="lazy"></figure>
<p>（注意把giriawsh替换成自己的用户名）</p>
<p>A类型的值可以通过</p>
<pre><code>ping &lt;your username&gt;.github.io
</code></pre>
<p>获得。</p>
<p>接着打开gridea，将CNAME设置为你的域名。</p>
<figure data-type="image" tabindex="6"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114258.png" alt="image-20200911114257991" loading="lazy"></figure>
<p>同步后github仓库中便可以看到CNAME已经做了同步修改，同时也出现了网站已被同步po到新域名的样式。再等全球服务器缓冲一会儿，你的域名就可以被访问了。等待一段时间后，可开启Enforce HTTPS选项。</p>
<figure data-type="image" tabindex="7"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114432.png" alt="image-20200911114432397" loading="lazy"></figure>
<h1 id="gitalk评论配置">Gitalk评论配置</h1>
<p>Gridea 支持 <a href="https://zhuanlan.zhihu.com/p/71681116/gitalk/gitalk">Gitalk</a> 和 Disqus 两种评论系统，这里我使用 Gitalk 评论系统。</p>
<p>首先你需要有一个 <strong>GitHub OAuth Applicatio</strong> ，点击<a href="https://link.zhihu.com/?target=https%3A//github.com/settings/applications/new">这里</a>创建。这里我用到另一个博主的图片：</p>
<figure data-type="image" tabindex="8"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114617.jpeg" alt="img" loading="lazy"></figure>
<p>创建完成后可将 <strong>Client ID</strong> 和 <strong>Client Secret</strong> 复制保存到本地。</p>
<figure data-type="image" tabindex="9"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114631.jpeg" alt="img" loading="lazy"></figure>
<p>打开Gridea-远程-评论配置，粘贴</p>
<figure data-type="image" tabindex="10"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911114708.jpeg" alt="img" loading="lazy"></figure>
<p>设置、保存并同步后评论模块就可用了。</p>
<p>踩坑（来自别的博主）：</p>
<p>开始配置完成后，虽然文章末尾已经显示评论界面，但是只要我一点击登录评论，就会跳转到首页。在网上查了波资料发现，是OAuth Apps配置页面中的Authorization callback URL和Homepage URL配置的有问题，我在博客绑定了自己的域名：<a href="https://www.wherelse.tk/">www.wherelse.tk</a> 的情况下，填写了我github pages默认的链接：<a href="https://wherelse.github.io/">wherelse.github.io</a> ，导致评论出现问题，在将配置修改应用之后，就可以使用了。</p>
<h1 id="主题配置">主题配置</h1>
<p>本站使用的是由<a href="https://github.com/qyxtim/bitcron-pro">SERENCE</a>改版的pro版。</p>
<p>在此基础上做了一些个人向的修改。再次感谢大佬的卓越贡献，已star。</p>
<p>从网站下载zip包后，把 bitcron-pro 文件夹放入 <code>~/Gridea/themes</code> 【默认文件夹】或者手动修改过的网站源文件文件夹中，解压缩，重启Gridea即可。</p>
<p>更多主题可查看官方给出的主题超市：https://gridea.dev/themes/</p>
<h1 id="踩坑">踩坑</h1>
<ul>
<li>如果创建了新标签，记得及时给文章引用上，不然会无法po文</li>
<li>gridea中无category的概念，如果需要，可以在菜单栏中链接到某个网页或者某个标签</li>
<li>一次性po文太多可能导致一些奇怪bug</li>
<li>不要在标题中使用~等特殊符号</li>
</ul>
<h1 id="参考致谢">参考致谢</h1>
<p><a href="https://zhuanlan.zhihu.com/p/71681116">Gridea 上手教程——小白也可以用的 GitHub Pages 搭建工具</a></p>
<p><a href="https://sspai.com/post/54212">Gridea 让你更方便地管理 Github Pages</a></p>
<p><a href="https://wherelse.cc/post/config-gitalk-in-the-gridea/">在Gridea中配置Gitalk评论系统</a>   -&gt; 这个博主的主题很好看，不知是源于什么自改的？</p>
<p><a href="https://gridea.dev/docs/">Gridea官方文档</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[规则之下的世界 —— 《电视人》有感]]></title>
        <id>https://giriawsh.github.io/post/gui-ze-zhi-xia-de-shi-jie-lesslessdian-shi-ren-greatergreater-you-gan/</id>
        <link href="https://giriawsh.github.io/post/gui-ze-zhi-xia-de-shi-jie-lesslessdian-shi-ren-greatergreater-you-gan/">
        </link>
        <updated>2020-09-10T16:23:13.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">📚前言</h1>
<p>不知道上一回动笔写这种随笔类的东西是什么时候的事儿了。高中毕业以来，断断续续地读了很多书，不知是冥冥中有缘分，还是只是秉性比较相近，唯一留下过的几篇读后感都是村上春树的书。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">📚前言</h1>
<p>不知道上一回动笔写这种随笔类的东西是什么时候的事儿了。高中毕业以来，断断续续地读了很多书，不知是冥冥中有缘分，还是只是秉性比较相近，唯一留下过的几篇读后感都是村上春树的书。</p>
<!-- more -->
<p>我对怪物有着特别的痴迷情节，当然说的不是克鲁苏神话中的多眼形象，而是人性中的善恶之形，亦或是一种行为所带来的可视性后果。说到底，我对是什么塑造了使我们区别于万物这一特性着迷，怪物也不过是千千万万人类不同点的具象性特征。言语上很难描绘，不过我正在试图用写小说来阐释使我夜夜流连忘返的世界。</p>
<p>《电视人》是一本我很早就放到书架里的书，篇幅不长，在ipad上只消翻个一百来页便能读完。零散收集了很多故事，大多光怪陆离，却又无不例外是窒息真实的体现。</p>
<h1 id="章节赏析">章节赏析</h1>
<h2 id="电视人">《电视人》</h2>
<p>这是第一个故事，大致讲的是某日，我看见了电视人来到了我的房间。他们外形是均衡地等比例缩小，就像是机械复印出来的，贸然地闯入，一人开门另外两人拿着索尼电视机。他们搅乱屋子，插上插头打开电视，无论怎么换台都没有任何画面，似乎只是为了接通电源，接着便出去了。</p>
<p>看到电视人后，我开始注意到他人对电视人全然不知，也不愿有人提起，我尝试张开嘴巴说些什么，却被妻子、同事无视，最后发现，我竟失去了语言。</p>
<blockquote>
<p>精彩点评：</p>
<p>对电视人要说的话，因为周日下午头痛，不想说；</p>
<p>对妻子想说的话，因为觉得没必要说，没有说；</p>
<p>心中对电视人的疑问，因为问题被忽略，说不下去；</p>
<p>丧失言语的过程，是缓慢的、不知不觉的。话语忍而不说，渐渐就同化成了机械工作、没有感情的电视人。 ——随欢</p>
</blockquote>
<p>整个故事想表达的，精彩点评概括得已经很到位。这个奇妙又透露着悲凉的故事其实在现实中也有很多影子。我们可能在家庭中、生活中，因为生理、心理上的一时隔阂或者情绪便放弃了表达自我的机会，也有可能身于桎梏而生不由己。但是表达确实表征灵魂特征的重要function，若是偏安一隅，或是苟且无言，身体与心灵和机器的同化，也便是眨眼之事了。</p>
<h3 id="佳句摘录">佳句摘录</h3>
<ul>
<li>关键词：【描写】【头疼】</li>
</ul>
<p>我不喜欢周日傍晚这一时分，或者说不喜欢它所附带的一切——总是不喜欢带有周日傍晚意味的状况。每当周日傍晚姗姗而至，我的脑袋必定开始作痛。痛的成都每次固然轻重有别，但终究是痛。两侧太阳穴一至一点五厘米左右的深处，柔软白嫩的肉块无端地绷得很紧，俨然肉块中间伸出无数条细线，而有人从遥远的地方握住那线头悄悄拉拽。不是特别痛。本来痛也无妨，却偏偏不很痛，不可思议。就像有根长针一下子刺进深度麻醉的部位一样。</p>
<ul>
<li>关键词：【人物形象描写】【心理看法】【平衡并非绝对】</li>
</ul>
<p>三个电视人一律身穿藏青色上移，不知是何不了，反正像是滑溜溜的，下身是蓝牛仔裤，脚上是网球鞋。服装和鞋都被缩小了一些。看他们忙这忙那看了良久，我竟开始怀疑自己认为其小的看法存在问题，觉得好像自己是戴一副高度数的眼睛倒坐在冲浪船上。景物前后变形，<strong>从中可以认识到自己迄今无意识置身期间的世界的平衡并非绝对的</strong>，而使我产生如此心情的便是电视人。</p>
<ul>
<li>关键词：【心理描写】【妻子离去】</li>
</ul>
<p>或许，我猛然想到，妻子或许真的不返回这里了。妻子已经跑到很远很远的地方去了，使用所有的交通工具，跑到我无法追及的远处去了。的确，我们的关系或许已破裂得无可挽回，成为泡影了，只不过自己没意识到而已。纷纭的思绪松懈开来，又合二为一。或许如此，我说出声来。我的声音在自己体内往来徘徊。</p>
<ul>
<li>关键词：【结尾】【心理描写】</li>
</ul>
<p>“马上会有电话打来。”电视人说了一句，<strong>然后像在运算似的停了一会</strong>，“五分钟后。”</p>
<p>我看着电话机。我思考电话机上的软线，连接天涯海角的软线，妻子便在这可怕的迷宫般的线路的某个末梢。那里远得很，远得我望尘莫及。我感觉到了她心脏的跳动。五分钟后，我想，哪头是前端哪头为后尾呢？我站起身，准备说出口。然而在站起的一瞬间，我竟失去了语言。</p>
<h2 id="飞机">《飞机》</h2>
<p>讲述的是一个男子与已婚女子偷情相爱，他们在男主人不在家时一起听歌、做爱。女方时不时地问他，你是不是一直有自言自语的毛病？男方阐述“我简直像念诗一样自言自语。”他们谈论不存在的飞机，森林深处制造的飞机，在哭泣中做爱收尾。男子自我摆脱了一种无形束缚，也读懂了女子的眼泪。</p>
<blockquote>
<p>精彩点评：</p>
<p>真的是男子在自言自语吗？</p>
<p>不，是女子的自言自语。她无法自由地自言自语，咽下去的话发酵成了泪，流了出来。</p>
<p>女子为何与男子交往？她发现男子与她一样，心中装着“飞机”，或者在心灵深处制造飞机——都想摆脱现实。</p>
<p>男子最后读懂了女子，但是，却是隔着坚硬的现实读懂的。</p>
</blockquote>
<h3 id="佳句摘录-2">佳句摘录</h3>
<ul>
<li>关键词：【动作描写】【心理描写】【摆弄咖啡匙】</li>
</ul>
<p>她把颜色素雅的花格衫的两袖挽到臂肘，细细白白的手指不停地摆弄咖啡匙。他注视着她的指尖。定睛注视之间，意识竟奇异地平坦起来，**仿佛她在拎起世界的边缘一点点揉平。**揉的态度甚是冷淡，仿佛例行公务，像是在说虽然花时间但也只能从那里揉下去。</p>
<ul>
<li>关键词：【语言描写】【生活描述】</li>
</ul>
<p>没有自我辩解意味，就像谈交通规则和日期变更线一样客观地讲述自己的婚姻生活。“我想我是幸福的，能够算作问题的问题一个也没有。”</p>
<ul>
<li>关键词：【动作描写】【拒绝】</li>
</ul>
<p>女子拒绝的时候也是有的——她不声不响地默默摇头。那时她的眼神看上去仿佛黎明时分浮在远方天际的银白色的月，随着一声报晓鸟鸣而颤抖的瘪平瘪平的富有暗示意味的月。看到她这样的眼睛，他就再也说不出什么了。</p>
<ul>
<li>关键词：【语言描写】【人心】</li>
</ul>
<p><strong>“人心这玩意儿，我想怕是深井那样的东西。谁都不清楚井底有什么，只能根据时而浮上来的东西的形状想象。”</strong></p>
<ul>
<li>关键词：【心理描写】【自我醒悟】</li>
</ul>
<p>这时间里，他手托着腮看她长长的睫毛。她几秒钟眨一次眼，眨得不规则。如此定睛地注视她那睫毛——就在刚才还被泪水打湿的睫毛——之间，他又糊涂起来，弄不清同她睡觉究竟意味什么。一种无可言喻的失落感——<strong>仿佛复杂系统的一部分被人拉长从而变得极其简单的失落感朝他袭来。他想，长此以往自己恐怕哪里也抵达不了。</strong></p>
<ul>
<li>关键词：【环境描写】【沉默】</li>
</ul>
<p>两人沉默有顷。桌面上，咖啡不断变浑，不断变凉。地轴旋转不休。月球让重力悄然发生变化致使潮涨潮落。时间在沉默中流逝。电车在铁路上驶过。</p>
<ul>
<li>关键词：【心理描写】【结尾】</li>
</ul>
<p>如此没过多久，她又哭了。一天当中哭两回，是头一遭，也是最后一次。对她来说，仿佛是一个特殊仪式。他隔桌伸过手碰她的头发。感触极其真实。恰如人生本身，坚硬、光滑，又相距辽远。</p>
<p>他想：不错，当时我的确像念诗一样自言自语来着。</p>
<h2 id="我们时代的民间传说">《我们时代的民间传说》</h2>
<p>emmm这是一个很具有写实性的故事。讲的是我偶遇了当年班上的优等生，他与我分享了自己的爱情故事。他和班上的班花可谓是那个时代的童男玉女，两个人也一直是父母眼里的乖娃娃，老师眼中的放心学生，对纪律和良心怀有敬意。不仅成绩优异，而且三观相符。他们互相表达爱慕，又听从女子出嫁后才能做爱的事宜。但渐渐的，男子爱得越来越深，和她探讨起了结婚事宜，但是女子依旧遵从父母的想法，虽然很爱他却必须要自己嫁给一位年长的男性。</p>
<p>“我们的爱还没接受任何考验，我们还没履行任何责任，我们还是孩子啊，无论我还是你。”</p>
<p>男子逐渐意识到了，曾经保护自己的规则围栏，此刻却成为了阻碍的进程，他感到软弱无力，却不想在里面虚度年华。最终两人分手了。后面因为机缘巧合两人重逢，女子基本上按照计划安排了自己的生活，得到了一桩得体的婚姻，却想和男主履行当年的“我很爱你，结婚后我们仍然可以做爱”的约定。男子意识到了很多东西都已发生了改变，与女子道别再见。</p>
<p>总体来说是一个比较写实性的故事，任何时代都有成长与幻灭，最终坦然向青春道别。故事中展现了两种不同的爱情观，纯粹理性与感性人生。我暂未经历过婚姻这种人生大事，可能对于不同人来说方法的选择是多样的，过于感性之人社会会毒打一顿后让你变得斤斤计较一些，过于理性之人在遭受漆黑之后也更需要尝试主动寻求温暖。这既是在他身上发生的事，也会在千千万万的人身上发生，这大概就是故事的魅力吧。</p>
<h3 id="佳句摘要">佳句摘要</h3>
<ul>
<li>关键词</li>
</ul>
<p>世界很大，存在着形形色色的平行的价值观，而且不需要什么优等生，</p>
<p>PS： 12点了，睡觉，明天再继续。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[【置顶】博客建设中🔨]]></title>
        <id>https://giriawsh.github.io/post/bo-ke-qian-yi-zhong/</id>
        <link href="https://giriawsh.github.io/post/bo-ke-qian-yi-zhong/">
        </link>
        <updated>2020-09-10T15:11:15.000Z</updated>
        <content type="html"><![CDATA[<p>本站正在进行博文迁移计划，建设尚未完成，暂时博文较少🤣</p>
<p>若想浏览更多，可先移步👉<a href="https://juejin.im/user/439393493975245/posts">个人掘金主页</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Vue学习笔记10-深入了解混入、渲染函数节点树相关]]></title>
        <id>https://giriawsh.github.io/post/vue-xue-xi-bi-ji-10-shen-ru-liao-jie-hun-ru-xuan-ran-han-shu-jie-dian-shu-xiang-guan/</id>
        <link href="https://giriawsh.github.io/post/vue-xue-xi-bi-ji-10-shen-ru-liao-jie-hun-ru-xuan-ran-han-shu-jie-dian-shu-xiang-guan/">
        </link>
        <updated>2020-09-10T07:21:14.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>本文深入了解混入、渲染函数节点树相关，vue学习笔记10.</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>本文深入了解混入、渲染函数节点树相关，vue学习笔记10.</p>
<!-- more -->
<h1 id="过渡与动画">过渡与动画</h1>
<p>这块放到后面的开发demo里再写吧，说不定能够结合数据可视化组件进行分析写。</p>
<h1 id="混入">混入</h1>
<p>混入 (mixin) 提供了一种非常灵活的方式，来分发 Vue 组件中的可复用功能。一个混入对象可以包含任意组件选项。当组件使用混入对象时，所有混入对象的选项将被“混合”进入该组件本身的选项。</p>
<pre><code>// 定义一个混入对象
var myMixin = {
  created: function () {
    this.hello()
  },
  methods: {
    hello: function () {
      console.log('hello from mixin!')
    }
  }
}

// 定义一个使用混入对象的组件
var Component = Vue.extend({
  mixins: [myMixin]
})

var component = new Component() // =&gt; &quot;hello from mixin!&quot;
</code></pre>
<h2 id="选项合并">选项合并</h2>
<p>当组件和混入对象含有同名选项时，这些选项将以恰当的方式进行“合并”。</p>
<p>数据对象在内部会进行递归合并，并在发生冲突时以<strong>组件数据优先</strong>。</p>
<pre><code>var mixin = {
  data: function () {
    return {
      message: 'hello',
      foo: 'abc'
    }
  }
}

new Vue({
  mixins: [mixin],
  data: function () {
    return {
      message: 'goodbye',//组件数据优先
      bar: 'def'
    }
  },
  created: function () {
    console.log(this.$data)
    // =&gt; { message: &quot;goodbye&quot;, foo: &quot;abc&quot;, bar: &quot;def&quot; }
  }
})
</code></pre>
<p>同名钩子函数将<strong>合并为一个数组</strong>，因此<strong>都将被调用</strong>。另外，<strong>混入对象的钩子将在组件自身钩子之前调用</strong>。</p>
<pre><code>var mixin = {
  created: function () {
    console.log('混入对象的钩子被调用')
  }
}

new Vue({
  mixins: [mixin],
  created: function () {
    console.log('组件钩子被调用')
  }
})

// =&gt; &quot;混入对象的钩子被调用&quot;
// =&gt; &quot;组件钩子被调用&quot;
</code></pre>
<p>值为对象的选项，例如 methods、components 和 directives，将被合并为同一个对象。两个对象键名冲突时，<strong>取组件对象</strong>的键值对。</p>
<pre><code>var mixin = {
  methods: {
    foo: function () {
      console.log('foo')
    },
    conflicting: function () {
      console.log('from mixin')
    }
  }
}

var vm = new Vue({
  mixins: [mixin],
  methods: {
    bar: function () {
      console.log('bar')
    },
    conflicting: function () {
      console.log('from self')
    }
  }
})

vm.foo() // =&gt; &quot;foo&quot;
vm.bar() // =&gt; &quot;bar&quot;
vm.conflicting() // =&gt; &quot;from self&quot;
</code></pre>
<p>反正冲突时候组件数据最大就对了。</p>
<blockquote>
<p>Vue.extend() 也使用同样的策略进行合并。</p>
</blockquote>
<h2 id="全局混入">全局混入</h2>
<p>混入也可以进行全局注册。使用时格外小心！一旦使用全局混入，它<strong>将影响每一个之后创建的 Vue 实例</strong>。使用恰当时，这可以用来为自定义选项注入处理逻辑。</p>
<pre><code>// 为自定义的选项 'myOption' 注入一个处理器。
Vue.mixin({
  created: function () {
    var myOption = this.$options.myOption
    if (myOption) {
      console.log(myOption)
    }
  }
})

new Vue({
  myOption: 'hello!'
})
// =&gt; &quot;hello!&quot;
</code></pre>
<blockquote>
<p>慎用！！！</p>
</blockquote>
<h2 id="自定义选项合并策略">自定义选项合并策略</h2>
<p>自定义选项将使用默认策略，即简单地覆盖已有值。如果想让自定义选项以自定义逻辑合并，可以向 Vue.config.optionMergeStrategies 添加一个函数。（不过模块化之后感觉基本用不到了啦）</p>
<pre><code>Vue.config.optionMergeStrategies.myOption = function (toVal, fromVal) {
  // 返回合并后的值
}
</code></pre>
<p>对于多数值为对象的选项，可以使用与 methods 相同的合并策略</p>
<pre><code>var strategies = Vue.config.optionMergeStrategies
strategies.myOption = strategies.methods
</code></pre>
<pre><code>一个更高级的例子
const merge = Vue.config.optionMergeStrategies.computed
Vue.config.optionMergeStrategies.vuex = function (toVal, fromVal) {
  if (!toVal) return fromVal
  if (!fromVal) return toVal
  return {
    getters: merge(toVal.getters, fromVal.getters),
    state: merge(toVal.state, fromVal.state),
    actions: merge(toVal.actions, fromVal.actions)
  }
}
</code></pre>
<h1 id="渲染函数jsx">渲染函数&amp;JSX</h1>
<p>Vue 推荐在绝大多数情况下使用模板来创建你的 HTML。然而在一些场景中，你真的需要 JavaScript 的完全编程的能力。这时你可以用渲染函数，它比模板更接近编译器。</p>
<p>假设我们要生成一些带锚点的标题</p>
<pre><code>&lt;h1&gt;
  &lt;a name=&quot;hello-world&quot; href=&quot;#hello-world&quot;&gt;
    Hello world!
  &lt;/a&gt;
&lt;/h1&gt;
对于上面的 HTML，你决定这样定义组件接口
&lt;anchored-heading :level=&quot;1&quot;&gt;Hello world!&lt;/anchored-heading&gt;


&lt;h1 v-if=&quot;level === 1&quot;&gt;
    &lt;slot&gt;&lt;/slot&gt;
  &lt;/h1&gt;

</code></pre>
<p>我们来尝试使用 render 函数替代一大串v-if与v-else-if</p>
<pre><code>Vue.component('anchored-heading', {
  render: function (createElement) {
    return createElement(
      'h' + this.level,   // 标签名称
      this.$slots.default // 子节点数组
    )
  },
  props: {
    level: {
      type: Number,
      required: true
    }
  }
})
</code></pre>
<p>向组件中传递不带 v-slot 指令的子节点时，比如 anchored-heading 中的 Hello world!，这些子节点被存储在组件实例中的 $slots.default 中。</p>
<h3 id="vmslots">vm.$slots</h3>
<p>用来访问被插槽分发的内容。每个具名插槽有其相应的 property (例如：<strong>v-slot:foo 中的内容将会在 vm.$slots.foo 中被找到)</strong>。default property 包括了所有没有被包含在具名插槽中的节点，或 v-slot:default 的内容。</p>
<h3 id="vmscopedslots">vm.$scopedSlots</h3>
<p>用来访问作用域插槽。对于包括 默认 slot 在内的每一个插槽，该对象都包含一个返回相应 VNode 的函数。</p>
<p>作用域插槽函数现在保证返回一个 VNode 数组，除非在返回值无效的情况下返回 undefined。</p>
<p>所有的 <code>$slots</code> 现在都会作为函数暴露在 <code>$scopedSlots</code> 中。如果你在使用渲染函数，不论当前插槽是否带有作用域，我们都推荐始终通过 <code>$scopedSlots</code> 访问它们。这不仅仅使得在未来添加作用域变得简单，也可以让你最终轻松迁移到所有插槽都是函数的 Vue 3。</p>
<h2 id="节点-树以及虚拟-dom超级重点哦">节点、树以及虚拟 DOM（超级重点哦！）</h2>
<pre><code>&lt;div&gt;
  &lt;h1&gt;My title&lt;/h1&gt;
  Some text content
  &lt;!-- TODO: Add tagline --&gt;
&lt;/div&gt;
</code></pre>
<p>对应的DOM节点树</p>
<p><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911111904.png" alt="DOM节点树" loading="lazy"><br>
每个元素都是一个节点。每段文字也是一个节点。甚至注释也都是节点。一个节点就是页面的一个部分。就像家谱树一样，每个节点都可以有孩子节点 (也就是说每个部分可以包含其它的一些部分)。</p>
<p>高效地更新所有这些节点会是比较困难的，不过所幸你不必手动完成这个工作。你只需要告诉 Vue 你希望页面上的 HTML 是什么</p>
<pre><code>这可以是在一个模板里
&lt;h1&gt;{{ blogTitle }}&lt;/h1&gt;

或者一个渲染函数里

render: function (createElement) {
  return createElement('h1', this.blogTitle)
}
</code></pre>
<p>在这两种情况下，Vue 都会自动保持页面的更新，即便 blogTitle 发生了改变。</p>
<h3 id="虚拟dom">虚拟DOM</h3>
<p>Vue 通过建立一个虚拟 DOM 来追踪自己要如何改变真实 DOM</p>
<p>上面的return到底会返回什么呢？</p>
<p>其实不是一个实际的 DOM 元素。它更准确的名字可能是 createNodeDescription，因为它所包含的信息<strong>会告诉 Vue 页面上需要渲染什么样的节点，包括及其子节点的描述信息</strong>。我们把这样的节点描述为“<strong>虚拟节点</strong> (virtual node)”，也常简写它为“VNode”。“虚拟 DOM”是我们对由 Vue 组件树建立起来的整个 VNode 树的称呼。</p>
<h2 id="createelement-参数">createElement 参数</h2>
<pre><code>// @returns {VNode}
createElement(
  // {String | Object | Function}
  // 一个 HTML 标签名、组件选项对象，或者
  // resolve 了上述任何一种的一个 async 函数。必填项。
  'div',

  // {Object}
  // 一个与模板中 attribute 对应的数据对象。可选。
  {
    // (详情见下一节)
  },

  // {String | Array}
  // 子级虚拟节点 (VNodes)，由 `createElement()` 构建而成，
  // 也可以使用字符串来生成“文本虚拟节点”。可选。
  [
    '先写一些文字',
    createElement('h1', '一则头条'),
    createElement(MyComponent, {
      props: {
        someProp: 'foobar'
      }
    })
  ]
)
</code></pre>
<h3 id="深入数据对象">深入数据对象</h3>
<p>正如 v-bind:class 和 v-bind:style 在模板语法中会被特别对待一样，它们在 VNode 数据对象中也有对应的顶层字段。</p>
<p>该对象也允许你绑定普通的 HTML attribute，也允许绑定如 innerHTML 这样的 DOM property (这会覆盖 v-html 指令)</p>
<blockquote>
<p>这块详见官方文档吧，我也不是很搞得懂，后面再做补充好了</p>
</blockquote>
<h3 id="约束">约束</h3>
<p>VNode 必须唯一，组件树中的所有 VNode 必须是唯一的。</p>
<p>这意味着，下面的渲染函数是不合法的</p>
<pre><code>render: function (createElement) {
  var myParagraphVNode = createElement('p', 'hi')
  return createElement('div', [
    // 错误 - 重复的 VNode
    myParagraphVNode, myParagraphVNode
  ])
}
</code></pre>
<p>如果你真的需要重复很多次的元素/组件，你可以使用工厂函数来实现。</p>
<pre><code>render: function (createElement) {
  return createElement('div',
    Array.apply(null, { length: 20 }).map(function () {
      return createElement('p', 'hi')
    })
  )
}
</code></pre>
<h2 id="使用-javascript-代替模板功能">使用 JavaScript 代替模板功能</h2>
<h3 id="v-if和v-for">v-if和v-for</h3>
<p>只要在原生的 JavaScript 中可以轻松完成的操作，Vue 的渲染函数就<strong>不会提供专有的替代方法</strong>。比如，在模板中使用的 v-if 和 v-for</p>
<pre><code>&lt;ul v-if=&quot;items.length&quot;&gt;
  &lt;li v-for=&quot;item in items&quot;&gt;{{ item.name }}&lt;/li&gt;
&lt;/ul&gt;
&lt;p v-else&gt;No items found.&lt;/p&gt;

//这些都可以在渲染函数中用 JavaScript 的 if/else 和 map 来重写
props: ['items'],
render: function (createElement) {
  if (this.items.length) {
    return createElement('ul', this.items.map(function (item) {
      return createElement('li', item.name)
    }))
  } else {
    return createElement('p', 'No items found.')
  }
}
</code></pre>
<h3 id="v-model打个重点标记">v-model（打个重点标记）</h3>
<p>渲染函数中没有与 v-model 的直接对应——你必须自己实现相应的逻辑。</p>
<pre><code>props: ['value'],
render: function (createElement) {
  var self = this
  return createElement('input', {
    domProps: {
      value: self.value
    },
    on: {
      input: function (event) {
        self.$emit('input', event.target.value)
      }
    }
  })
}
</code></pre>
<h3 id="事件-按键修饰符">事件 &amp; 按键修饰符</h3>
<p>对于 .passive、.capture 和 .once 这些事件修饰符，Vue 提供了相应的前缀可以用于 on</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/7/20/1736cdf2d12af7ce?w=262&amp;h=242&amp;f=png&amp;s=10196" alt="Vue前缀" loading="lazy"></figure>
<pre><code>on: {
  '!click': this.doThisInCapturingMode,
  '~keyup': this.doThisOnce,
  '~!mouseover': this.doThisOnceInCapturingMode
}
</code></pre>
<p>对于所有其它的修饰符，私有前缀都不是必须的，因为你可以在事件处理函数中使用事件方法（有种源码重现的味儿了）</p>
<figure data-type="image" tabindex="2"><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911111914.png" alt="处理函数等价操作" loading="lazy"></figure>
<h3 id="插槽">插槽</h3>
<p>你可以通过 this.$slots 访问静态插槽的内容，每个插槽都是一个 VNode 数组</p>
<pre><code>render: function (createElement) {
  // `&lt;div&gt;&lt;slot&gt;&lt;/slot&gt;&lt;/div&gt;`
  return createElement('div', this.$slots.default)
}
</code></pre>
<p>也可以通过 this.$scopedSlots 访问作用域插槽，每个作用域插槽都是一个返回若干 VNode 的函数</p>
<pre><code>props: ['message'],
render: function (createElement) {
  // `&lt;div&gt;&lt;slot :text=&quot;message&quot;&gt;&lt;/slot&gt;&lt;/div&gt;`
  return createElement('div', [
    this.$scopedSlots.default({
      text: this.message
    })
  ])
}
</code></pre>
<p>如果要用渲染函数<strong>向子组件中传递作用域插槽</strong>，可以利用 VNode 数据对象中的 scopedSlots 字段</p>
<pre><code>render: function (createElement) {
  // `&lt;div&gt;&lt;child v-slot=&quot;props&quot;&gt;&lt;span&gt;{{ props.text }}&lt;/span&gt;&lt;/child&gt;&lt;/div&gt;`
  return createElement('div', [
    createElement('child', {
      // 在数据对象中传递 `scopedSlots`
      // 格式为 { name: props =&gt; VNode | Array&lt;VNode&gt; }
      scopedSlots: {
        default: function (props) {
          return createElement('span', props.text)//？？需要demo进行验证
        }
      }
    })
  ])
}
</code></pre>
<p>render这块有点搞晕了，等冲一遍react再回来写好了。一般来讲用HTML模板就足足够够了。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Vue学习笔记9-深入了解组件3-动态组件、异步组件、边界处理]]></title>
        <id>https://giriawsh.github.io/post/vue-xue-xi-bi-ji-9-shen-ru-liao-jie-zu-jian-3-dong-tai-zu-jian-yi-bu-zu-jian-bian-jie-chu-li/</id>
        <link href="https://giriawsh.github.io/post/vue-xue-xi-bi-ji-9-shen-ru-liao-jie-zu-jian-3-dong-tai-zu-jian-yi-bu-zu-jian-bian-jie-chu-li/">
        </link>
        <updated>2020-09-10T07:20:19.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>这块基本参照官方文档与API文档进行知识点整理。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>这块基本参照官方文档与API文档进行知识点整理。</p>
<!-- more -->
<h1 id="动态组件">动态组件</h1>
<p>我们之前曾经在一个多标签的界面中使用 is attribute 来切换不同的组件（在学习笔记4中）</p>
<pre><code>&lt;component v-bind:is=&quot;currentTabComponent&quot;&gt;&lt;/component&gt;
</code></pre>
<p>当在这些组件之间切换的时候，你有时会想保持这些组件的状态，以避免反复重渲染导致的性能问题。</p>
<p>每次切换新标签的时候，Vue 都创建了一个新的 currentTabComponent 实例。导致如果切换选项卡，不会保存之前选项卡中的选择结果。</p>
<p>重新创建动态组件的行为通常是非常有用的，但是在这个案例中，我们更希望那些标签的组件实例能够被在它们第一次被创建的时候缓存下来。为了解决这个问题，我们可以用一个 <code>&lt;keep-alive&gt;</code> 元素将其动态组件包裹起来。</p>
<pre><code>&lt;!-- 失活的组件将会被缓存！--&gt;
&lt;keep-alive&gt;
  &lt;component v-bind:is=&quot;currentTabComponent&quot;&gt;&lt;/component&gt;
&lt;/keep-alive&gt;
</code></pre>
<p>现在这个 Posts 标签保持了它的状态 (被选中的文章) 甚至当它未被渲染时也是如此。</p>
<blockquote>
<p>注意这个<code>&lt;keep-alive&gt;</code>要求被切换到的组件都有自己的名字，不论是通过组件的 name 选项还是局部/全局注册。它自身不会渲染一个 DOM 元素，也不会出现在组件的父组件链中。</p>
</blockquote>
<p>主要用于保留组件状态或避免重新渲染。</p>
<pre><code>&lt;!-- 基本 --&gt;
&lt;keep-alive&gt;
  &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;
&lt;/keep-alive&gt;

&lt;!-- 多个条件判断的子组件 --&gt;
&lt;keep-alive&gt;
  &lt;comp-a v-if=&quot;a &gt; 1&quot;&gt;&lt;/comp-a&gt;
  &lt;comp-b v-else&gt;&lt;/comp-b&gt;
&lt;/keep-alive&gt;

&lt;!-- 和 `&lt;transition&gt;` 一起使用 --&gt;
&lt;transition&gt;
  &lt;keep-alive&gt;
    &lt;component :is=&quot;view&quot;&gt;&lt;/component&gt;
  &lt;/keep-alive&gt;
&lt;/transition&gt;
</code></pre>
<p>注意，<code>&lt;keep-alive&gt;</code> 是用在其一个直属的子组件被开关的情形。如果你在其中有 v-for 则不会工作。如果有上述的多个条件性的子元素，<code>&lt;keep-alive&gt;</code> 要求同时只有一个子元素被渲染。</p>
<h1 id="异步组件">异步组件</h1>
<p>在大型应用中，我们可能需要将应用分割成小一些的代码块，并且只在需要的时候才从服务器加载一个模块。（公司实习中这样的问题更为常见了）</p>
<p>Vue 允许你以一个工厂函数的方式定义你的组件，这个工厂函数会异步解析你的组件定义。Vue 只有在这个组件需要被渲染的时候才会触发该工厂函数，且会把结果缓存起来供未来重渲染。</p>
<pre><code>Vue.component('async-example', function (resolve, reject) {
  setTimeout(function () {
    // 向 `resolve` 回调传递组件定义
    resolve({
      template: '&lt;div&gt;I am async!&lt;/div&gt;'
    })
  }, 1000)
})
</code></pre>
<p>如你所见，这个工厂函数会收到一个 resolve 回调，这个回调函数会在你从服务器得到组件定义的时候被调用。你也可以调用 reject(reason) 来表示加载失败。这里的 setTimeout 是为了演示用的，如何获取组件取决于你自己。一个推荐的做法是将异步组件和 webpack 的 code-splitting 功能一起配合使用。</p>
<pre><code>Vue.component('async-webpack-example', function (resolve) {
  // 这个特殊的 `require` 语法将会告诉 webpack
  // 自动将你的构建代码切割成多个包，这些包
  // 会通过 Ajax 请求加载
  require(['./my-async-component'], resolve)
})
</code></pre>
<p>你也可以在工厂函数中返回一个 Promise，所以把 webpack 2 和 ES2015 语法加在一起，我们可以这样使用动态导入</p>
<pre><code>Vue.component(
  'async-webpack-example',
  // 这个动态导入会返回一个 `Promise` 对象。
  () =&gt; import('./my-async-component')
)

当使用局部注册的时候，你也可以直接提供一个返回 Promise 的函数

new Vue({
  // ...
  components: {
    'my-component': () =&gt; import('./my-async-component')
  }
})
</code></pre>
<h3 id="处理加载状态">处理加载状态</h3>
<p>异步组件工厂函数也可以返回一个如下格式的对象</p>
<pre><code>const AsyncComponent = () =&gt; ({
  // 需要加载的组件 (应该是一个 `Promise` 对象)
  component: import('./MyComponent.vue'),
  // 异步组件加载时使用的组件
  loading: LoadingComponent,
  // 加载失败时使用的组件
  error: ErrorComponent,
  // 展示加载时组件的延时时间。默认值是 200 (毫秒)
  delay: 200,
  // 如果提供了超时时间且组件加载也超时了，
  // 则使用加载失败时使用的组件。默认值是：`Infinity`
  timeout: 3000
})
</code></pre>
<h1 id="处理边界情况">处理边界情况</h1>
<h2 id="访问元素与组件">访问元素与组件</h2>
<p>在绝大多数情况下，我们<strong>最好不要触达另一个组件实例内部或手动操作 DOM 元素</strong>。</p>
<h3 id="访问根实例">访问根实例</h3>
<p>在每个 new Vue 实例的子组件中，其根实例可以通过 $root property 进行访问。</p>
<pre><code>// Vue 根实例
new Vue({
  data: {
    foo: 1
  },
  computed: {
    bar: function () { /* ... */ }
  },
  methods: {
    baz: function () { /* ... */ }
  }
})

所有的子组件都可以将这个实例作为一个全局 store 来访问或使用。

// 获取根组件的数据
this.$root.foo

// 写入根组件的数据
this.$root.foo = 2

// 访问根组件的计算属性
this.$root.bar

// 调用根组件的方法
this.$root.baz()
</code></pre>
<blockquote>
<p>这个模式扩展到中大型应用来说就不然了。因此在绝大多数情况下，我们强烈推荐使用 Vuex 来管理应用的状态。</p>
</blockquote>
<h3 id="访问父级组件实例">访问父级组件实例</h3>
<p>和 <code>$root</code> 类似，<code>$parent</code> property 可以用来从一个子组件访问父组件的实例。它提供了一种机会，可以在后期随时触达父级组件，以替代将数据以 prop 的方式传入子组件的方式。</p>
<blockquote>
<p>在绝大多数情况下，触达父级组件会使得你的应用更难调试和理解，尤其是当你变更了父级组件的数据的时候。当我们稍后回看那个组件的时候，很难找出那个变更是从哪里发起的。慎用！慎用！慎用！</p>
</blockquote>
<p>在一些可能适当的时候，你需要特别地共享一些组件库。举个例子，在和 JavaScript API 进行交互而不渲染 HTML 的抽象组件内，诸如这些假设性的 Google 地图组件一样</p>
<pre><code>&lt;google-map&gt;
  &lt;google-map-markers v-bind:places=&quot;iceCreamShops&quot;&gt;&lt;/google-map-markers&gt;
&lt;/google-map&gt;
</code></pre>
<p>这个 <code>&lt;google-map&gt;</code> 组件可以定义一个 map property，所有的子组件都需要访问它。在这种情况下 <code>&lt;google-map-markers&gt;</code>可能想要通过类似 this.$parent.getMap 的方式访问那个地图，以便为其添加一组标记。</p>
<p>通过这种模式构建出来的那个组件的内部仍然是容易出现问题的。</p>
<p>设想一下我们添加一个新的 <code>&lt;google-map-region&gt;</code> 组件，当 <code>&lt;google-map-markers&gt;</code> 在其内部出现的时候，只会渲染那个区域内的标记。</p>
<pre><code>&lt;google-map&gt;
  &lt;google-map-region v-bind:shape=&quot;cityBoundaries&quot;&gt;
    &lt;google-map-markers v-bind:places=&quot;iceCreamShops&quot;&gt;&lt;/google-map-markers&gt;
  &lt;/google-map-region&gt;
&lt;/google-map&gt;
</code></pre>
<p>那么在 <code>&lt;google-map-markers&gt;</code> 内部你可能发现自己需要一些类似这样的 hack：</p>
<pre><code>var map = this.$parent.map || this.$parent.$parent.map
</code></pre>
<p>很快它就会失控。我们针对需要向任意更深层级的组件提供上下文信息时推荐<strong>依赖注入</strong>。</p>
<h3 id="依赖注入">依赖注入</h3>
<p>使用 $parent property 无法很好的扩展到更深层级的嵌套组件上。这也是依赖注入的用武之地，它用到了两个新的实例选项：provide 和 inject。<strong>使用它们子组件就可以使用父组件的数据和方法辣！</strong></p>
<ul>
<li>provide 选项允许我们指定我们想要<strong>提供给后代组件的数据/方法</strong></li>
</ul>
<p>在这个例子中，就是 <google-map> 内部的 getMap 方法</p>
<pre><code>provide: function () {
  return {
    getMap: this.getMap
  }
}
</code></pre>
<ul>
<li>然后在任何后代组件里，我们都可以使用 inject 选项来<strong>接收指定的我们想要添加在这个实例上的 property</strong></li>
</ul>
<pre><code>inject: ['getMap']
</code></pre>
<p>完整demo代码：</p>
<pre><code> &lt;div id=&quot;app&quot;&gt;
      &lt;google-map&gt;
        &lt;google-map-marker v-bind:places=&quot;vueConfCities&quot;&gt;&lt;/google-map-marker&gt;
        &lt;!--&gt;需要访问父级的map property&lt;--&gt;
      &lt;/google-map&gt;
    &lt;/div&gt;

    &lt;script&gt;
      Vue.component(&quot;google-map&quot;, {
        provide: function() {
          return {
            getMap: this.getMap//我们指定我们想要提供给后代组件的方法
          };
        },
        data: function() {
          return {
            map: null
          };
        },
        mounted: function() {
          this.map = new google.maps.Map(this.$el, {
            center: { lat: 0, lng: 0 },
            zoom: 1
          });
        },
        methods: {
          getMap: function(found) {
            var vm = this;
            function checkForMap() {
              if (vm.map) {
                found(vm.map);//得到map数据
              } else {
                setTimeout(checkForMap, 50);
              }
            }
            checkForMap();
          }
        },
        template: '&lt;div class=&quot;map&quot;&gt;&lt;slot&gt;&lt;/slot&gt;&lt;/div&gt;'
      });

      Vue.component(&quot;google-map-marker&quot;, {
        inject: [&quot;getMap&quot;],//使用 inject 选项来接收指定的我们想要添加在这个实例上的 property
        props: [&quot;places&quot;],
        created: function() {
          var vm = this;
          vm.getMap(function(map) {
            vm.places.forEach(function(place) {
              new google.maps.Marker({
                position: place.position,
                map: map
              });
            });
          });
        },
        render(h) {
          return null;
        }
      });

      new Vue({
        el: &quot;#app&quot;,
        data: {
          vueConfCities: [
            {
              name: &quot;Wrocław&quot;,
              position: {
                lat: 51.107885,
                lng: 17.038538
              }
            },
            {
              name: &quot;New Orleans&quot;,
              position: {
                lat: 29.951066,
                lng: -90.071532
              }
            }
          ]
        }
      });
    &lt;/script&gt;
</code></pre>
<h3 id="访问子组件实例或子元素">访问子组件实例或子元素</h3>
<p>尽管存在 prop 和事件，有的时候你仍可能需要在 JavaScript 里<strong>直接访问一个子组件</strong>。为了达到这个目的，你可以通过 ref 这个 attribute 为子组件赋予一个 ID 引用。</p>
<pre><code>&lt;base-input ref=&quot;usernameInput&quot;&gt;&lt;/base-input&gt;

现在在你已经定义了这个 ref 的组件里，你可以使用：

this.$refs.usernameInput  来访问这个 &lt;base-input&gt; 实例
</code></pre>
<p>假如我们的目的是从一个父级组件聚焦一个input输入框，该 <code>&lt;base-input&gt;</code> 组件也可以使用一个类似的 ref 提供对内部这个指定元素的访问。</p>
<pre><code>例如base-input这个组件内部长这样：
&lt;input ref=&quot;input&quot;&gt;

也可以通过其父级组件定义方法：
methods: {
  // 定义在子级中
  // 用来从父级组件聚焦输入框
  focus: function () {
    this.$refs.input.focus()
  }
}
</code></pre>
<p>这样就允许父级组件通过下面的代码聚焦 <code>&lt;base-input&gt;</code> 里的输入框：</p>
<pre><code>this.$refs.usernameInput.focus()
</code></pre>
<p>当 ref 和 v-for 一起使用的时候，你得到的 ref 将会是一个包含了对应数据源的这些子组件的数组。</p>
<blockquote>
<p>注意！<code>$refs</code> 只会在组件渲染完成之后生效，并且它们不是响应式的。这仅作为一个用于直接操作子组件的“逃生舱”——你应该避免在模板或计算属性中访问 <code>$refs</code>。</p>
</blockquote>
<p>相比 $parent 来说，这个用法可以让我们在任意后代组件中访问 getMap，而不需要暴露整个 <code>&lt;google-map&gt;</code> 实例。这允许我们更好的持续研发该组件，而不需要担心我们可能会改变/移除一些子组件依赖的东西。同时这些组件之间的接口是始终明确定义的，就和 props 一样。</p>
<blockquote>
<p>然而，依赖注入还是有负面影响的。它将你应用程序中的组件与它们当前的组织方式耦合起来，使重构变得更加困难。同时所提供的 property 是非响应式的。这是出于设计的考虑，因为使用它们来创建一个中心化规模化的数据跟使用 $root做这件事都是不够好的。如果你想要共享的这个 property 是你的应用特有的，而不是通用化的，或者如果你想在祖先组件中更新所提供的数据，那么这意味着你可能需要换用一个像 Vuex 这样真正的状态管理方案了。</p>
</blockquote>
<p>有关Vuex的状态管理学习笔记待填坑。</p>
<p>详细官方API文档可看：<a href="https://cn.vuejs.org/v2/api/#provide-inject">provide-inject API官方文档说明</a></p>
<h2 id="程序化的事件侦听器">程序化的事件侦听器</h2>
<p>$emit 可以被 v-on 侦听，但是 Vue 实例同时在其事件接口中提供了其它的方法。我们可以：</p>
<ul>
<li>通过 $on(eventName, eventHandler) 侦听一个事件</li>
<li>通过 $once(eventName, eventHandler) 一次性侦听一个事件</li>
<li>通过 $off(eventName, eventHandler) 停止侦听一个事件</li>
</ul>
<p>当你需要在一个组件实例上手动侦听事件时，它们是派得上用场的。它们也可以用于代码组织工具。</p>
<pre><code>一种集成一个第三方库的模式
// 一次性将这个日期选择器附加到一个输入框上
// 它会被挂载到 DOM 上。
mounted: function () {
  // Pikaday 是一个第三方日期选择器的库
  this.picker = new Pikaday({
    field: this.$refs.input,
    format: 'YYYY-MM-DD'
  })
},
// 在组件被销毁之前，
// 也销毁这个日期选择器。
beforeDestroy: function () {
  this.picker.destroy()
}
</code></pre>
<p>这里有两个潜在的问题：</p>
<ol>
<li>它需要在这个组件实例中保存这个 picker，如果可以的话最好只有生命周期钩子可以访问到它。这并不算严重的问题，但是它可以被视为杂物。</li>
<li>我们的建立代码独立于我们的清理代码，这使得我们比较难于程序化地清理我们建立的所有东西。</li>
</ol>
<p>应该通过一个程序化的侦听器解决这两个问题：</p>
<pre><code>mounted: function () {
  var picker = new Pikaday({
    field: this.$refs.input,
    format: 'YYYY-MM-DD'
  })

  this.$once('hook:beforeDestroy', function () {
    picker.destroy()
  })
}
</code></pre>
<p>使用了这个策略，我甚至可以让多个输入框元素同时使用不同的 Pikaday，每个新的实例都程序化地在后期清理它自己。</p>
<pre><code>mounted: function () {
  this.attachDatepicker('startDateInput')
  this.attachDatepicker('endDateInput')
},
methods: {
  attachDatepicker: function (refName) {
    var picker = new Pikaday({
      field: this.$refs[refName],
      format: 'YYYY-MM-DD'
    })

    this.$once('hook:beforeDestroy', function () {
      picker.destroy()//一次性侦听一个事件
    })
  }
}
</code></pre>
<p>在这个例子中，我们推荐创建一个可复用的<code>&lt;input-datepicker&gt;</code> 组件</p>
<h2 id="循环引用">循环引用</h2>
<h3 id="递归组件">递归组件</h3>
<p>组件是可以在它们自己的模板中调用自身的。不过它们<strong>只能通过 name 选项</strong>来做这件事</p>
<pre><code>name: 'unique-name-of-my-component'
</code></pre>
<p>当你使用 Vue.component 全局注册一个组件时，这个全局的 ID 会自动设置为该组件的 name 选项。</p>
<p>稍有不慎，递归组件就可能导致无限循环</p>
<pre><code>name: 'stack-overflow',
template: '&lt;div&gt;&lt;stack-overflow&gt;&lt;/stack-overflow&gt;&lt;/div&gt;'
</code></pre>
<p>类似上述的组件将会导致“max stack size exceeded”错误，所以<strong>请确保递归调用是条件性</strong>的 (例如使用一个<strong>最终会得到 false 的 v-if</strong>)</p>
<h3 id="组件之间的循环引用">组件之间的循环引用</h3>
<p>假设你需要构建一个文件目录树，像访达或资源管理器那样的。你可能有一个 <tree-folder> 组件。</p>
<pre><code>外部 &lt;tree-folder&gt; 组件模板：
&lt;p&gt;
  &lt;span&gt;{{ folder.name }}&lt;/span&gt;
  &lt;tree-folder-contents :children=&quot;folder.children&quot;/&gt;
&lt;/p&gt;

还有一个 &lt;tree-folder-contents&gt; 组件，模板是这样的
&lt;ul&gt;
  &lt;li v-for=&quot;child in children&quot;&gt;
    &lt;tree-folder v-if=&quot;child.children&quot; :folder=&quot;child&quot;/&gt;
    &lt;span v-else&gt;{{ child.name }}&lt;/span&gt;
  &lt;/li&gt;
&lt;/ul&gt;
</code></pre>
<p>当你仔细观察的时候，你会发现这些组件在渲染树中<strong>互为对方的后代和祖先</strong>。</p>
<ul>
<li>当通过 Vue.component 全局注册组件的时候，这个悖论会被自动解开。</li>
<li>如果你使用一个模块系统依赖/导入组件，例如通过 webpack 或 Browserify，你会遇到<code>Failed to mount component: template or render function not defined.</code>循环依赖报错</li>
</ul>
<p>为了解决这个问题，我们需要给模块系统一个点，在那里“A 反正是需要 B 的，但是我们不需要先解析 B。”</p>
<p>把 <tree-folder> 组件设为了那个点。我们知道那个产生悖论的子组件是 <tree-folder-contents> 组件，所以我们会<strong>等到生命周期钩子 beforeCreate 时去注册</strong>它</p>
<pre><code>beforeCreate: function () {
  this.$options.components.TreeFolderContents = require('./tree-folder-contents.vue').default
}
</code></pre>
<p>或者，在本地注册组件的时候，你可以使用 <strong>webpack 的异步 import</strong>：</p>
<pre><code>components: {
  TreeFolderContents: () =&gt; import('./tree-folder-contents.vue')
}
</code></pre>
<p>这样问题就解决了！</p>
<h2 id="模板定义的替代品">模板定义的替代品</h2>
<h3 id="内联模板">内联模板</h3>
<p>当 inline-template 这个特殊的 attribute 出现在一个子组件上时，<strong>这个组件将会使用其里面的内容作为模板，而不是将其作为被分发的内容</strong>。这使得模板的撰写工作更加灵活。</p>
<pre><code>&lt;my-component inline-template&gt;
  &lt;div&gt;
    &lt;p&gt;These are compiled as the component's own template.&lt;/p&gt;
    &lt;p&gt;Not parent's transclusion content.&lt;/p&gt;
  &lt;/div&gt;
&lt;/my-component&gt;
俺自己就是模板
</code></pre>
<p>内联模板需要定义在 Vue 所属的 DOM 元素内。</p>
<blockquote>
<p>不过，inline-template 会让模板的作用域变得更加难以理解。所以作为最佳实践，<strong>请在组件内优先选择 template 选项或 .vue 文件里的一个 <code>&lt;template&gt;</code> 元素来定义模板。</strong></p>
</blockquote>
<h3 id="x-template">X-Template</h3>
<p>另一个定义模板的方式是在一个 <code>&lt;script&gt;</code>元素中，并为其带上 text/x-template 的类型，然后通过一个 id 将模板引用过去。</p>
<pre><code>&lt;script type=&quot;text/x-template&quot; id=&quot;hello-world-template&quot;&gt;
  &lt;p&gt;Hello hello hello&lt;/p&gt;
&lt;/script&gt;

Vue.component('hello-world', {
  template: '#hello-world-template'
})
</code></pre>
<p>x-template 需要定义在 Vue 所属的 DOM 元素外。</p>
<blockquote>
<p>这些可以用于模板特别大的 demo 或极小型的应用，但是其它情况下请避免使用，因为这会将模板和该组件的其它定义分离开。</p>
</blockquote>
<h2 id="控制更新">控制更新</h2>
<p>由于拥有Vue 的响应式系统，它始终知道何时进行更新 (如果你用对了的话)。</p>
<p>不过还是有一些边界情况，你想要强制更新，尽管表面上看响应式的数据没有发生改变。也有一些情况是你想阻止不必要的更新。</p>
<h3 id="强制更新">强制更新</h3>
<p>如果你发现你自己需要在 Vue 中做一次强制更新，99.9% 的情况，是你在某个地方做错了事。（艹太真实了）</p>
<p>你可能还没有留意到数组或对象的变更检测注意事项，或者你可能依赖了一个未被 Vue 的响应式系统追踪的状态。</p>
<p>然而，如果你已经做到了上述的事项仍然发现在极少数的情况下需要手动强制更新，那么你可以通过 $forceUpdate 来做这件事。</p>
<h3 id="通过-v-once-创建低开销的静态组件">通过 v-once 创建低开销的静态组件</h3>
<p>渲染普通的 HTML 元素在 Vue 中是非常快速的，但有的时候你可能有一个组件，这个组件包含了大量静态内容。在这种情况下，你可以在根元素上添加 v-once attribute 以确保这些内容只计算一次然后缓存起来。</p>
<pre><code>Vue.component('terms-of-service', {
  template: `
    &lt;div v-once&gt;
      &lt;h1&gt;Terms of Service&lt;/h1&gt;
      ... a lot of static content ...
    &lt;/div&gt;
  `
})
</code></pre>
<p>试着不要过度使用这个模式。当你需要渲染大量静态内容时，极少数的情况下它会给你带来便利，除非你非常留意渲染变慢了，不然它完全是没有必要的——再加上它在后期会带来很多困惑。例如，设想另一个开发者并不熟悉 v-once 或漏看了它在模板中，他们可能会花很多个小时去找出模板为什么无法正确更新。变得难以维护。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Vue学习笔记8-深入了解组件2-插槽]]></title>
        <id>https://giriawsh.github.io/post/vue-xue-xi-bi-ji-8-shen-ru-liao-jie-zu-jian-2-cha-cao/</id>
        <link href="https://giriawsh.github.io/post/vue-xue-xi-bi-ji-8-shen-ru-liao-jie-zu-jian-2-cha-cao/">
        </link>
        <updated>2020-09-10T07:19:14.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>结合官方文档、废弃用法文档、知乎、掘金等资料进行插槽知识点整理。</p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">🏃前言</h1>
<p>结合官方文档、废弃用法文档、知乎、掘金等资料进行插槽知识点整理。</p>
<!-- more -->
<h1 id="插槽">插槽</h1>
<blockquote>
<p>在 2.6.0 中，我们为具名插槽和作用域插槽引入了一个新的统一的语法 (即 v-slot 指令)。它取代了 slot 和 slot-scope 这两个目前已被废弃但未被移除且仍在文档中的 attribute。</p>
</blockquote>
<h2 id="插槽是什么">插槽是什么</h2>
<p>slot，也称插槽，可以类比为插卡式的FC游戏机，<strong>游戏机（子组件）暴露卡槽（插槽）让用户插入不同的游戏磁条（自定义内容）</strong>，游戏机会读取并加载磁条里的游戏。</p>
<p>Vue的slot，是组件的一块HTML模版，这块模版由使用组件者即父组件提供。可以说是<strong>子组件暴露的一个让父组件传入自定义内容的接口</strong>。</p>
<p><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911111939.png" alt="游戏机" loading="lazy"><br>
转载自掘金：<a href="https://juejin.im/post/6844903817746628615">slots详解</a></p>
<h2 id="为什么需要引入插槽">为什么需要引入插槽</h2>
<p>举个例子，比如我要实现一个这样的 Alert 组件：</p>
<p><img src="https://giriawsh-bucket.oss-cn-beijing.aliyuncs.com/img/20200911111945.png" alt="插槽例子" loading="lazy"><br>
它会有默认的内容和样式，比如第一行的 Default 我们只需要使用：</p>
<pre><code class="language-html">&lt;alert&gt;&lt;/alert&gt;
</code></pre>
<p>当需要换用不同的样式与内容时，我们希望只需要这样调用：</p>
<pre><code class="language-html">&lt;alert type=&quot;success&quot;&gt;
    &lt;strong&gt;Success!&lt;/strong&gt; Looks good to me!
&lt;/alert&gt;
</code></pre>
<p>要实现这个功能就可以使用slots</p>
<figure data-type="image" tabindex="1"><img src="https://user-gold-cdn.xitu.io/2020/7/20/17368104fde76574?w=643&amp;h=532&amp;f=png&amp;s=28705" alt="代码1" loading="lazy"></figure>
<p><img src="https://user-gold-cdn.xitu.io/2020/7/20/17368108b5ae8ca2?w=643&amp;h=574&amp;f=png&amp;s=27001" alt="代码2" loading="lazy"><br>
当外部调用<code>&lt;slot&gt;</code>标签内容的时候，如果外部调用没有提供内容，则使用自己默认的内容，否则提供外部的内容。</p>
<p>转载自知乎：<a href="https://www.zhihu.com/question/37548226/answer/609289491">如何理解Vue.js的组件中的slot?</a></p>
<h2 id="插槽内容">插槽内容</h2>
<p>Vue 实现了一套内容分发的 API，将 <slot> 元素作为承载分发内容的出口。</p>
<pre><code class="language-html">&lt;navigation-link url=&quot;/profile&quot;&gt;
  Your Profile
&lt;/navigation-link&gt;

在模板内部：
&lt;a
  v-bind:href=&quot;url&quot;
  class=&quot;nav-link&quot;
&gt;
  &lt;slot&gt;&lt;/slot&gt;
&lt;/a&gt;
</code></pre>
<p>当组件渲染的时候，<code>&lt;slot&gt;&lt;/slot&gt;</code> 将会被替换为“Your Profile”。插槽内可以包含任何模板代码，包括 HTML、甚至其它的组件。</p>
<p>如果 <code>&lt;navigation-link</code>&gt; 的 template 中没有包含一个 <code>&lt;slot&gt;</code> 元素，则该组件起始标签和结束标签之间的任何内容都会被抛弃。</p>
<h2 id="编译作用域">编译作用域</h2>
<p>当你想在一个插槽中使用数据时</p>
<pre><code class="language-html">&lt;navigation-link url=&quot;/profile&quot;&gt;
  Logged in as {{ user.name }}
&lt;/navigation-link&gt;
</code></pre>
<p>该插槽跟模板的其它地方一样可以访问相同的实例 property (也就是相同的“作用域”)，而不能访问 <code>&lt;navigation-link&gt;</code> 的作用域</p>
<p><strong>父级模板里的所有内容都是在父级作用域中编译的；子模板里的所有内容都是在子作用域中编译的。</strong></p>
<h2 id="后备内容">后备内容</h2>
<p>有时为一个插槽设置具体的后备 (也就是默认的) 内容是很有用的，它只会在没有提供内容的时候被渲染。</p>
<p>我们可能希望<code>&lt;button&gt;</code> 内绝大多数情况下都渲染文本“Submit”。为了将“Submit”作为后备内容，我们可以将它放在 <code>&lt;slot&gt;</code> 标签内</p>
<pre><code class="language-html">&lt;button type=&quot;submit&quot;&gt;
  &lt;slot&gt;Submit&lt;/slot&gt;
&lt;/button&gt;
</code></pre>
<h2 id="具名插槽">具名插槽</h2>
<p>有时我们需要多个插槽。</p>
<p>对于这样的情况，<code>&lt;slot&gt;</code> 元素有一个特殊的 attribute：name。这个 attribute 可以用来定义额外的插槽</p>
<pre><code class="language-html">&lt;div class=&quot;container&quot;&gt;
  &lt;header&gt;
    &lt;slot name=&quot;header&quot;&gt;&lt;/slot&gt;
  &lt;/header&gt;
  &lt;main&gt;
    &lt;slot&gt;&lt;/slot&gt;
  &lt;/main&gt;
  &lt;footer&gt;
    &lt;slot name=&quot;footer&quot;&gt;&lt;/slot&gt;
  &lt;/footer&gt;
&lt;/div&gt; 
</code></pre>
<p>一个不带 name 的 <code>&lt;slot&gt;</code> 出口会带有隐含的名字“default”</p>
<p>在向具名插槽提供内容的时候，我们可以在一个<code>&lt;template&gt;</code> 元素上使用 v-slot 指令，并以 v-slot 的参数的形式提供其名称</p>
<pre><code class="language-html">&lt;base-layout&gt;
  &lt;template v-slot:header&gt;
    &lt;h1&gt;Here might be a page title&lt;/h1&gt;
  &lt;/template&gt;

  &lt;p&gt;A paragraph for the main content.&lt;/p&gt;
  &lt;p&gt;And another one.&lt;/p&gt;

  &lt;template v-slot:footer&gt;
    &lt;p&gt;Here's some contact info&lt;/p&gt;
  &lt;/template&gt;
&lt;/base-layout&gt;
</code></pre>
<p>任何没有被包裹在带有 v-slot 的 <code>&lt;template&gt;</code> 中的内容都会被视为默认插槽的内容。</p>
<p><strong>注意 v-slot 只能添加在 <code>&lt;template&gt;</code> 上</strong> (只有一种例外情况-&gt;独占默认插槽的缩写语法，不带参数的 v-slot 被假定对应默认插槽，同时不支持和具名插槽混用)，这一点和已经废弃的 slot attribute 不同。</p>
<h2 id="作用域插槽">作用域插槽</h2>
<p>用于<strong>让插槽内容能够访问子组件中才有的数据</strong>。</p>
<pre><code class="language-html">&lt;span&gt;
  &lt;slot&gt;{{ user.lastName }}&lt;/slot&gt;//子组件内部的property
&lt;/span&gt;
我们可能想换掉备用内容，用名而非姓来显示：
&lt;current-user&gt;
  {{ user.firstName }}//外部无法访问内部的数据
&lt;/current-user&gt;
</code></pre>
<p>然而上述代码不会正常工作，因为只有 <code>&lt;current-user&gt;</code> 组件可以访问到 user 而我们提供的内容是在父级渲染的。</p>
<p>为了让 user 在父级的插槽内容中可用，我们可以将 user 作为<code>&lt;slot&gt;</code>元素的一个 attribute 绑定上去。</p>
<pre><code class="language-html">&lt;span&gt;
  &lt;slot v-bind:user=&quot;user&quot;&gt;//暴露在外层
    {{ user.lastName }}
  &lt;/slot&gt;
&lt;/span&gt;
</code></pre>
<p>绑定在 <code>&lt;slot&gt;</code> 元素上的 attribute 被称为插槽 prop。现在在父级作用域中，我们可以使用带值的 v-slot 来定义我们提供的插槽 prop 的名字</p>
<pre><code>&lt;current-user&gt;html
  &lt;template v-slot:default=&quot;slotProps&quot;&gt;//使用带值的 v-slot 来定义我们提供的插槽 prop 的名字
    {{ slotProps.user.firstName }}
  &lt;/template&gt;
&lt;/current-user&gt;
</code></pre>
<p>在这个例子中，我们选择将包含所有插槽 prop 的对象命名为 slotProps，但你也可以使用任意你喜欢的名字。</p>
<h3 id="独占默认插槽的缩写语法">独占默认插槽的缩写语法</h3>
<p>在上述情况下，当被提供的内容<strong>只有默认插槽</strong>时，组件的标签才可以被当作插槽的模板来使用。这样我们就可以把 v-slot 直接用在组件上。</p>
<p>这种写法还可以更简单。就像假定未指明的内容对应默认插槽一样，不带参数的 v-slot 被假定对应默认插槽。</p>
<pre><code class="language-html">&lt;current-user v-slot=&quot;slotProps&quot;&gt;
  {{ slotProps.user.firstName }}
&lt;/current-user&gt;
</code></pre>
<p>注意默认插槽的缩写语法不能和具名插槽混用，因为它会导致作用域不明确.</p>
<pre><code class="language-html">&lt;!-- 无效，会导致警告 --&gt;
&lt;current-user v-slot=&quot;slotProps&quot;&gt;
  {{ slotProps.user.firstName }}
  &lt;template v-slot:other=&quot;otherSlotProps&quot;&gt;
    slotProps is NOT available here
  &lt;/template&gt;
&lt;/current-user&gt;
</code></pre>
<p><strong>只要出现多个插槽，请始终为所有的插槽使用完整的基于 <code>&lt;template&gt;</code> 的语法</strong></p>
<h3 id="解构插槽-prop">解构插槽 Prop</h3>
<p>作用域插槽的内部工作原理是将你的插槽内容包括在一个传入单个参数的函数里。</p>
<pre><code class="language-html">function (slotProps) {
  // 插槽内容
}
</code></pre>
<p>这意味着 v-slot 的值实际上可以是任何能够作为函数定义中的参数的 JavaScript 表达式。</p>
<p>所以在支持的环境下 (单文件组件或现代浏览器)，你也可以使用 ES2015 解构来传入具体的插槽 prop。</p>
<pre><code class="language-html">&lt;current-user v-slot=&quot;{ user }&quot;&gt;
  {{ user.firstName }}
&lt;/current-user&gt;
</code></pre>
<p>这样可以使模板更简洁，尤其是在该插槽提供了多个 prop 的时候。它同样开启了 prop 重命名等其它可能。</p>
<pre><code class="language-html">将 user 重命名为 person

&lt;current-user v-slot=&quot;{ user: person }&quot;&gt;
  {{ person.firstName }}
&lt;/current-user&gt;

甚至可以定义后备内容，用于插槽 prop 是 undefined 的情形
&lt;current-user v-slot=&quot;{ user = { firstName: 'Guest' } }&quot;&gt;
  {{ user.firstName }}
&lt;/current-user&gt;
</code></pre>
<h2 id="动态插槽名">动态插槽名</h2>
<p>动态指令参数也可以用在 v-slot 上，来定义动态的插槽名</p>
<pre><code>&lt;base-layout&gt;
  &lt;template v-slot:[dynamicSlotName]&gt;
    ...
  &lt;/template&gt;
&lt;/base-layout&gt;
</code></pre>
<h2 id="具体插槽的缩写">具体插槽的缩写</h2>
<p>把参数之前的所有内容 (v-slot:) 替换为字符 #。例如 v-slot:header 可以被重写为 #header</p>
<p><strong>该缩写只在其有参数的时候才可用</strong></p>
<pre><code class="language-html">&lt;!-- 这样会触发一个警告 --&gt;
&lt;current-user #=&quot;{ user }&quot;&gt;
  {{ user.firstName }}
&lt;/current-user&gt;

如果你希望使用缩写的话，你必须始终以明确插槽名取而代之：
&lt;current-user #default=&quot;{ user }&quot;&gt;
  {{ user.firstName }}
&lt;/current-user&gt;
</code></pre>
<h2 id="其他示例">其他示例</h2>
<p>插槽 prop 允许我们将插槽转换为可复用的模板，这些模板可以基于输入的 prop 渲染出不同的内容。</p>
<p>要实现一个 <code>&lt;todo-list&gt;</code> 组件，它是一个列表且包含布局和过滤逻辑</p>
<pre><code class="language-html">&lt;ul&gt;
  &lt;li
    v-for=&quot;todo in filteredTodos&quot;
    v-bind:key=&quot;todo.id&quot;
  &gt;
    {{ todo.text }}
  &lt;/li&gt;
&lt;/ul&gt;
</code></pre>
<p>我们可以将每个 todo 作为父级组件的插槽，以此通过父级组件对其进行控制，然后将 todo 作为一个插槽 prop 进行绑定</p>
<pre><code class="language-html">&lt;ul&gt;
  &lt;li
    v-for=&quot;todo in filteredTodos&quot;
    v-bind:key=&quot;todo.id&quot;
  &gt;
    &lt;!--
    我们为每个 todo 准备了一个插槽，
    将 `todo` 对象作为一个插槽的 prop 传入。
    --&gt;
    &lt;slot name=&quot;todo&quot; v-bind:todo=&quot;todo&quot;&gt;
      &lt;!-- 后备内容 --&gt;
      {{ todo.text }}
    &lt;/slot&gt;
  &lt;/li&gt;
&lt;/ul&gt;
</code></pre>
<p>现在当我们使用 <code>&lt;todo-list&gt;</code>组件的时候，我们可以选择为 todo 定义一个不一样的 <code>&lt;template&gt;</code> 作为替代方案，并且可以从子组件获取数据。</p>
<pre><code class="language-html">&lt;todo-list v-bind:todos=&quot;todos&quot;&gt;
  &lt;template v-slot:todo=&quot;{ todo }&quot;&gt;
    &lt;span v-if=&quot;todo.isComplete&quot;&gt;✓&lt;/span&gt;
    {{ todo.text }}
  &lt;/template&gt;
&lt;/todo-list&gt;
</code></pre>
<h2 id="废弃了的语法">废弃了的语法</h2>
<p>在接下来所有的 2.x 版本中 slot 和 slot-scope attribute 仍会被支持，但已经被官方废弃且不会出现在 Vue 3 中。</p>
<h3 id="带有-slot-attribute-的具名插槽">带有 slot attribute 的具名插槽</h3>
<p>在 <code>&lt;template&gt;</code> 上使用特殊的 slot attribute，可以将内容从父级传给具名插槽 (把这里提到过的 <code>&lt;base-layout&gt;</code> 组件作为示例)</p>
<pre><code class="language-html">&lt;base-layout&gt;
  &lt;template slot=&quot;header&quot;&gt;
    &lt;h1&gt;Here might be a page title&lt;/h1&gt;
  &lt;/template&gt;

  &lt;p&gt;A paragraph for the main content.&lt;/p&gt;
  &lt;p&gt;And another one.&lt;/p&gt;

  &lt;template slot=&quot;footer&quot;&gt;
    &lt;p&gt;Here's some contact info&lt;/p&gt;
  &lt;/template&gt;
&lt;/base-layout&gt;
</code></pre>
<p>或者直接把 slot attribute 用在一个普通元素上：</p>
<pre><code class="language-html">&lt;base-layout&gt;
  &lt;h1 slot=&quot;header&quot;&gt;Here might be a page title&lt;/h1&gt;

  &lt;p&gt;A paragraph for the main content.&lt;/p&gt;
  &lt;p&gt;And another one.&lt;/p&gt;

  &lt;p slot=&quot;footer&quot;&gt;Here's some contact info&lt;/p&gt;
&lt;/base-layout&gt;
</code></pre>
<p>未命名插槽，也就是默认插槽，捕获所有未被匹配的内容。</p>
<p>类似于具名插槽</p>
<h3 id="带有-slot-scope-attribute-的作用域插槽">带有 slot-scope attribute 的作用域插槽</h3>
<p>在 <code>&lt;template&gt;</code> 上使用特殊的 slot-scope attribute，可以接收传递给插槽的 prop。</p>
<pre><code class="language-html">&lt;slot-example&gt;
  &lt;template slot=&quot;default&quot; slot-scope=&quot;slotProps&quot;&gt;
    {{ slotProps.msg }}
  &lt;/template&gt;
&lt;/slot-example&gt;
这里的 slot=&quot;default&quot; 可以被忽略为隐性写法：
&lt;slot-example&gt;
  &lt;template slot-scope=&quot;slotProps&quot;&gt;
    {{ slotProps.msg }}
  &lt;/template&gt;
&lt;/slot-example&gt;
</code></pre>
<p>类似于作用域插槽</p>
<p>这里的 slot-scope 声明了被接收的 prop 对象会作为 slotProps 变量存在于<code>&lt;template&gt;</code> 作用域中。你可以像命名 JavaScript 函数参数一样随意命名 slotProps。</p>
<p>slot-scope attribute 也可以直接用于非 <code>&lt;template&gt;</code> 元素 (包括组件)。</p>
<p>slot-scope 的值可以接收任何有效的可以出现在函数定义的参数位置上的 JavaScript 表达式。这意味着在支持的环境下 (单文件组件或现代浏览器)，你也可以在表达式中使用 ES2015 解构</p>
<pre><code class="language-html">&lt;slot-example&gt;
  &lt;span slot-scope=&quot;{ msg }&quot;&gt;
    {{ msg }}
  &lt;/span&gt;
&lt;/slot-example&gt;
</code></pre>
<p><code>&lt;todo-list&gt;</code>等价的使用 slot-scope 的代码是</p>
<pre><code class="language-html">//废弃语法
&lt;todo-list v-bind:todos=&quot;todos&quot;&gt;
  &lt;template slot=&quot;todo&quot; slot-scope=&quot;{ todo }&quot;&gt;
    &lt;span v-if=&quot;todo.isComplete&quot;&gt;✓&lt;/span&gt;
    {{ todo.text }}
  &lt;/template&gt;
&lt;/todo-list&gt;

//新语法
对比于：
&lt;todo-list v-bind:todos=&quot;todos&quot;&gt;
  &lt;template v-slot:todo=&quot;{ todo }&quot;&gt;
    &lt;span v-if=&quot;todo.isComplete&quot;&gt;✓&lt;/span&gt;
    {{ todo.text }}
  &lt;/template&gt;
&lt;/todo-list&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Vue学习笔记7-深入了解组件1-组件注册、Prop、自定义事件]]></title>
        <id>https://giriawsh.github.io/post/vue-xue-xi-bi-ji-7-shen-ru-liao-jie-zu-jian-1-zu-jian-zhu-ce-propzi-ding-yi-shi-jian/</id>
        <link href="https://giriawsh.github.io/post/vue-xue-xi-bi-ji-7-shen-ru-liao-jie-zu-jian-1-zu-jian-zhu-ce-propzi-ding-yi-shi-jian/">
        </link>
        <updated>2020-09-10T07:17:52.000Z</updated>
        <summary type="html"><![CDATA[<h1 id="前言">‍🏃前言</h1>
<p>组件的深入理解，这块基本是官方文档的重要知识点梳理，详情可移步：<a href="https://cn.vuejs.org/v2/guide/components-slots.html">官方文档</a></p>
]]></summary>
        <content type="html"><![CDATA[<h1 id="前言">‍🏃前言</h1>
<p>组件的深入理解，这块基本是官方文档的重要知识点梳理，详情可移步：<a href="https://cn.vuejs.org/v2/guide/components-slots.html">官方文档</a></p>
<!-- more -->
<h1 id="组件注册">组件注册</h1>
<h2 id="组件名">组件名</h2>
<pre><code>Vue.component('my-component-name', { /* ... */ })
</code></pre>
<p>该组件名就是 Vue.component 的第一个参数。</p>
<h2 id="命名风格">命名风格</h2>
<p>使用 kebab-case</p>
<pre><code>Vue.component('my-component-name', { /* ... */ })
</code></pre>
<p>使用 PascalCase</p>
<pre><code>Vue.component('MyComponentName', { /* ... */ })
</code></pre>
<h2 id="全局注册">全局注册</h2>
<pre><code>Vue.component('component-a', { /* ... */ })
Vue.component('component-b', { /* ... */ })
Vue.component('component-c', { /* ... */ })

new Vue({ el: '#app' })

&lt;div id=&quot;app&quot;&gt;
  &lt;component-a&gt;&lt;/component-a&gt;
  &lt;component-b&gt;&lt;/component-b&gt;
  &lt;component-c&gt;&lt;/component-c&gt;
&lt;/div&gt;
</code></pre>
<p>这些组件是全局注册的。也就是说它们在注册之后可以用在任何新创建的 Vue 根实例 (new Vue) 的模板中。</p>
<p><strong>全局注册往往是不够理想的。比如，如果你使用一个像 webpack 这样的构建系统，全局注册所有的组件意味着即便你已经不再使用一个组件了，它仍然会被包含在你最终的构建结果中。这造成了用户下载的 JavaScript 的无谓的增加。</strong></p>
<h2 id="局部注册">局部注册</h2>
<pre><code>var ComponentA = { /* ... */ }
var ComponentB = { /* ... */ }
var ComponentC = { /* ... */ }

new Vue({
  el: '#app',
  components: {
    'component-a': ComponentA,
    'component-b': ComponentB
  }
})
</code></pre>
<p>注意局部注册的组件<strong>在其子组件中不可用</strong>。如果需要可用，需要重新注册。</p>
<pre><code>import ComponentA from './ComponentA.vue'

export default {
  components: {
    ComponentA
  },
  // ...
}
</code></pre>
<p>注意在 ES2015+ 中，在对象中放一个类似 ComponentA 的变量名其实是 ComponentA: ComponentA 的缩写，即这个变量名同时是：</p>
<ul>
<li>用在模板中的自定义元素的名称</li>
<li>包含了这个组件选项的变量名</li>
</ul>
<h2 id="模块系统">模块系统</h2>
<p>可能你的许多组件只是包裹了一个输入框或按钮之类的元素，是相对通用的。我们有时候会把它们称为基础组件，它们会在各个组件中被频繁的用到。所以会导致很多组件里都会有一个包含基础组件的长列表。</p>
<p>如果你恰好使用了 webpack (或在内部使用了 webpack 的 Vue CLI 3+)，那么就可以使用 require.context 只全局注册这些非常通用的基础组件。这里有一份可以让你在应用入口文件 (比如 src/main.js) 中全局导入基础组件的示例代码</p>
<pre><code>import Vue from 'vue'
import upperFirst from 'lodash/upperFirst'
import camelCase from 'lodash/camelCase'

const requireComponent = require.context(
  // 其组件目录的相对路径
  './components',
  // 是否查询其子目录
  false,
  // 匹配基础组件文件名的正则表达式
  /Base[A-Z]\w+\.(vue|js)$/
)

requireComponent.keys().forEach(fileName =&gt; {
  // 获取组件配置
  const componentConfig = requireComponent(fileName)

  // 获取组件的 PascalCase 命名
  const componentName = upperFirst(
    camelCase(
      // 获取和目录深度无关的文件名
      fileName
        .split('/')
        .pop()
        .replace(/\.\w+$/, '')
    )
  )

  // 全局注册组件
  Vue.component(
    componentName,
    // 如果这个组件选项是通过 `export default` 导出的，
    // 那么就会优先使用 `.default`，
    // 否则回退到使用模块的根。
    componentConfig.default || componentConfig
  )
})
</code></pre>
<p><strong>全局注册的行为必须在根 Vue 实例 (通过 new Vue) 创建之前发生</strong></p>
<h1 id="prop">Prop</h1>
<p>HTML 中的 attribute 名是大小写不敏感的，所以浏览器会把所有大写字符解释为小写字符。这意味着当你使用 DOM 中的模板时，camelCase (驼峰命名法) 的 prop 名需要使用其等价的 kebab-case (短横线分隔命名) 命名。</p>
<pre><code>Vue.component('blog-post', {
  // 在 JavaScript 中是 camelCase 的
  props: ['postTitle'],
  template: '&lt;h3&gt;{{ postTitle }}&lt;/h3&gt;'
})

&lt;!-- 在 HTML 中是 kebab-case 的 --&gt;
&lt;blog-post post-title=&quot;hello!&quot;&gt;&lt;/blog-post&gt;
</code></pre>
<p>如果你使用字符串模板，那么这个限制就不存在了。</p>
<h2 id="prop类型">Prop类型</h2>
<p>通常你希望每个 prop 都有指定的值类型。这时，你可以以对象形式列出 prop，这些 property 的名称和值分别是 prop 各自的名称和类型。</p>
<pre><code>props: {
  title: String,
  likes: Number,
  isPublished: Boolean,
  commentIds: Array,
  author: Object,
  callback: Function,
  contactsPromise: Promise // or any other constructor
}
</code></pre>
<h2 id="传递静态或者动态prop">传递静态或者动态prop</h2>
<p>prop可以传入一个静态值，也可以通过v-bind动态赋值。</p>
<p>可通过v-bind传入数字、布尔值、数组、对象、传入一个对象的所有property</p>
<pre><code>post: {
  id: 1,
  title: 'My Journey with Vue'
}
&lt;blog-post v-bind=&quot;post&quot;&gt;&lt;/blog-post&gt;

等价于
&lt;blog-post
  v-bind:id=&quot;post.id&quot;
  v-bind:title=&quot;post.title&quot;
&gt;&lt;/blog-post&gt;
</code></pre>
<h2 id="单向数据流">单向数据流</h2>
<p>所有的 prop 都使得其父子 prop 之间形成了一个<strong>单向下行绑定</strong>。</p>
<p>父级 prop 的更新会向下流动到子组件中，但是反过来则不行。这样会防止从子组件意外变更父级组件的状态，从而导致你的应用的数据流向难以理解。</p>
<p>额外的，每次父级组件发生变更时，子组件中所有的 prop 都将会刷新为最新的值。这意味着你不应该在一个子组件内部改变 prop。如果你这样做了，Vue 会在浏览器的控制台中发出警告。</p>
<p>两种典型变更prop情形：</p>
<ol>
<li>这个 prop 用来传递一个初始值；<strong>这个子组件接下来希望将其作为一个本地的 prop 数据来使用</strong>。在这种情况下，最好定义一个本地的 <strong>data property</strong> 并将这个 prop 用作其初始值</li>
</ol>
<pre><code>props: ['initialCounter'],
data: function () {
  return {
    counter: this.initialCounter
  }
}
</code></pre>
<ol start="2">
<li>这个 prop 以一种原始的值传入<strong>且需要进行转换</strong>。在这种情况下，最好使用这个 prop 的值来定义一个<strong>计算属性</strong></li>
</ol>
<pre><code>props: ['size'],
computed: {
  normalizedSize: function () {
    return this.size.trim().toLowerCase()
  }
}
</code></pre>
<blockquote>
<p>注意在 JavaScript 中对象和数组是通过<strong>引用传入</strong>的，所以对于一个数组或对象类型的 prop 来说，在子组件中改变变更这个对象或数组本身<strong>将会影响到父组件的状态</strong>。</p>
</blockquote>
<h2 id="prop验证">Prop验证</h2>
<p>我们可以为组件的 prop 指定验证要求，例如你知道的这些类型。如果有一个需求没有被满足，则 Vue 会在浏览器控制台中警告你。这在开发一个会被别人用到的组件时尤其有帮助。</p>
<p>为了定制 prop 的验证方式，你可以为 props 中的值提供一个带有验证需求的对象，而不是一个字符串数组。<br>
这里的demo详见组件板块的学习笔记。</p>
<blockquote>
<p>注意那些 prop 会在一个<strong>组件实例创建之前进行验证</strong>，所以实例的 <strong>property (如 data、computed 等) 在 default 或 validator 函数中是不可用的</strong>。</p>
</blockquote>
<h3 id="类型检查">类型检查</h3>
<p>type 可以是下列原生构造函数中的一个：</p>
<pre><code>String
Number
Boolean
Array
Object
Date
Function
Symbol
</code></pre>
<p>额外的，type 还可以是一个自定义的构造函数，并且通过 instanceof 来进行检查确认。</p>
<h2 id="非prop的attribute">非Prop的Attribute</h2>
<p>因为显式定义的 prop 适用于向一个子组件传入信息，然而组件库的作者并不总能预见组件会被用于怎样的场景。这也是为什么组件可以接受任意的 attribute，而这些 attribute 会被添加到这个组件的根元素上。</p>
<p>例如，想象一下你通过一个 Bootstrap 插件使用了一个第三方的 <code>&lt;bootstrap-date-input&gt;</code>组件，这个插件需要在其 <code>&lt;input&gt;</code> 上用到一个 data-date-picker attribute。我们可以将这个 attribute 添加到你的组件实例上</p>
<pre><code>&lt;bootstrap-date-input data-date-picker=&quot;activated&quot;&gt;&lt;/bootstrap-date-input&gt;
</code></pre>
<p>然后这个 data-date-picker=&quot;activated&quot; attribute 就会自动添加到 <code>&lt;bootstrap-date-input&gt;</code>  的根元素上。</p>
<h3 id="替换合并已有的-attribute">替换/合并已有的 Attribute</h3>
<pre><code>&lt;bootstrap-date-input&gt;的内部模板：
&lt;input type=&quot;date&quot; class=&quot;form-control&quot;&gt;

外部模板：
&lt;bootstrap-date-input
  data-date-picker=&quot;activated&quot;
  class=&quot;date-picker-theme-dark&quot;
&gt;&lt;/bootstrap-date-input&gt;
</code></pre>
<p>在这种情况下，我们定义了两个不同的 class 的值：</p>
<ul>
<li>form-control，这是在组件的模板内设置好的</li>
<li>date-picker-theme-dark，这是从组件的父级传入的</li>
</ul>
<p>class 和 style attribute 会稍微智能一些，即两边的值会被合并起来，从而得到最终的值：form-control date-picker-theme-dark。</p>
<h3 id="禁用attribute继承待补充">禁用Attribute继承？（待补充）</h3>
<p>如果你<strong>不希望组件的根元素继承 attribute</strong>，你可以在组件的选项中设置 inheritAttrs: false。</p>
<pre><code>Vue.component('my-component', {
  inheritAttrs: false,
  // ...
})
</code></pre>
<p>这尤其适合配合实例的 $attrs property 使用，该 property 包含了传递给一个组件的 attribute 名和 attribute 值</p>
<pre><code>{
  required: true,
  placeholder: 'Enter your username'
}
</code></pre>
<p>有了 inheritAttrs: false 和 $attrs，你就可以手动决定这些 attribute 会被赋予哪个元素。在撰写基础组件的时候是常会用到的</p>
<pre><code>Vue.component('base-input', {
  inheritAttrs: false,
  props: ['label', 'value'],
  template: `
    &lt;label&gt;
      {{ label }}
      &lt;input
        v-bind=&quot;$attrs&quot;
        v-bind:value=&quot;value&quot;
        v-on:input=&quot;$emit('input', $event.target.value)&quot;
      &gt;
    &lt;/label&gt;
  `
})
</code></pre>
<blockquote>
<p>注意 inheritAttrs: false 选项<strong>不会影响 style 和 class 的绑定</strong></p>
</blockquote>
<h1 id="自定义事件">自定义事件</h1>
<h2 id="事件名">事件名</h2>
<p>不同于组件和 prop，事件名不存在任何自动化的大小写转换。而是触发的事件名需要<strong>完全匹配</strong>监听这个事件所用的名称。</p>
<p>不同于组件和 prop，事件名不会被用作一个 JavaScript 变量名或 property 名，所以就没有理由使用 camelCase 或 PascalCase 了。并且 v-on 事件监听器在 DOM 模板中会被自动转换为全小写 (因为 HTML 是大小写不敏感的)，所以 v-on:myEvent 将会变成 v-on:myevent——导致 myEvent 不可能被监听到。</p>
<p><strong>因此，我们推荐你始终使用 kebab-case 的事件名。</strong></p>
<h2 id="自定义组件的v-model">自定义组件的v-model</h2>
<p>一个组件上的 v-model 默认会利用名为 value 的 prop 和名为 input 的事件，但是像单选框、复选框等类型的输入控件可能会将 value attribute 用于不同的目的。model 选项可以用来避免这样的冲突</p>
<pre><code>Vue.component('base-checkbox', {
  model: {
    prop: 'checked',
    event: 'change'
  },
  props: {
    checked: Boolean //注意你仍然需要在组件的 props 选项里声明 checked 这个 prop。
  },
  template: `
    &lt;input
      type=&quot;checkbox&quot;
      v-bind:checked=&quot;checked&quot;
      v-on:change=&quot;$emit('change', $event.target.checked)&quot;
    &gt;
  `
})
现在在这个组件上使用 v-model 的时候：
&lt;base-checkbox v-model=&quot;lovingVue&quot;&gt;&lt;/base-checkbox&gt;
</code></pre>
<p>这里的 lovingVue 的值将会传入这个名为 <strong>checked</strong> 的 prop。同时当 <code>&lt;base-checkbox&gt;</code> 触发一个 <strong>change 事件</strong>并附带一个新的值的时候，这个 lovingVue 的 property 将会被更新。</p>
<h2 id="将原生事件绑定到组件">将原生事件绑定到组件</h2>
<p>想要在一个组件的根元素上直接监听一个原生事件。这时，你可以使用 v-on 的 .native 修饰符。</p>
<pre><code>&lt;base-input v-on:focus.native=&quot;onFocus&quot;&gt;&lt;/base-input&gt;
</code></pre>
<p>不过在你尝试监听一个类似 <code>&lt;input&gt;</code> 的非常特定的元素时，这并不是个好主意。比如上述<code>&lt;base-input&gt;</code> 组件可能做了如下重构，<strong>所以根元素实际上是一个 <code>&lt;label&gt;</code> 元素</strong></p>
<pre><code>&lt;label&gt;
  {{ label }}
  &lt;input
    v-bind=&quot;$attrs&quot;
    v-bind:value=&quot;value&quot;
    v-on:input=&quot;$emit('input', $event.target.value)&quot;
  &gt;
&lt;/label&gt;
</code></pre>
<p>这时，父级的 .native 监听器将静默失败。它不会产生任何报错，但是 onFocus 处理函数不会如你预期地被调用。</p>
<p>为了解决这个问题，Vue 提供了一个 <strong>$listeners property</strong>，它是一个对象，里面包含了作用在这个组件上的所有监听器。</p>
<pre><code>{
  focus: function (event) { /* ... */ }
  input: function (value) { /* ... */ },
}
</code></pre>
<p>有了这个 <code>$listeners property</code>，你就可以配合<code>v-on=&quot;$listeners&quot;</code> 将所有的事件监听器指向这个组件的某个特定的子元素。</p>
<p>类似 <code>&lt;input&gt;</code> 的你希望它也可以配合 v-model 工作的组件来说，为这些监听器创建一个类似下述 inputListeners 的计算属性。</p>
<pre><code>Vue.component('base-input', {
  inheritAttrs: false,
  props: ['label', 'value'],
  computed: {
    inputListeners: function () {
      var vm = this
      // `Object.assign` 将所有的对象合并为一个新对象
      return Object.assign({},
        // 我们从父级添加所有的监听器
        this.$listeners,
        // 然后我们添加自定义监听器，
        // 或覆写一些监听器的行为
        {
          // 这里确保组件配合 `v-model` 的工作
          input: function (event) {
            vm.$emit('input', event.target.value)
          }
        }
      )
    }
  },
  template: `
    &lt;label&gt;
      {{ label }}
      &lt;input
        v-bind=&quot;$attrs&quot;
        v-bind:value=&quot;value&quot;
        v-on=&quot;inputListeners&quot;
      &gt;
    &lt;/label&gt;
  `
})
</code></pre>
<p>现在 <code>&lt;base-input&gt;</code> 组件是一个完全透明的包裹器了，也就是说它可以完全像一个普通的 <code>&lt;input&gt;</code> 元素一样使用了：所有跟它相同的 attribute 和监听器都可以工作，不必再使用 .native 监听器。</p>
<h2 id="sync修饰符">.sync修饰符</h2>
<p>在有些情况下，我们可能需要对一个 prop 进行“双向绑定”。不幸的是，真正的双向绑定会带来维护上的问题，因为子组件可以变更父组件，且在父组件和子组件都没有明显的变更来源。</p>
<p>我们推荐以<strong>update:myPropName</strong> 的模式触发事件取而代之。</p>
<pre><code>子组件：
this.$emit('update:title', newTitle)

父组件可以监听那个事件并根据需要更新一个本地的数据 property
&lt;text-document
  v-bind:title=&quot;doc.title&quot;
  v-on:update:title=&quot;doc.title = $event&quot;
&gt;&lt;/text-document&gt;
</code></pre>
<p>为了方便起见，我们为这种模式提供一个缩写，即 .sync 修饰符</p>
<pre><code>&lt;text-document v-bind:title.sync=&quot;doc.title&quot;&gt;&lt;/text-document&gt;
</code></pre>
<p>注意带有 .sync 修饰符的 v-bind <strong>不能和表达式一起使用</strong> (例如 v-bind:title.sync=”doc.title + ‘!’” 是无效的)。取而代之的是，你只能提供你想要绑定的 property 名，类似 v-model。</p>
<pre><code>&lt;text-document v-bind.sync=&quot;doc&quot;&gt;&lt;/text-document&gt;
</code></pre>
<p>这样会把 doc 对象中的每一个 property (如 title) 都作为一个独立的 prop 传进去，然后各自添加用于更新的 v-on 监听器。</p>
<p>将 v-bind.sync 用在一个字面量的对象上，例如 <code>v-bind.sync=”{ title: doc.title }”</code>，是无法正常工作的</p>
]]></content>
    </entry>
</feed>